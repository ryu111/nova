# `驗證紅` 連續兩次是綠的就收護欄

## 固定負控

`卡住了` 原本只數紅，`測試 → 驗證紅（竟然是綠的）→ 測試` 這條回頭邊一路上一個紅都沒有，
對它完全隱形；實測有一條線（4eff1c）就這樣繞了 12 圈、燒滿 24 步從沒進到實作。
現在 `驗證紅` 連續 `沒有紅可看門檻`（2）次判成綠就收護欄，
理由是「這張票沒有紅可看（補紀錄／補登記／重構型工作），不是測試員寫錯」，
護欄原因落 `no-red-to-see`。**判定不看工作區雜湊、也不看模型的自由文字。**

| 破壞什麼 | 該紅的測試 | 親眼看到 |
|---|---|---|
| 把 `卡住了` 裡的 `_沒有紅可看(軌跡)` 檢查整段刪掉，退回只數紅 | `tests/單元/test_狀態機.py::Test驗證紅一直是綠的就是沒有紅可看::test_驗證紅連續兩輪都是綠的就收護欄` | KILLED |
| 把 `_沒有紅可看` 的歸零改成累計（`else 0` → `else 連綠次數`），讓它數總綠而不是連綠 | `tests/單元/test_狀態機.py::Test驗證紅一直是綠的就是沒有紅可看::test_中間紅過一次就從頭數` | KILLED |

變異登記在 `tests/負控/登記們/沒有紅可看.py`；本輪手動跑該變異為 1 failed（KILLED），已還原。
