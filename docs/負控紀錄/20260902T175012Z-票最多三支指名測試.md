# 票最多三支指名測試

## 固定負控

登記在 `tests/負控/登記們/三支上限.py`，共三把：

| 破壞什麼 | 預期被哪裡抓到 |
|---|---|
| 把指名測試上限放寬十倍 | 人寫票單元測試、派工整合測試 |
| 拿掉參數化尾巴的剝除 | 人寫票單元測試 |
| 自主來源也查指名測試 | 自主來源單元測試 |

## 實測

2026-09-02T17:50:12Z 執行負控 runner 的「指名」相關選取，其中包含這三筆登記：

- 「參數化尾巴不再剝除」被 `KILLED`。
- 「上限放寬十倍」在 coverage 前置被報 `WRONG_TEST`：登記點名的是未帶參數尾巴的參數化測試函式，runner 的上下文實際帶著參數 id。
- 「自主來源也被指名測試上限攔截」是 `SURVIVED`：自主票的五個候選都在 `FAILED` 開頭的行，而規則明定這些行不算指名。

後兩項是測試／登記問題，不以放寬生產規則或改測試來掩蓋。
