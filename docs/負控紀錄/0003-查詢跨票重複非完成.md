# 查詢跨票重複非完成

## 固定負控（2026-09-01）

這一格的同題判定必須看 `task` 內容，不能看 `run_id`。固定負控把
`_是同一題目` 暫改成比對 `執行識別碼`，用八筆不同識別碼、相同完整 task
的真實形狀資料跑第一支測試，當場得到 1 failed（查到 0 次而不是 8 次）；
再還原成 task 內容比對。

| 破壞什麼 | 該紅的測試 | 親眼看到 |
|---|---|---|
| `_是同一題目` 改成比對 `執行識別碼` | `tests/整合/test_重複失敗.py::Test查重複失敗::test_真實八次護欄紀錄查得出重複失敗與累積花費` | 1 failed |

