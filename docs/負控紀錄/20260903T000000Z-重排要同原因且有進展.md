# 重排要同一種護欄、而且有進展

## 固定負控

守的是 `_也許接著排` 那道新判準：撞到護欄不再等於自動續命。

| 破壞什麼 | 預期被哪裡抓到 |
|---|---|
| 拿掉 `收件.跳開的理由` 裡的護欄原因比對（`上一輪原因 != 這一輪原因` 那一段） | `tests/單元/test_接續票.py::Test原因換了就不再排::test_上一輪測試檔護欄這一輪步數就不排` |

## 實測

2026-09-03 手動把 `src/nova/載體/收件.py:跳開的理由` 的原因比對整段刪掉，跑
`uv run pytest tests/單元/test_接續票.py`：

- 「上一輪測試檔護欄、這一輪步數就不排」**紅**——上一輪是 `touched-tests`、
  這一輪是 `steps`，比對沒了就退回「任何 4 都重排」，第 2 輪的票真的落進收件匣
  （斷言打在 `收下一件(匣) is None` 上）。
- 另外兩支（同原因看有沒有進展）照舊綠：它們守的是階段順序那一段，
  跟原因比對是兩把不同的刀，**不會互相掩護**。

改回來之後三支全綠。
