# 角色提示回饋：審查員範圍外發現必須寫 FOLLOW-UP

## 固定負控

登記在 `tests/負控/登記們/角色提示回饋.py`。

| 破壞什麼 | 該紅測試 | 判準 |
|---|---|---|
| 刪掉 `src/nova/迴圈/角色提示.py` 審查員規矩第 1 行「範圍外的發現寫 FOLLOW-UP」整句 | `tests/單元/test_角色提示.py::Test退回原因已回饋進提示::test_審查員只審票的範圍範圍外寫FOLLOW_UP` | 變異後必須失敗；若仍通過就是 `SURVIVED` |

這把刀只驗證提示保留範圍外回報的形狀，不驗證審查流程是否真的收集或計數 `FOLLOW-UP`。

## 實測

2026-09-02T16:53:57Z 只執行這一筆登記的負控 runner，結果為 `KILLED`：基線哨兵通過，刪句後該哨兵失敗。
