# 固定負控紀錄

> 自動化負控的執行真相住在 [`tests/負控/登記.py`](../tests/負控/登記.py)；runner 依登記執行，
> 這份 Markdown 只保留人可讀的紀錄，不反過來當規格來源。本票登記的 19 刀於 2026-08-31
> 在本機實測為 21.90 秒。
> 這份 Markdown 只保留人可讀的紀錄，不反過來當規格來源。前一輪登記的 12 刀於 2026-08-30
> 在本機實測為 9.77 秒。
> 這份 Markdown 只保留人可讀的紀錄，不反過來當規格來源。本票登記的 14 刀於 2026-08-30
> 在本機實測為 15.12 秒。
> 這份 Markdown 只保留人可讀的紀錄，不反過來當規格來源。本票登記的 19 刀於 2026-08-30
> 在本機實測為 20.63 秒。
> 這份 Markdown 只保留人可讀的紀錄，不反過來當規格來源。本票登記的 13 刀於 2026-08-30
> 在本機實測為 13.66 秒。
> 這份 Markdown 只保留人可讀的紀錄，不反過來當規格來源。本票登記的 15 刀於 2026-08-31
> 在本機實測為 15.20 秒。
> 三條完成判準也已實跑：拔掉角色工廠的深度接線會紅、改點無關測試會先報 `WRONG_TEST`、
> 拔掉 `registered-mutation` 規則會讓規則表測試紅；三項驗證後均已還原。

**這是紀錄，不是規範。** 規則本身住 CLAUDE.md 與 `docs/決策/`；
這份檔只回答一個問題：**這條防護，有沒有人親眼看過它紅？**

每一列都是做過一次的實驗：故意把實作弄壞，確認指名的那支測試真的會失敗，再改回來。
所以**下次有人懷疑同一條防護時不必重推**——照著「破壞什麼」那一欄做一次就好。

新增保證時做一次負控，寫進 commit 訊息，然後在這裡補一列。**不要往 CLAUDE.md 加列**
（理由見 [`docs/決策/0001-規則要住哪一層.md`](決策/0001-規則要住哪一層.md)）。

> 這其實是**手工版的 mutation testing**：改一個字元、看測試會不會抓到。
> 業界的機械版是 `cosmic-ray` 或 `mutmut`，會直接算出 mutation score。
> 目前沒導入，理由與前置問題寫在那份決策紀錄的「之後可能怎麼走」。

| 破壞什麼 | 結果 |
|---|---|
| `mkdir nova`（破壞 src layout） | `test_採用_src_layout_而非_flat_layout` 紅 |
| 工具箱拿掉單檔寫入上限 | `test_單次寫入的內容有大小上限` 紅 |
| 工具箱拿掉單輪寫檔上限 | `test_一輪最多寫幾個檔案` 紅 |
| 「不准寫」改成字串前綴比對 | `test_同前綴的兄弟目錄不受影響` 紅 |
| 帳本只算 `success` 收場的 token | `test_unknown收場的token算進總計`、`test_失敗收場的token也算` 紅 |
| 本地能力探針的端點網址指到死 port | `test_長提示不漏中段指令`、`test_拒絕答不出的事實` 紅（通道判定，不是能力判定） |
| 測試員提示拿掉「讀取策略」那段 | `test_提示必須包含讀取策略段落` 紅 |
| 例行第一順位翻回 `agy` | `test_例行工作第一順位是claude` 紅 |
| 推理列拿掉備援變回 `("codex",)` | `test_推理工作要有備援` 紅 |
| 接力換家時把型號帶著走（`換家選項 = 選項`） | `Test接力::test_選項原樣傳給每一顆_型號除外` 紅 |
| 從 `.gitignore` 拿掉 `.env` | `test_git_本人確認會忽略[.env]` 紅 |
| 直接 `git push origin main` | 被 ruleset 擋下（`Changes must be made through a pull request`） |
| 測試檔留了簡體字 | `nova 閘` 的 `lang-traditional` 紅，指到檔名行號 |
| `規則表.py` import 沒排序 | `nova 閘` 的 `ruff-check` 紅，commit 被擋 |
| 寫一個分支複雜度 9 的函式 | `ruff-check` 紅在 `C901 too complex (9 > 8)` |
| 拿掉成功邊包的 `來源` 欄位（成功終局不再帶 provenance） | `test_成功產出必帶_schema_與_provenance` 紅（`邊包` 不接受 `來源`） |
| 新增失敗代碼卻不進 `_終局表` | `test_每個失敗代碼都要有明確的終局` 紅（原本那支不會紅，靠負控才發現） |
| `_成功但沒話說算未知` 改成直接 `return 答` | `Test成功但沒話說` 四支紅（空回應又被當成功） |
| codex 可編輯換回 `--approve-for-me` | `test_codex的可編輯有真的邊界` 紅（真跑 CLI，檔案寫進家目錄） |
| claude 唯讀工具白名單改回 `""` | `test_唯讀看得到工作目錄裡的檔案[claude]` 紅（連 Read 都沒了） |
| agy 唯讀不給 `--add-dir` | `test_agy三種權限都要給add_dir` 紅；真跑則是審查員看不到檔案 |
| 審查階段改回 `種類.模型` | `test_審查要求修改要退回實作` 與 `test_審查沒給判定一律中止` 紅 |
| `git rm` 掉整支測試檔 | `test_整支測試檔被git_rm掉要擋` 紅（基準改走 ls-tree 之前會**放行**） |
| gates.yml 拿掉 `NOVA_TEST_COUNT_BASE` | `test_CI把測試數基準指到base_branch` 紅 |
| gates.yml 拿掉 `git fetch` 那步 | `test_CI有先把基準抓下來` 紅 |
| 把 `test_repo檢查.py` 搬回 `tests/單元/` | `test_單元層不准fork子程序` 紅，指到檔名與痕跡 |
| 解析 claude 時改看 `subtype` 而非 `is_error` | `test_模型不存在_不准看subtype` 紅（實錄裡失敗案例的 `subtype` 也是 `"success"`） |
| 把 `timeout` 的終局改成 `failed` | `test_可能已經做了一半的是結果未知[timeout]` 紅 |
| 新增失敗代碼但不進 `_終局表` | `test_每個失敗代碼都要有明確的終局` 紅 |
| 把 `驗證紅` 的期望改成綠 | 狀態機與工作流共 9 支紅 |
| 判準佔不到機器鎖時改回 `判準終局.紅`（原本是 `跑不起來`） | `test_佔不到鎖是跑不起來不是紅` 紅（`佔不到鎖被判成 red`——工作流會白跑一輪實作） |
| 把 `跑工作流` 的步數上限拿掉 | `test_來回不停會撞到步數上限` **掛住跑不完**（證明沒有 stop rule 就是成本漏洞） |
| 讓驗證階段改由角色做（自寫自評） | `test_判準階段的步驟結果帶紅綠模型階段不帶` 等 4 支紅 |
| 某條閘忘了排除 `真cli` 或 `真端點` 標記 | `test_兩個閘都排除真cli與真端點` 紅 |
| 把家族名檢查加回 `nova.派工` 或 `_子命令_工作流` | `test_命令列也不再用家族名擋同一家`、`test_同一家不再擋在門口` 紅 |
| `派工` 讓兩家共用同一個 `執行檔` | `test_執行檔不准誤用到審查那家` 紅 |
| 門面多匯出一個名字 | `test_門面很小` 紅 |
| 平行度改回 `-n auto` | `test_規則表用的是算出來的數字不是auto` 紅 |
| `平行成數` 改成 1.0（吃滿） | `test_不會吃滿` 等 5 支紅 |
| 接力在可編輯下遇到結果未知也換腦 | `test_結果未知在可編輯時不准換` 等 2 支紅 |
| 接力全掛時不留「試過誰」的證據 | `test_全部失敗要留下試過誰的證據` 等 2 支紅 |
| 逾時預設調回 300 秒 | `test_預設夠寬` 紅 |
| 文件提到一支不存在的測試 | `test_文件提到的測試都真的存在` 紅 |
| codex 的 `-c` 值沒包引號（TOML 解析不出來） | `test_codex的推理強度值是合法TOML` 紅 |
| claude 的隔離換回 `--bare` | `test_claude用setting_sources隔離而不是bare` 紅 |
| codex 續接時仍給 `--sandbox` | `test_codex續接時不准給sandbox或核准旗標` 紅 |
| 解析器改回嚴格 JSON | `test_response裡有原始換行也解得動` 紅 |
| 禁令改回「拆不開一律擋」 | `test_拆不開但沒有禁令要放行` 紅 |
| 帳本拿掉每筆的 `flush()` | `test_程序被殺掉之前寫的那筆還在` 紅（SIGKILL 不給清緩衝區的機會） |
| 帳本的欄位對應表少一格 | `test_每個欄位都有對應` 紅，外加 12 支連帶紅 |
| 帳本記帳失敗改成往上丟例外 | `Test帳本壞掉` 三支紅（會害呼叫端重跑已經做過的事） |
| `記帳每一顆` 忘了編 `接力第幾顆` | `test_每一顆各有一對事件` 紅 |
| 把模型全文塞進 `文字雜湊` 欄位 | `test_模型全文會記進去` 紅（這一列的測試 2026-08-30 改名了：遮罩做出來之後，全文從「不准記」變成「記，但一定遮過」）|
| 負控紀錄裡寫一支不存在的測試 | `test_文件提到的測試都真的存在` 紅 |
| CLAUDE.md 加 30 行流水帳 | `test_CLAUDE_md不准無限長` 紅 |
| 門面 `_建腦` 不包 `記帳每一顆` | `test_接力的每一顆都記到` 等 3 支紅 |
| `派工` 不套 `記帳執行器` | `test_派工把七個階段都記下來` 紅 |
| 門面改成不給目錄也開帳本 | `test_不給帳本目錄就不記帳` 紅（**修正斷言位置之後才會紅**，見下） |
| CLI 改成預設不記帳 | `test_問預設就記帳` 紅 |
| `記帳執行器` 的結束事件不寫在 `finally` | `test_執行器炸了也要留下結束事件` 紅 |
| 讀取端把認不得的事件當正常事件 | `test_認不得的事件種類算壞行` 紅 |
| 階段結束的 token 也加進總數 | `test_階段的token不准重複算` 紅（會變兩倍） |
| 不追蹤只有開始沒有結束的呼叫 | `test_階段也算` 等 3 支紅 |
| 壞行不跳過、直接往上丟 | `test_不是JSON的行要跳過不要爆` 紅 |
| 派工表把例行改成 codex 打頭 | ~~`test_例行工作第一順位是agy`~~ 紅 |
| 推理那格不指名模型 | `test_推理工作要用sol` 等 3 支紅（會靜默拿到便宜的預設） |
| 例行只留一顆腦（沒有備援） | `test_例行工作要有備援` 紅 |
| CLI 忽略 `--工作`、不查派工表 | `test_推理工作會帶上sol` 等 2 支紅 |
| `--工作` 與 `--用` 同時給時挑一個而不報錯 | `test_工作跟模型不准同時給` 等 2 支紅 |
| 測試忘了注入假 CLI ＋ 互斥檢查退化 | `不准摸到真的CLI` fixture 當場 `FileNotFoundError`（0.03 秒，沒出網路；沒有它是 11 秒真呼叫） |
| 生圖的權限降成可編輯 | `test_一定走全開` 紅（可編輯下圖進不了工作目錄，而 CLI 假回報成功） |
| 生圖只看模型說什麼、不看檔案 | `test_沒生出來要用結果未知的退出碼` 等 4 支紅 |
| 生圖看「目錄裡有沒有圖」而不是「多了什麼」 | `test_本來就有的圖不算數` 紅 |
| 生圖沒生出來時降成確定失敗 | `test_本來就有的圖不算數` 等 3 支紅（會害人重跑、再花一次錢） |
| 生圖的提示不叫它搬進工作目錄 | `test_提示會叫它搬進工作目錄` 紅（**修正斷言位置之後才會紅**，見下） |
| 生圖的 `續接` 不往下傳 | `test_續接會把sid傳下去` 等 2 支紅 |
| CLI 沒把 `--續接` 接進生圖 | `test_續接旗標接得上` 紅（旗標在、但沒作用） |
| 生圖加回一個永遠是 True 的 `保留對話` | `test_沒有保留對話這個參數` 紅 |
| 撿回殘骸順便把終局改成成功 | `test_撿回來不准改終局` 紅（拿半成品當成品） |
| 沒有殘骸時默默回原本的訊息 | `test_空檔案跟沒檔案一樣要明講` 等 2 支紅 |
| 殘骸不截斷 | `test_太大的殘骸要截斷` 紅 |
| `--輸出檔` 在唯讀下自動升權而不是擋 | `test_唯讀給輸出檔要當場擋` 紅 |
| CLI 拿到答案之後不撿殘骸 | `test_掛掉之後殘骸會出現在輸出裡` 紅 |
| `跑cli` 拿掉 `stdin=subprocess.DEVNULL` | `test_子程序讀不到父程序的stdin` 紅（子程序讀到父程序的 6 個字元） |
| 逾時例外不帶部分輸出 | `test_逾時之後撿得回sid` 等 3 支紅 |
| 逾時的回應改成從半成品抄終局 | `test_逾時之後終局還是結果未知` 等 3 支紅 |
| 解析器給不出 sid 時不自己撿 | `test_解析器給不出sid就自己撿` 等 2 支紅 |
| `撿對話識別碼` 取最後一個而不是第一個 | `test_codex的thread_id` 等 5 支紅 |
| 部分輸出不截斷 | `test_太長要截斷` 紅（**修正測試用的解析器之後才會紅**，見下） |
| 接力鏈只回最後一顆的用量 | `test_換過腦就要把兩顆都算進去` 等 2 支紅（token 上限會低估） |
| 成本有一顆給不出來也照加 | `test_成本有一顆給不出來就整個不給` 紅（低報的成本比沒有成本更危險） |
| 鏈上缺席時仍然把 `FileNotFoundError` 往上丟 | `test_缺席的那顆要留下痕跡` 等 3 支紅 |
| 只指定一家沒裝時也降級（不炸） | `test_只指定一家沒裝要當場炸` 紅 |
| `失敗代碼.未安裝` 改成結果未知 | `test_未安裝是確定失敗不是結果未知` 紅（會讓接力當場停） |
| 帳本把有成本的筆加總、忽略缺成本的筆 | `test_有成本與沒成本混在一起時總成本應為None`、`test_完全沒有成本時總成本也是None` 紅 |
| **`總成本美金` 永遠回 `None`** | `test_全部都有成本時總成本是加總` 紅。**這條抓到的是上一輪真的漏掉的洞**——當時只守 `None` 那一半，一個從來沒讀過成本的實作照樣全綠 |
| `_總成本` 把缺成本的那幾家當 0 加進去 | 同上兩支紅——低報的成本比沒有成本更危險，它看起來像個數字 |
| 成果欄位對照漏掉 `cost_usd` | `test_成本帶得進成果也讀得回來`、`test_落盤的欄位名是ASCII`、`test_事件帳本裡的成本會出現在成果上` 紅 |
| `_歸檔成果` 不把摘要的總成本傳進成果 | `test_事件帳本裡的成本會出現在成果上` 紅——這是「加總對了、顯示對了，但線沒接」的那一格 |
| `_一行成果` 漏印總成本 | `test_有成本就印出來` 紅 |
| 沒有成本時印成 `US$0.0000` | `test_沒有成本就不印`、`test_給不出成本的那一家不印金額` 紅——0 看起來像免費，留白看起來像不知道 |
| 在負控紀錄裡加一列提到不存在的測試 | `test_文件提到的測試都真的存在` 紅——**這條規則早就有執法點了**，我一度另外寫了一支重複的，發現之後刪掉（`寫程式.md` 階梯第二階：這個 codebase 已經有了嗎）|
| **遮罩的邊界換回 `\|b`** | `test_祕密緊貼中文也要遮掉` 13 支紅。**這條抓到一個真 bug**：Python 的 `\|w` 把中文也算單字字元，所以「我讀到AKIA…這串」（祕密緊貼中文、沒有空格）完全不會命中，而那在中文專案裡是常態 |
| 遮罩的特徵規則全部拿掉 | `test_每一種特徵都遮得掉` 等 28 支紅 |
| 遮罩的環境變數那段拿掉 | `test_環境變數的值會被遮掉` 紅——那條規則精準度最高，而且它抓得到**沒有任何特徵**的祕密 |
| 網址規則貪心到把主機也吃掉 | `test_只遮密碼留下主機` 紅——主機名要留著，不然看不出是哪台機器出的事 |
| 記帳不遮就記全文 | `test_全文裡的祕密進不了帳本` 紅 |
| 記帳預設不記全文 | `test_模型全文會記進去` 等 3 支紅 |
| **先截斷再遮罩**（順序反過來） | `test_遮罩要在截斷之前` 紅——祕密剛好跨在邊界上時，前半段會原封不動留在帳本裡，而且看起來完全正常 |
| 忽略 `記全文=False` | `test_明講不記全文時只剩長度與雜湊` 紅 |
| **收件匣落在工作目錄裡** | `test_收件匣不在工作目錄裡` 等 2 支紅——放錯的話一個能寫檔的模型就能自己派工給自己 |
| 收下之後不把檔案移走 | `test_收下之後就不在佇列裡了` 等 3 支紅——不移走的話下一輪拿到同一件，副作用做第二次 |
| `待處理` 連子目錄一起掃 | `test_子目錄不算收件` 等 2 支紅——`處理中/` 就住在旁邊，掃到它會變無限迴圈 |
| 佇列改成後丟的先做 | `test_先丟的先處理`、`test_先丟的先做` 紅 |
| 完成之後不清 `處理中/` | `test_完成之後處理中是空的` 等 2 支紅 |
| 空白檔案也算一件 | `test_空白檔案不算一件` 紅。**第一版沒紅**：檔名用「空的.md／真的.md」，而「真」的碼位比「空」小，真的那件本來就排前面——測試根本沒走到那條分支。改成 01／02 開頭才紅 |
| 工作流不把原始請求搬到成果旁邊 | `test_丟一個檔進去就會被做掉` 紅 |
| 空收件匣回阻擋而不是放行 | `test_空收件匣是正常狀態不是錯誤` 紅——排程每小時醒來一次，多數時候本來就是空的；回錯誤碼會讓 log 被永遠沒人修的錯誤塞滿 |
| 單例鎖改成共享鎖（`LOCK_SH`） | `test_另一個程序拿不到` 等 3 支紅 |
| 拿不到鎖時人手動下的也回放行 | `test_人手動下的指令撞到就是真衝突` 紅——人打了指令卻什麼都沒發生還回 0，他會以為做完了 |
| 拿不到鎖時排程也回阻擋 | `test_排程撞到正在跑的就安靜讓開` 紅 |
| **拿掉單例鎖的明確 `LOCK_UN`** | **沒紅**——`with open` 一定會 close，close 就會放掉 flock。那一行是多餘的，而且我在註解裡編了一個「不然會自己擋住自己」的理由（`with` 之下不可能發生）。**刪掉而不是留著**：沒有測試背書的程式碼就是沒有保證 |
| plist 不擋直譯器／代跑工具當執行檔 | `test_執行檔名字不對就當場炸` 紅——寫 `uv` 的話「登入項目」只顯示 uv，看不出是誰的 job 而且清不乾淨 |
| plist 不帶 `--從收件匣` | `test_跑的是從收件匣那條路` 紅 |
| `建角色表` 漏掉最後一個 blueprint | `test_研究角色不改變_TDD_角色表` 紅 |
| plist 的 log 落在專案裡 | `test_log落在狀態目錄不落在專案裡` 紅——log 也是會被餵回模型的東西 |
| Label 不過濾非 ASCII | `test_Label是ASCII` 紅——CJK 會讓 launchctl、pkill、log 過濾的字串比對出問題 |
| 本地模型默默接受可編輯 | ~~`test_要求可編輯就明講做不到`~~ 等 2 支紅——假裝做得到會讓工作流以為檔案改好了，然後在驗證階段才發現什麼都沒發生，而那時候看起來像「模型做錯了」不是「這顆腦根本做不到」 |
| 本地模型默默接受續接 | `test_要求續接就明講做不到` 紅 |
| 本地模型的成本回 `None` | `test_成本是零不是None` 紅——本地跑沒有 API 帳單是**事實不是估算**；回 None 會讓混著本地與 claude 的那次整個算不出成本，而那次其實算得出來 |
| 本地模型把逾時當確定失敗 | `test_逾時是結果未知不是確定失敗` 紅 |
| 「連不上」歸成上游而不是未安裝 | `test_連不上是未安裝` 紅——伺服器沒開跟 CLI 沒裝是同一件事，分兩種代碼的話降級規則要寫兩份 |
| 本地模型寫死型號 | `test_沒指定型號就問伺服器有哪些` 等 2 支紅 |
| 本地模型把連線拒絕分類成上游 | `test_URLError連不上歸類為未安裝` 紅——單元測試只注入 `URLError`，不碰真的伺服器 |
| 本地模型把逾時分類成未安裝 | `test_逾時落在結果未知` 紅——逾時可能已經送出請求，必須保留「結果未知」語意 |
| 本地模型把 HTTP 4xx／5xx 分類成未安裝 | `test_HTTP錯誤歸類為上游` 的四百系、五百系案例都紅——收到上游回應不能誤報成伺服器沒裝 |
| 命令列拿掉本地腦的審查資格檢查（只涵蓋命令列入口；門面見下方負控） | `test_本地腦不准當審查員` 紅——即使執行與審查是不同家，9B 本地模型也不能成為唯一把關點 |
| 派工表把 local 塞進任一自動派工列 | `test_能力未量到前本地腦只保留手動指定` 紅——本次量測的長提示與未知事實拒答未達三次全過，所以 local 只能人手指定 |
| 真端點不驗模型清單 | `test_真端點列得出模型清單` 紅——模型清單是獨立的可達性保證，fixture 只負責探測外部服務 |
| 真端點沒開時把跳過改成失敗 | `test_真的打一次本機推論伺服器` 紅——關掉 server 跑 `-m 真端點` 應該跳過，不該把外部資源缺席算成 nova 壞了 |
| 本地模型把 `completion_tokens` 讀成 0 | `test_真的打一次本機推論伺服器` 與 `test_token數從usage讀出來` 都紅——真 server 與假伺服器都會抓到輸出用量遺失 |
| 欄位對應表少了 `成本美金` | `test_每個欄位都有對應` 紅 |
| 記帳腦不把成本寫進事件 | `test_記下成本` 紅 |
| 讀取端不用檔名補執行識別碼 | `test_一筆都沒寫也認得出是哪次執行` 紅 |
| 檔名蓋掉檔案裡記的識別碼 | `test_檔名補的識別碼不准蓋掉檔案裡的` 紅 |
| 閘不記規則結束事件 | `test_一條規則留下開始與結束` 紅 |
| 提前停止之後把沒跑的也記進去 | `test_提前停止之後沒跑的不准記` 紅（把「沒輪到」算成「通過」） |
| 規則統計把開始事件也算進去 | `test_跨執行加總` 等 2 支紅（次數變兩倍） |
| 拿掉規則報表的樣本下限 | `test_樣本不夠不准說刪除候選` 紅（跑兩次就叫人刪規則） |
| 拿掉重構階段（驗證綠直接進審查） | `test_驗證綠之後是重構不是審查` 紅 |
| 重構完不再驗一次綠 | `test_重構完要再驗一次綠` 紅（「行為沒變」失去背書） |
| 驗證重構紅了退回重構（fix-forward） | `test_驗證重構的lint或格式紅了要退回重構員` 紅 |
| 驗證重構改成模型階段 | `test_驗證是機械的不是模型` 紅（自寫自評） |
| `預設最多步數` 改回 12 | 12 支紅（七階段跑不完） |
| `建TDD執行器` 裡拿掉 `_檢查角色表(角色表)` | `test_角色表少一格_建構當場炸`、`test_要哪些角色是從階段表推導的` 兩支都紅 |
| 守衛改成寫死四個代碼，同時往 `TDD階段表` 加一個模型階段 | `test_要哪些角色是從階段表推導的` 紅、`test_角色表少一格_建構當場炸` **照樣綠**——證明第二支守的是「推導」不是「有守衛」 |
| `_乾淨工作區` 改回直接回 `tmp_path` | `test_唯讀真的寫不出檔案[claude]` 紅在 `assert not ['state']`——證明那個假紅的根因是 conftest 把 `XDG_STATE_HOME` 指進 `tmp_path`，不是權限破口 |
| 拿掉 codex 的 `network_access` 設定、把 agy 的危險旗標收回全開 | `test_codex可編輯要開網路而且不必拿掉沙箱`、`test_agy可編輯要開網路` 兩支都紅，`test_唯讀不開網路` 照樣綠——證明那支守的是「唯讀別跟著鬆」，不是「有沒有網路」 |
| 把 `_成功但沒話說算未知` 的罐頭句改回無條件優先（不看 stderr） | `test_agy空回應時要拿stderr當證據`、`test_三家都要收得下stderr` 兩支紅，`test_沒有stderr時才回罐頭句` **照樣綠**——它守的是後備行為，不該跟著紅 |
| 拿掉 `_提示` 裡的前情段落 | `test_有前情就要放進提示裡` 紅、`test_沒有前情就不要放空段落` 照樣綠 |
| 拿掉 `跑工作流` 裡的 `卡住了(...)` 呼叫（純函式還在） | 只有 `test_同一個判準紅三次就停不要燒完步數` 紅，狀態機那 6 支照樣綠——證明迴圈層那支守的是「有沒有人叫它」 |
| 拿掉 `_成功但沒話說算未知` 裡的權限分類與環境診斷 | `test_agy的auto_deny要分成確定失敗`、`test_agy的auto_deny訊息要指得出家工具和權限` 紅——空回應那條路不走 `_壞掉`，分類與環境證據都要在那裡保留 |
| `可以換下一顆` 不看失敗代碼（退回只看終局） | `test_權限被擋不准換下一顆` 紅、`test_其他確定失敗照樣換` 照樣綠 |
| `權限被擋` 的終局改成`確定失敗` | **第一次做這條負控時 310 支全過**——那表示這個決定沒有背書。補了 `test_所以它的退出碼是確定失敗的那個`（釘在退出碼 1 上）之後才會紅 |
| 關鍵詞表拿掉額度那一列 | `test_codex額度用完` 紅——順帶證明接力那兩支測的是 `可以換下一顆` 的契約，不會跟著分類一起紅 |
| `_額度樣式` 退回寫死四個型號名（session／weekly／opus／sonnet） | **只有** `test_還沒出現的型號名也要命中` 紅，「四種講法」那支照樣綠——證明那支才是真正在守的 |
| 五個護欄點全退回 `結束代碼.中止`（分不夠） | 21 支紅 |
| `_哪幾家` 沒給旗標就回空集合 | **第一次做時 3 支全綠**——空集合跟空集合不重疊，自寫自評的檢查會「通過」。補了 `test_沒給旗標時哪幾家要問派工表`（直接釘接縫）才會紅 |
| 拿掉 CLI 裡的 `檢查進度檔位置(...)` 呼叫（純函式還在） | `test_CLI要真的擋` 紅，`tests/單元/test_進度檔.py` 那 5 支照樣綠——證明整合層那支守的是「有沒有人叫它」 |
| `預設帳本目錄` 退回全域（不看專案） | 4 支紅，全是「歸屬」那一軸（看得出是哪個專案、不同專案不同目錄）；「完整性」那 2 支照樣綠 |
| 拿掉 `_預算不夠` 的事前那條（退回只看已花） | 4 支紅，而「預算 0 一步都不准跑」照樣綠——兩條各擋各的 |
| 拿掉 `剖析器.py` 裡 7 個 `set_defaults(執行=…)` | `test_每個子命令都綁得到處理函式`、`test_少一格就在建剖析器時炸` 兩支紅，`test_沒有第二份登記表` 照樣綠——它守的是「沒有第二份」，不是「第一份有綁」 |
| 熔斷把「未知」也算成失敗 | `test_只有未知的執行不推進熔斷` 紅 |
| 熔斷把「未知」也算成成功（歸零） | `test_夾在中間的未知不打斷連續失敗` 紅——兩條各紅各的，證明「不知道就不要動計數」兩個方向都守著 |
| 拿掉 `_建腦` 裡的 `_濾掉熔斷的(...)` 呼叫 | **只有** `test_建腦時真的會問熔斷` 紅，熔斷那 15 支純函式測試照樣綠——證明整合層那支守的是「有沒有人叫它」 |
| `_估下一階` 改成看「上一階」而不是「最近一次非零」 | 同樣 4 支紅：判準階段花費是 `None`，估計會退化成 0，事前擋整個失效 |
| `預設最多token` 拉成 `10**12`（拉大寫成拿掉） | `test_預設不是無限大` 紅——放寬是人的決定，取消不是 |
| `預設帳本目錄` 改成寫進 `專案/.nova/帳本` | **只有** 2 支紅，全是「完整性」那一軸——證明兩條軸各有各的測試守，不會互相頂替 |
| 例行的備援改回 codex（跟推理重疊） | 3 支紅，包含端到端那支（退出碼 2 不是 4） |
| 「角色確定失敗」也標成護欄（分過頭） | **只有 2 支紅**，就是那兩支斷言「壞掉」的——證明它們守的是「不准把要修的東西也算成護欄」 |
| `護欄碼` 從 4 改回 1 | **只有** `test_四個碼互不相同` 紅。`test_四種收場四個碼` 照樣綠（兩個代碼都對到 1，斷言仍成立）——沒有那支互異測試，這個洞就看不見 |
| `_額度樣式` 放寬成 `exhausted\|limit` | 兩支「不准誤判成額度用完」紅，外加 agy 那支紅（被更前面的規則搶走）——一條抓太緊、一條抓太鬆 |
| `卡住了` 的鍵拿掉「證據」（變成純粹紅 N 次就停） | `test_證據不一樣就不算卡住`、`test_有進展就不要停` 兩支紅——這兩支防的是**太早停**，把正在收斂的一輪砍掉 |
| `進度執行器` 的 `open("a")` 改成 `open("w")`（覆寫） | `test_第二階要接在後面不是蓋掉` 紅——只記得最後一步等於沒有記憶 |
| 拿掉 `_這個專案誰熔斷了` 的 `if not 啟用` 守衛（退回一律熔斷） | `test_預設不熔斷就算帳本有連續失敗也照樣叫` 紅，`test_明確指定熔斷旗標時連續失敗的家會被濾掉` 照樣綠——兩支各守一個方向：預設不准擋、明講了才擋 |
| 拿掉 `剖析器.py` 的 `--熔斷` 旗標 | 兩支都紅（一支 `unrecognized arguments`、一支 `參數.熔斷` 不存在）——旗標是這條保證唯一的入口 |
| `預設最多token` 少算一輪（5,000,000） | `test_預設至少涵蓋一階實測成本乘上TDD階段數` 紅——預設至少要蓋住一階實測成本乘七階 |
| 護欄估計改成 0，不再沿用最近一次實測成本 | `test_訊息要說得出估計是怎麼來的` 紅——建議值的估算基礎不能消失 |
| 護欄建議值少算剩餘階數（`剩餘token = 估`） | `test_建議值等於已花加剩餘階數乘花費並向上取整` 紅——建議值必須把剩餘階數乘進去，並向上取整到百萬 |
| 路線圖某一格的 `檔` 指到不存在的路徑 | `test_路線圖指到的檔案都真的存在` 紅——Markdown 那支只掃反引號，路線圖的路徑住在 JSON 陣列裡，一條都掃不到 |
| 把路線圖的 `id="圖資料"` 改名 | `test_路線圖有節點可以檢查` 與那支一起紅——抓不到節點時不准靜靜地變成永遠綠 |
| agy 的「剩下多少」當成「用掉多少」（`100 - 剩餘` 改成 `剩餘`） | `test_89趴Remaining要變成used_percent_11` 與端到端那支一起紅——方向寫反會顯示成「快滿了」，剛好相反 |
| `分鐘轉標籤` 寫死回傳 `5h` | 3 支紅，含 `test_10080分鐘換算為7天`——codex 的 `primary` 不一定是 5 小時，這台是 10080 分鐘（7 天） |
| `短窗排前面` 改成比字串 | `test_排序的鍵是視窗長度不是字串` 紅。**第一次做這條負控時沒紅**：原本用 90m 與 1h 舉例，字串比剛好也給對答案；換成 10h 與 5h 才分得開（`"10h" < "5h"` 跟正確答案相反） |
| `佇.get` 拿掉逾時（退回無限等） | `test_codex裝死時要在截止時間內放棄` 跑 24 秒還沒結束——子程序活著、管線沒關，`readline` 永遠不會回來，截止時間是唯一擋這件事的東西 |
| 拿掉 `執行查詢額度` 裡的 `該重抓嗎(...)` 呼叫（純函式還在） | 整合層那 2 支紅，單元層 5 支照樣綠——證明整合層守的是「有沒有人叫它」 |
| `該重抓嗎` 的邊界改成「嚴格大於」 | `test_剛好等於門檻也要抓` 與 `test_門檻是0就一律要抓` 紅——後者是關鍵：門檻 0 要是不重抓，`nova 額度` 直接叫就再也拿不到新數字 |
| `該重抓嗎` 把「快取不在」當成「還新」 | 3 支紅——快取不在卻不去抓，第一次就永遠生不出檔案 |
| 拿掉第三條禁令（合併要帶刪除分支） | `test_沒有帶刪除分支要被擋下` 紅 |
| 第三條的判斷少看 `pr` 這個詞 | **第一次做這條負控時 21 支全綠**——沒有任何輸入分得開。補了 `test_gh的其他子命令帶merge這個詞不准誤擋`（`gh run view --job merge`）才會紅 |
| 把第三條塞進關鍵詞掃描那層（`_危險詞`） | **第一次做時也全綠**。前兩條是肯定條件、退回掃描不會變寬鬆；第三條是**否定條件**，掃描判不出「少了什麼」，會把引號沒收尾、卻帶了 `--delete-branch` 的正確指令誤擋。補了 `test_拆不開的正確合併不准被退路擋掉` 才會紅 |
| 把三個型別從門面的 `__all__` 拿掉 | `test_門面很小` 與 `test_門面匯出符號` 兩支紅——前者是**對外承諾的 ratchet**：多匯出一個名字就是多一份承諾，只能被刻意加長 |
| `每家` 改成兩家都問完之後才一次叫完 | `test_每家回呼在兩家都問完之前就被叫第一次` 紅。它靠「第一次回呼時快取檔還不存在」釘住順序——寫快取是最後一步 |
| `每家` 丟出來的例外不再被吞掉 | `test_每家回呼拋出例外時吞掉例外並跑完查詢` 紅——回呼是外面給的，它炸掉不准把整個查詢帶走 |
| 規則表的 `ruff-check` 拿掉 `--no-cache` | `test_閘的ruff規則不會被假快取騙過去` 紅，而 `test_ruff的快取真的會給假綠` 照樣綠——後者證明坑存在，前者證明閘沒踩進去 |
| 重現裡不還原 mtime 奈秒 | `test_ruff的快取真的會給假綠` 紅（前提斷言就擋下來）——證明這個坑真的靠「大小與 mtime 都一樣」才成立，不是隨便換內容就會中 |
| 在 `CLAUDE.md` 尾巴加 10 行 | `test_CLAUDE_md不准無限長` 紅——上限是 ratchet，只准往下調 |
| 把 `CLAUDE.md` 裡某個設計文件的路徑指歪 | `test_文件提到的檔案都真的存在` 紅 |
| `跑cli` 拿掉 `start_new_session=True`（killpg 照留） | `test_逾時要連孫程序一起收掉` 紅——沒有自成一組，`_自成一組` 回 False，退回只殺直接子程序 |
| `收割整棵(程序)` 換回 `程序.kill()` | `test_逾時要連孫程序一起收掉` 紅——證明紅的是收割本身，不是 `start_new_session` 那一半 |
| 收割後多寫一句 `程序.communicate()` | **沒紅**（1.09 秒）。要連 `start_new_session` 一起拿掉才紅（4.4 秒）——見下面「一次負控沒紅」的第四次 |
| `執行逾時` 的 `部分標準輸出` 改傳空字串 | `test_部分輸出在收割之後仍然撿得回來` 紅——收割的改寫不准把 sid 弄丟 |
| `具名啟動` 對 python 腳本也原封不動回傳 | `test_python腳本開出來的子程序在ps上叫得出名字` 紅——ucomm 退回 `python3.13` |
| 具名連結搬到 `<venv>/程序名/` | `test_具名直譯器不准搬離直譯器所在的目錄` 紅。**這條是先在閘裡踩到才補測試的**：`sys.executable` 一搬家，`規則表._外部指令` 的 `Path(sys.executable).parent` 就找不到 ruff／mypy／pytest，退回 PATH 之後 Popen 直接 FileNotFoundError，而且每次紅的測試都不一樣 |
| 不設 `APP_ROLE` | `test_子程序一律帶著APP_ROLE` 紅 |
| 對真二進位也包一層具名直譯器 | `test_真二進位不准被改名` 紅——包一層只會把 `ruff`／`agy` 真正的名字藏起來 |
| 工作流收場時不歸檔 | `test_工作流跑完之後帳上看得到這次的收場` 等 2 支紅 |
| 成果帳本自己再發一次執行識別碼 | `test_成果對得回事件帳本` 紅——兩本帳走散了就跳不過去，而且斷掉的樣子跟正常一樣 |
| `已處理目錄` 改成落在專案裡面 | `test_落在專案外面` 等 2 支紅——落在專案裡執行者就摸得到 |
| 讀不動的那筆讓它整個炸（只接 `OSError`） | `test_讀不動的那筆不准把整本帳帶走` 紅 |
| `列出成果` 拿掉 `reverse=True` | `test_最近的排前面` 紅 |
| `字典轉成果` 改用 `原始[外]` | `test_少欄位的舊紀錄不准把讀取端整個弄爆` 紅——帳是 append-only 的歷史，舊紀錄不會回頭補欄位 |
| 禁令 hook 拿掉 `\|\| exit 2` | `test_nova起不來的時候要擋下不是放行` 紅。**端到端也驗過**：故意改壞 `載體/命令列.py` 的 import 之後，修好的那行回 2（擋），原本那行回 1（Claude Code 視為非阻斷錯誤，指令照跑） |
| 把判斷邏輯搬進 `.claude/settings.json`（`grep -q no-verify && exit 2`） | `test_判決還是由nova做` 紅——**這條防的是修過頭**：自己 grep 也能 fail-closed，但那就是把邏輯搬進別人的設定檔，測不到，而且換一個宿主就整個消失 |
| `_子命令_問` 不再叫 `_預算先擋一下` | `Test超支要停在打出去之前` 四支全紅——**純函式在、但沒人叫，預算鎖就不存在**
| `_工作流跑一輪` 不再叫 `_預算先擋一下` | `test_工作流也有預算鎖` 紅。排程自己跑的是工作流，只接在 `問` 上等於在最需要的路徑上不存在 |
| `_窗口內的執行` 拿掉檔名時戳過濾 | `test_三個月前的帳本不會被開起來` 紅（開 40 個檔 vs 1 個）。**第一次負控時這條沒紅**——時間過濾其實由 `花了多少` 做，檔名過濾是純效能最佳化、沒有任何測試守著；補上這支數開檔數的測試才成立 |
| `超支了嗎` 的 `>` 改成 `>=` | `test_token剛好等於上限還沒超` 紅。等於不算超，不然「上限 1000」實際只能用到 999 |
| `超支了嗎` 在 `花.美金 is None` 時也擋 | `test_算不出成本時成本上限一律放行` 紅。codex 與 agy 都不回成本，算不出就擋等於**永遠擋住**——那不是保護，那是壞掉 |
| `花了多少` 拿掉 `幾小時 <= 0` 的 `raise` | `test_窗口不准是零或負數` 紅。窗口是 0 什麼都不算，預算鎖看起來在跑但永遠不會擋 |
| `_檔名時戳` 跟寫端的 `新執行識別碼` 走散 | 兩個方向都紅，但接住的是不同支：改成 `%y…`（濾過頭）→ `test_剛剛那次自己記的帳下一次就算得進去` 等六支紅；改成 `%Y-%m-%d…`（濾不掉）→ `test_三個月前的帳本不會被開起來` 紅 |
| `_工作流開跑前` 移到 `_這次要做什麼`（`收下一件`）之後 | `test_被預算鎖擋下時那一件要留在收件匣` 紅——那一件被搬進 `處理中/` 變成「沒人做也沒人看得到」的孤兒 |
| `工作流` 的剖析器沒有 `--預算token` | `test_帶了預算旗標之後那行指令還是跑得起來` 紅——**旗標帶得出去、接不到**，而失敗發生在 launchd 的 log 裡沒有人會看到 |
| `排程預算.旗標()` 回空的（預算進不了 `ProgramArguments`） | 排程單元三支＋`test_帶了預算旗標之後那行指令還是跑得起來` 紅。寫死的話，人在終端機打的每一次都擋得住，時鐘自己跑的那幾百次一次都擋不住 |
| 旗標跟數字塞成同一格 `"--預算token 500000"` | `test_每一格都是獨立的字串` 紅。`ProgramArguments` **不經過 shell** |
| `_子命令_排程` 自己算一次 `f"com.nova.{專案.name.lower()}"` | `test_印出來的檔名跟plist裡的Label是同一個` 紅。**兩份對照表就是這條 bug 的形狀**——專案名有空格或大寫時，印出來的安裝指令會給一個 `launchctl load` 不動的檔名，而那行字自己說「檔名要跟 Label 一致」 |
| `_問的前置` 不再叫 `_秘密先交出去` | `test_載進去的祕密會被用到` 等三支紅——憑證根本沒交到子程序手上 |
| 祕密載進 `dict(os.environ)` 而不是 `os.environ` | `test_祕密一個字都不准進帳本` 紅。**祕密會被用、但不會被遮**——遮罩讀的是 `os.environ`，這是一個沒有任何症狀的洩漏 |
| `遮罩` 不看 `NOVA_LOADED_SECRETS` 名單 | `test_名單上的鍵不管叫什麼都要遮`＋`test_祕密一個字都不准進帳本` 紅。`MY_THING` 撞不上「鍵名看起來是祕密」那條規則，只能靠名單 |
| `載入到` 不把鍵名記進名單 | 同上三支紅 |
| 權限太鬆只警告不擋 | `test_太鬆的時候載入要炸不是靜默跳過`＋`test_權限太鬆時整個不出生` 紅 |
| 錯誤訊息改成 `f"...{行}"`（把整行印出來） | `test_錯誤訊息不准把值印出來` 三個參數化案例紅。**祕密要放在出錯的那一行上**——第一版放在別行，那條路根本走不到，負控完全沒紅 |
| `nova 跑` 不落收件檔、直接把字串塞進工作流 | `test_敲的那句話會先變成一個收件檔`＋`test_撞到鎖時題目要留在佇列上` 紅。**那就是第二條執行路徑**——佇列上看不到、`ls` 查不到、撞到鎖就整個掉了 |
| `nova 跑` 先跑再落檔 | 上面兩支＋`test_敲一句話就會被做掉` 紅。撞到鎖時題目掉了，而使用者以為他派出去了 |
| 成果不記來源（`來源=""`） | `test_成果上看得出是誰觸發的`＋`test_手丟的檔案來源是檔案不是你敲` 紅。四個事件源收斂到同一個入口之後就分不出是誰敲的了 |
| `誰造的` 永遠回 `你敲` | `test_手丟的檔案來源是檔案不是你敲` 紅。**這條防的是全部標成你敲**——來源是「誰造了這個收件檔」，不是「誰醒來把它撈起來」 |
| `丟一件` 的檔名不帶時戳 | `test_開頭是讀得回來的時戳`＋`test_來源讀得回來` 紅。`待處理` 照檔名排序，時戳掉了「先進先出」就靜默壞掉，而佇列看起來完全正常 |
| 醒來完全不記（回到今天的狀態） | `test_需要你的事都在這.py` 六支紅。排程醒來的絕大多數在兩本帳上一筆都不留 |
| 空收件匣那條不填結果（從退出碼反推） | `test_收件匣是空的也要留痕跡` 紅。**反推不出來**：`0` 同時是「做完了」「收件匣空的」「撞到鎖讓開」 |
| 預算鎖擋下時不填結果 | `test_被預算鎖擋下要留痕跡` 紅。護欄生效不是壞了，**但它要看得見** |
| 跑完之後不用工作流自己的 `結束代碼` | `test_做完一件之後看得到`＋`test_人看得懂的那份也要印出來` 紅。兩套詞彙會讓人不知道哪個是真的 |
| 佇列深度改讀狀態檔裡的快照 | `test_佇列上還有幾件看得到` 紅。快照會過期——丟五個檔進去還說 0 |
| `讀現況` 不吃 `OSError` | `test_第一次跑的人不會看到錯誤` 紅。「還沒有狀態」是第一次跑的人的正常狀態，炸掉的話狀態列會一直閃紅 |
| 把「遮罩還不存在」那句舊註放回路線圖 | `test_不准說一個已經有的模組不存在` 紅。**第一版是綠的**——正規式 `([\u4e00-\u9fff]{2,6})(?:還)?不存在` 貪婪匹配抓到的是「而遮罩」，永遠不等於模組名。改成直接拿模組名拼字串比 |
| 成果帳的 `任務` 不過遮罩 | `test_使用者自己打進去的祕密也不准進成果帳` 紅 |
| 歸檔的 `.收件` 不過遮罩 | 同上一支紅。「原文」的意思是「當初丟進來的是什麼」，不是「連憑證一起留一份」 |
| 把 serial 佔比門檻改成 0.1% | `test_預設門檻為百分之八` 與 `test_佔比低於門檻放行` 紅 |
| 拿掉判準對 pytest 4／5 的映射（退回全當紅） | `test_沒收集到任何測試是跑不起來`、`test_用法錯誤是跑不起來`、`test_真pytest在沒有測試的目錄回跑不起來`、`test_真pytest旗標打錯回跑不起來` 紅。**exit 5「沒收集到任何測試」正是「研究題誤進 TDD 工作流」的形狀**——當紅回報就會一路回去「再實作一次」 |
| 讓 `_像pytest` 無條件回 `True` | `test_不是pytest的指令退出碼5仍是紅` 紅。退出碼語意是各程式自己的知識（nova 自己的 4 是「護欄生效」），無條件翻譯等於把別人的回饋偽裝成環境問題 |
| `_包不可信` 拿掉 marker 清除、直接回傳原文 | `test_前情與上一步證據都被fence且內嵌marker不能提早跳出` 紅。**防的是控制通道被資料通道冒充**——上游輸出含偽造標記時不能提早跳出圍欄 |
| 規範落點同步檢查漏掉文件規則 | `test_文件多一條規則但登記表沒跟上時會紅` 紅 |
| **扇出拿掉開樹那一步**（`_開這顆的樹` 直接回原本的工作，兩顆分支回到同一個目錄） | `test_兩個分支寫同一個檔名不會互相蓋掉` 紅：甲讀回來的是「乙要寫的東西」。**這正是這一格要擋的那種失敗**——兩顆都回成功、產出卻是同一份，看起來像模型變笨 |
| **扇出拿掉收樹那一步**（`finally` 裡的 `_收掉這批的樹` 換成 `pass`） | `test_乾淨的分支跑完不留樹` 紅：跑完 `git worktree list` 比跑前多兩棵。**這台機器上二十幾棵殘骸就是這條沒人守的樣子** |
| 唯一那份 `角色工廠.TDD階段的工作` 把「審查」改成例行 | `test_資格檢查算的家就是藍圖真的派的家`（`agy→claude` vs 該有的 `sol→codex`）與 `test_審查跟例行三階派的不是同一條鏈`（`{agy, claude}` 兩邊相等）紅。破壞前後有比對 SHA-256、還原後雜湊回到原值。**這條守的是「哪一階算例行、哪一階算推理」只有一份**——合併前那份知識在 `載體/命令列.py` 與 `迴圈/角色工廠.py` 各一份，改一份、更新它自己的測試，另一份照樣綠，症狀是資格檢查驗的家 ≠ 真的被叫的家（靜默放行或誤擋） |

## 本次本地腦負控的操作紀律（2026-08-30）

三條負控都先記錄 SHA-256，破壞後以 `test "$原始雜湊" != "$目前雜湊"` 斷言檔案真的變了，才執行被點名的測試；破壞前與還原後都清掉 `src/`、`tests/` 底下的 `__pycache__`。

| 防護 | 原始雜湊 → 破壞後雜湊 | 結果 | 還原後雜湊 |
|---|---|---|---|
| 本地腦不得當審查員（共用判斷） | `b0795786…` → `d30cfcc5…` | `test_本地腦不准當審查員` 紅，退出碼 1 | `b0795786…` |
| local 不得進自動派工列（`src/nova/載體/派工表.py`） | `b1e780f7…` → `a5585d6d…` | `test_能力未量到前本地腦只保留手動指定` 紅，退出碼 1 | `b1e780f7…` |
| 只拿掉門面 `派工()` 的資格接線 | `375c7fd0…` → `f04187b2…` | `test_門面不准本地腦當審查員` 紅，退出碼 1；既有命令列測試綠，退出碼 0 | `375c7fd0…` |
| 工作區有紅卻判定為綠 | `test_工作區有紅時指名哪幾支紅` 紅 |
| 工作區未知不准自動往下跑 | `test_判定為綠也不准自動往下執行` 紅 |
| 非 main 分支閘紅也放行落票 | `test_不符合現場條件的閘紅絕對不落成票` 紅（守住 TDD 工作樹與手動執行的雜訊不灌爆收件匣） |
| 拿掉收件匣與處理中的重複落票檢查 | `test_待處理已有同一條閘紅票不准重複落成` 與 `test_處理中已有同一條閘紅票不准重複落成` 紅（同閘點同規則只留一張） |

## 跑負控之後一定要清 `__pycache__`

2026-08-29 被咬兩次：程式碼還原了、直接求值也對，**但測試持續紅在同一行**。
原因是 `__pycache__` 還是被破壞過的那份。

```bash
find src tests -name __pycache__ -type d -exec rm -rf {} +
```

**「還原後還是紅」有兩種成因**：真的沒還原、或是快取。兩者長得一模一樣，
而後者會讓人去追一個不存在的 bug。清掉再驗，兩秒的事。

## 一次「負控沒紅」的紀錄（2026-08-29）

「門面改成不給目錄也開帳本」這條**第一次跑的時候沒有紅**——當時的斷言看的是
`tmp_path`，而帳本其實寫到了 `~/.local/state/nova/帳本/`（73 個檔案）。
**測試看錯地方，所以它守不住任何東西。**

修法有兩層：斷言改看 `預設帳本目錄()`，並加一支 autouse 的
`帳本不准寫到家目錄` fixture 把預設目錄整個導進暫存區。沒有那支 fixture，
這條保證在測試裡根本驗不了——真的寫了也不在測試看得到的地方。

教訓：**負控沒紅不代表防護沒問題，也可能是測試沒對準。**
兩者長得一模一樣，只有真的去看檔案才分得出來。

### 第二次（2026-08-29，生圖）

「生圖的提示不叫它搬進工作目錄」這條也沒紅。原因同一類：斷言掃的是**整串 argv**，
而 `--add-dir` 本來就會把工作目錄放進去——**把提示裡那句話整個拿掉，測試照樣綠**。

修法：只看提示那一個參數（用 `generate_image` 這個關鍵字認出它），不看整串。

兩次都是同一個形狀：**斷言的範圍比要守的東西大**。範圍一大，
別的東西就會替它成立，於是防護拆掉了測試也不知道。

### 第三次（2026-08-29，逾時截斷）

`test_太長要截斷` 餵 10 萬字進去，但用的假解析器**會回一段短文字**，
而實作是 `半成品.文字 or 部分標準輸出`——短文字是真值，那 10 萬字根本沒走到截斷那一行。
把截斷整個拿掉，測試照樣綠。

三次都是同一句話的不同寫法：**測試沒有真的碰到它要守的那段程式碼。**
第一次是斷言看錯目錄，第二次是斷言掃的範圍太大，第三次是輸入根本到不了那條分支。

所以負控要看的不只是「有沒有紅」，還有**紅的是不是那一支**。

### 第四次（2026-08-30，程序收割的管線那一支）

`test_孫程序抓著管線不准讓收屍卡住` 的 docstring 原本寫「守的是收割後多一句
`communicate()`」。單獨把那句加回去——**綠的，1.09 秒**。
因為收割已經先把孫殺了，第二次讀立刻拿到 EOF。

要連 `start_new_session=True` 一起拿掉才紅（4.4 秒）。所以那支測試真正守的是
**「不准有活著的後代抓著管線」**，不是「不准再 communicate 一次」。
docstring 照這個結論改掉了。

這次和前三次不一樣：**測試有碰到它要守的程式碼，是我對它守什麼描述錯了。**
一支只在兩項同時壞掉才會紅的測試不是沒有價值，但**它的 docstring 必須講明這件事**
——否則下一個人會以為單改一項有防護。

### 第五次（2026-08-30，破壞根本沒套用）

跑「`列出成果` 拿掉 `reverse=True`」時看到綠的，一度以為 `test_最近的排前面`
守不住東西。實際上是**破壞本身沒生效**：改寫腳本用
`python3 -c "...s.replace(...)..."`，殼把引號吃掉之後比對字串對不上，
而 **`str.replace` 找不到就靜默回原字串**。檔案根本沒變，跑的還是好的那份。

改成引號 heredoc（`python3 - <<'PY'`）並加一行
`assert 新 != s, "替換沒生效"` 之後，同一條負控當場紅。

前四次是「測試沒碰到要守的程式碼」，這次是**破壞沒碰到程式碼**——
方向相反，但症狀一模一樣：綠的。所以負控腳本一律要斷言「我真的改到了」，
不要只看測試結果。同一類的還有查 ruff 快取假綠那次：用「改內容再跑一次」去驗，
mtime 會跟著變，坑就不成立了——**測法把要測的東西填掉了**。

### 第六次（2026-08-30，負控驗的是跑完就丟的臨時斷言）

派 nova 做成本落盤那輪，`負控紀錄` 一次進了七列，其中**六列寫著
「固定 smoke assertion」**——意思是負控是對著一段臨時寫、驗完就刪的斷言做的。

那不是負控。**負控要證明的是「repo 裡的某支測試會紅」**；斷言不留下來的話，
被證明的那個保證在下一次改動時沒有任何東西守著。而且它比沒做更糟：
紀錄上看起來有七道防線，實際只有一道。**在「防止假安全感」的文件裡製造假安全感。**

實際的洞：那一輪只留下一支真測試，而且它只斷言「混著有跟沒有的時候是 `None`」。
把實作改成 `總成本美金 = None`（永遠不讀成本）——**全綠**。
補上 `test_全部都有成本時總成本是加總` 之後，同一個破壞當場紅。

所以往後：**負控那一列裡的測試名，必須是 repo 裡搜得到的名字。**
搜不到就代表那一列在說謊。

### 第七次（2026-08-30，負控沒紅但實作是對的）

預算鎖那輪的第三條負控——拿掉 `_窗口內的執行` 的檔名時戳過濾——**綠的**。

原因不是實作錯了：時間過濾本來就由 `花了多少` 做，檔名過濾是**純效能最佳化**
（別把三個月前的幾千個帳本檔全開起來讀）。所以「窗口外的不算」那支測試
拿掉最佳化照樣綠。

但這一格仍然是假保證：**那段程式碼沒有任何測試守著，下一個人會很合理地把它刪掉**，
然後每次呼叫前都把整個專案的帳本讀完——一個沒有人會發現的成本漏洞。

修法不是刪掉那段程式碼，也不是接受負控是綠的，而是**把它的真行為釘住**：
它改變的是「開幾個檔」，就寫一支數開檔數的測試（`test_三個月前的帳本不會被開起來`，
monkeypatch `nova.載體.命令列.讀一次執行` 計次）。補上之後同一個破壞當場紅，40 vs 1。

前六次都是「負控該紅卻綠」＝實作有洞。**這次是「負控該紅卻綠」＝測試有洞**——
症狀一樣，位置相反。所以負控綠掉的時候要先問完兩題再動手：
**是實作沒做到，還是這條保證根本沒人守？**

順帶一個 monkeypatch 的坑：`from x import y` 之後，呼叫端查的是**自己模組裡**的
那個名字。打在來源模組 `nova.載體.帳本讀取.讀一次執行` 上不會有任何效果——
改了、測試綠、其實沒接上，就是「用規避換來的假綠」。要打在
`nova.載體.命令列.讀一次執行`。

**同一次的兩個補課。** 上面那兩列第一次做的時候都是綠的，原因各不相同：

- `_檔名時戳` 那條：我挑的破壞（`%Y-%m-%d…`）剛好落在**另一支**測試的守備範圍
  （它讓過濾不再排除任何東西，是效能退化不是算錯），而我只跑了一支。
  **負控綠掉先問「是保證沒人守，還是我挑的破壞打歪了」**——跑整個檔案就看得出來。
- 收件匣那條：斷言寫成 `"一件.md" in stdout`，而 `nova 收件` **連 `處理中/` 也印**
  （印成「⚠ 沒收尾」），所以搬走了照樣綠。**斷言要分得出好狀態與壞狀態**，
  「名字還印得出來」不是「它還在等」。改成 `"等著  一件.md" in` 加上
  `"沒收尾" not in` 之後當場紅。

### 第八次（2026-08-30，祕密放在不會出錯的那一行）

「錯誤訊息不准把值印出來」第一版寫成：

```python
讀祕密("A=1\nB C\nGOOD_KEY=超級機密的值\n")   # 壞的是第 2 行
assert "超級機密的值" not in str(錯)
```

第 2 行 `B C` 一報錯就停了，第 3 行根本沒被讀到。**那個祕密不在任何一條
可能被印出來的路徑上**，所以把訊息改成 `f"...{行}"` 也不會紅。

同一類的還有第七次那個「我挑的破壞打歪了」。合起來是一條：
**負控要打在被測的那一格上，測試資料也要放在被測的那一格上。**
祕密要放在出錯的那一行；改壞的要是那支測試真正守著的那段。

改法：`parametrize` 三種壞行，每一種的祕密都在出錯的那一行上
（整行看不懂、鍵名不合法、鍵裡有空白），兩個方向的破壞都當場紅。

### 第九次（2026-08-30，負控腳本自己跑到系統 Python）

`nova 跑` 那五條負控第一次全部「紅 ✓」——**而且全是假的**。訊息是：

```
/Library/Developer/CommandLineTools/usr/bin/python3: No module named pytest
```

腳本用 `python3 - <<'PY'` 起（前一輪 hook 擋掉 `uv run` 之後順手換的），
於是 `sys.executable` 是系統的 3.9，pytest 根本沒裝。**五次「紅」的理由
都不是那條保證失效，是測試套件根本沒跑起來。**

CLAUDE.md 早就寫著「一律走 `uv run`，忘了會靜默跑到系統 Python 3.9」——
這次是它在**驗證腳本**上發作。驗證腳本比被驗的程式碼更容易漏掉這條，
因為它「只是個腳本」。

所以負控腳本從此要斷言兩件事，跟「我真的改到了嗎」並列：

```python
assert "/.venv/" in sys.executable, f"跑到系統 python 了：{sys.executable}"
assert "No module named pytest" not in 尾, "又跑到系統 python 了"
```

前八次問的是「這個紅是真的嗎」。這次要多問一句：**這個紅是因為我要測的那件事嗎？**

### 第十次（2026-08-30，量法只掃了一半，剩下那一半就是洩漏的地方）

`test_祕密一個字都不准進帳本` 的量法第一版寫成「掃 `帳本/` 與 `已處理/`」，
理由是要避開祕密檔自己（它就住在同一個專案目錄底下）。那支測試一直是綠的。

結案盤點時真的跑一次、然後 `grep` 整個狀態目錄，才發現**祕密躺在
`已處理/*.json` 的 `task` 欄位裡**——模型講的那句話被遮了，
使用者自己打進去的那句話沒有。

為什麼那支測試沒抓到：它讓假模型**複述祕密**，所以驗的是「模型的回應有沒有被遮」。
題目那一半從來沒有真的檢查過——兩本帳合起來掃，其中一本的漏洞會被另一本的
綠掩蓋掉。改成分開量（`_只有事件帳本` 與 `_成果帳裡的全部字`）之後，
新測試當場紅。

原本的理由「任務已經在 `ps` 的 args 上，記它沒有多開一條路」在兩個方向上站不住：
`ps` 的 args 是程序活著的那幾秒，`已處理/` 是躺在磁碟上直到有人刪掉；
而**收件匣那條路根本不經過 `ps`**——題目來自一個檔案。

教訓跟第七、九次同一族，但這次在**量法**上：
**一個把兩個對象合起來算的斷言，只證明了「至少有一個是乾淨的」。**

### 第十一次（2026-08-30，接續：三條都紅，但偵測腳本的比對字串打錯）

新增「撞到上限自動接續」時做的三條固定負控：

| 破壞什麼 | 該紅的測試 |
|---|---|
| `_也許接著排` 的判準 `碼 != 護欄碼` 放寬成 `碼 == 放行`（非 0 就接續） | `test_結果未知不准排回去` |
| `收件.最多輪次` 從 3 改成 999 | `test_輪次用完就停下來等你` |
| `接著排` 裡的 `遮罩(前情).文字` 改回 `前情` | `test_祕密一個字都不進接續票` |

三條都紅。**但第一次跑的時候第三條被報成「沒紅」**——腳本用
`該紅的 in 尾` 判斷是不是紅在該紅的地方，而我在腳本裡把那個測試名
多打了一個「准」字。pytest 的輸出裡當然找不到，於是一條真的紅被記成假綠。
（那個打錯的名字這裡不照抄，`test_文件提到的測試都真的存在` 會把
文件裡出現的每一個測試名都當成宣稱去查——它抓到過這一條。）

教訓跟第七到第十次同一族，但位置又換了一層：
**這次錯的不是測試、不是破壞、不是量法，是「判斷紅得對不對」的那個字串。**
負控腳本裡每一個用來比對的名字，都要先對著來源檔斷言它存在：

```python
assert 該紅的 in pathlib.Path(測試檔).read_text(encoding="utf-8"), "測試名打錯"
```

第一條負控是這一批裡最要緊的那條。它守的是使用者親自畫的那條線——
**「撞到上限的自動接著做，被殺掉的等我看」**。退出碼 3 的意思是
「不知道工作做了沒」，自動重排會把可能已經做過的事再做一次；
判準寫成「非 0 就接續」的話兩支測試裡只有一支會紅，而那一支正是這條。

### 第十二次（2026-08-30，預設走 nova 的 hook：五條）

`nova 檢查編輯` 掛在 agent 的 `Edit`／`Write` 上，沒說出理由就擋下編輯。

| 破壞什麼 | 該紅的測試 |
|---|---|
| `_子命令_檢查編輯` 不印那段 JSON（殼變裝飾） | `test_改repo裡的檔案要先說理由` |
| `說得出理由了嗎` 反過來（有記號才擋） | `test_繞過之後同一個session就不擋了` |
| `記下繞過` 收下空理由 | `test_理由是空的不算說過` |
| `在管轄範圍嗎` 連點開頭的地盤也管 | `test_點開頭與暫存工作區的地盤不管` |
| 擋下來時退出碼改回非零（fail-closed 化） | `test_真的擋下來的時候也是零` |

五條都紅。

**最後一條是這一批的核心。** 這一支跟 `檢查指令` 的方向**刻意相反**：

| | `檢查指令`（禁令） | `檢查編輯`（預設走 nova） |
|---|---|---|
| 守什麼 | 不可逆的動作 | 一個**預設值** |
| 讀不懂輸入 | fail-closed | **fail-open** |
| 怎麼擋 | 退出碼 2 | **印 JSON**，退出碼永遠 0 |

理由是這一支掛在 `Edit`／`Write` 上，而**修 nova 的唯一辦法就是編輯 nova 的檔案**。
用退出碼擋的話，`uv run` 自己失敗（venv 沒建好、import 錯）跟「故意擋下」
在退出碼上分不開，於是 nova 一壞就再也修不回來。2026-08-30 這件事真的
發生過兩次——那次掛在 `Bash` 上，還能用 Edit 逃出來；掛在 Edit 上沒有逃生口。

**已知不管的：走 `Bash` 寫檔（`sed`、heredoc、`cat >`）繞得過去。**
不去補：判斷一條 shell 指令有沒有在寫檔是判不準的，而 `禁令.py` 的立場同樣適用
——這裡的對象不是對手，是**會手滑的執行者**。文件不准宣稱有把關器而沒有，
所以這條限制寫在這裡也寫在模組 docstring 裡。

### 第十三次（2026-08-30，活動監視器上的名字：四條，其中一條揭出多餘的程式碼）

`nova 排程` 的 `ProgramArguments[0]` 從 console script 換成硬連結出來的
專用直譯器 `.venv/bin/nova-inbox`，這樣活動監視器才看得出是誰。

| 破壞什麼 | 該紅的測試 | 結果 |
|---|---|---|
| 第一格改回 console script | `test_啟動器自己的kernel名字是nova開頭` | 紅 ✓ |
| `確保啟動器在` 改成建符號連結 | `test_跟著符號連結走到真的那份` | 紅 ✓ |
| 已經在就不對 inode | `test_直譯器換掉之後啟動器要跟著換` | 紅 ✓ |
| 拿掉 `-m nova` | `test_那行指令整條真的跑得起來` | 紅 ✓ |

**第二條第一次跑的時候沒紅，而我對它的判讀是錯的。** 原本的破壞方式是把
`真身 = 直譯器.resolve()` 改成 `真身 = 直譯器`，預期「不跟符號連結走」會出事。
在我這台 macOS 上它沒出事，於是我當成「那行本來就不做事」，把 `resolve()` 刪了。

**CI（Linux）當場紅。** 硬連結會不會跟著符號連結走**兩個平台不一樣**：
macOS 會，Linux 不會。那行不是多餘的，是**在我跑負控的那個平台上剛好看不出來**。

所以真正的教訓是：**固定負控只在一個作業系統上跑。** 一條保證在 macOS 上
「破壞了也沒事」，可能只代表 macOS 幫你兜住了。這一族的問法要多一句——
「我真的改到了嗎 → 我的紅是因為要測的那件事嗎 → **我只在一個平台上問過嗎**」。

修法不是把 `resolve()` 加回去就算了，而是**不要依賴那個平台差異**：
自己 `resolve()`，兩個平台的行為就一樣了。破壞方式也換成
「改建符號連結」（`hardlink_to` → `symlink_to`），那是這支測試真正守著的東西
（`assert not 落點.is_symlink()`），在哪個平台都紅。

### 第十四次（2026-08-30，判準「跑不起來」被當成「紅」，燒掉 172 萬 token）

**這一次的成因是先發生了損失才被發現的**，不是先寫測試。真實 trace：

```
排程在 launchd 底下跑。launchd 不跑登入 shell，PATH 是
/usr/bin:/bin:/usr/sbin:/sbin——而 uv 住在 ~/.local/bin
      ↓
判準 `uv run pytest -q` → FileNotFoundError
      ↓
當時的實作：`return False, f"判準指令跑不起來：{錯}"`  ← 被當成「紅」
      ↓
狀態機看到判準紅 → 走紅邊 → 回去「再實作一次」（叫模型，最貴的一步）
      ↓
再驗一次 → 還是跑不起來 → 再實作 → ...
      ↓
第 3 次才被卡住偵測器停下。單次 997,031 token，三次醒來共 1,720,140
```

而 `狀態機.下一步` 裡還有一條特例讓它更糟：

```python
if 結果.終局 is 終局.確定失敗:
    if 定義.種類 is 種類.判準:
        return 定義.紅        # ← 判準壞掉也走紅邊
```

**兩層都把「環境壞了」翻譯成「測試沒過」。** nova 自己的診斷順序寫著
**環境 → 回饋 → 流程**：綠與紅是回饋，跑不起來是環境。壓進同一個 `bool`
就是把環境問題偽裝成回饋，而**環境問題重跑一百次還是同一個環境**。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 把「判準確定失敗走紅邊」的特例加回 `狀態機.下一步` | `test_跑不起來不准走紅邊` |
| `建判準` 的 `OSError` 改回 `判準終局.紅` | `test_指令不存在是跑不起來` |
| plist 的 `EnvironmentVariables` 拿掉 `PATH` | `test_排程的環境跑得起預設判準` |

三條都紅。

**逾時刻意留在「紅」**：它分不出是環境壞了還是測試真的卡住，fail-closed
當紅是既有的決定，卡住偵測器會在第 3 次擋下來。這條寫成
`test_逾時還是當紅`，是**刻意不修**的宣告，不是漏掉。

教訓：**「假的安全感比沒有規範更糟」在這裡的形狀是「判準看起來在守著，
其實它連跑都沒跑」。** 而讓它變貴的不是那個錯，是**把它翻譯成一個可以重試的
錯**——可重試的錯每重試一次就叫一次模型。

### 第十五次（2026-08-30，研究存檔的抬頭：四條）

`docs/研究/` 的 README 寫著「每份開頭要寫誰產的、什麼時候、用什麼題目問的」。
**只寫在 README 裡的規矩等於不存在**——下一份研究存進來的人不會讀它。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 拿掉「產出者：」那行 | `test_寫得出誰產的` |
| 拿掉日期 | `test_寫得出什麼時候` |
| 拿掉「問法：」 | `test_寫得出當初問了什麼` |
| 拿掉「不是 nova 的設計決定」那句 | `test_講明是外部資料不是我們的決定` |

四條都紅。

**這一批的順序是反的**（先寫好抬頭、再補測試），所以負控不是「補做」而是
**唯一的證明**。先有實作再寫的測試，不做負控就完全不知道它在測什麼——
它可能只是在覆述已經成立的事實。

另外多加了一支 `test_研究目錄有東西`：`parametrize` 吃空清單時
**一支都不跑，而零支測試也是綠的**。那是這種寫法特有的假綠。

### 第十六次（2026-08-30，背景派工看得見：五條，其中一條又是路徑裡的測試名）

`nohup uv run nova 問 … &` 派出去的研究**使用者的介面上什麼都看不到**。
第一版的修法是「以後改用 harness 的背景任務功能」——那是懇求，而且更糟：
那個功能是 Claude Code 的，換一個 harness 就整個消失。
改成 `nova 問 --背景` ＋ `nova 狀態` 列出「有發出去、還沒寫下結果」的執行。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 背景不印識別碼 | `test_印得出識別碼與輸出檔` |
| `--背景` 改成前景跑 | `test_背景跑起來就立刻回` |
| 背景與帳本各編一個號碼 | `test_印出來的識別碼就是帳本上那個` |
| `nova 狀態` 不列還在跑的 | `test_有開始沒結束的會被列出來` |

四條都紅——**但前兩條第一次跑的時候都沒紅**：

1. `test_印得出識別碼與輸出檔` 斷言 `"識別碼" in stdout`，而 `tmp_path`
   的路徑裡有**測試自己的名字**，那三個字本來就在 stdout 上。
   **這是今晚第三次同一種假綠**（前兩次是 `"前情"` 與 `"還在跑"`）。
   改成**驗值不驗標籤**：把識別碼取出來對正規式，並確認輸出檔真的存在、
   檔名就是識別碼。
2. `test_背景跑起來就立刻回` 用「花了幾秒」當判準，而假 CLI 快到前景跑
   也在一秒內結束。改成**「模型的答案不在父程序的 stdout 上」**——
   那才是「丟出去了」的證據。

第五條（`NOVA_RUN_ID` 不驗格式就照收）**目前沒有測試守著**，誠實記在這裡：
亂設一個殘留的環境變數會讓所有執行共用同一個檔名，靜默把帳蓋掉。

### 第十七次（2026-08-30，思考深度：統一介面上的旋鈕，三家各自實作）

使用者說「gpt-5.6-luna max」，我把它當型號傳下去 → `model-not-found`。
`max` 是**思考深度**，`gpt-5.6-luna` 才是型號。查下去發現三家都有這個旋鈕，
只是機制完全不同，而 nova 原本**把 codex 的深度寫死成 `max`**：

```
claude   --effort <level>                       low, medium, high, xhigh, max
codex    -c model_reasoning_effort="<level>"    原本寫死 max
agy      包在型號後綴 gemini-3.7-flash-high      只有 low, medium, high
```

| 破壞什麼 | 該紅的測試 |
|---|---|
| agy 給不到的深度自動降級成 high | `test_agy沒有那幾階就當場炸` |
| codex 再加一條 `-c` 而不是取代寫死那條 | `test_codex走toml設定` |
| agy 後綴不換掉只是接上去 | 當時守這條的那支 agy 型號測試（2026-08-31 隨 gemini 白名單改名，見本檔最後一節） |
| 不認得的值不擋 | `test_不認得的值三家都炸` |
| claude 不加 `--effort` | `test_claude走effort旗標` |

五條都紅。

第一條是這一批的核心：**agy 只有三階，給它 `max` 而 nova 自作主張換成 `high`，
使用者會以為叫到了最深——帳照付、深度沒開、沒有任何訊息。**
那是「假的安全感」在這個旋鈕上的形狀。

### 第十八次（2026-08-30，執行識別碼：補上一個自己記過的洞）

第十六次結尾誠實記著「`NOVA_RUN_ID` 不驗格式那條**目前沒有測試守著**」。
守衛寫了，但**沒有測試背書的守衛按 nova 自己的規矩就等於沒有**。這次補上。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 不驗格式，有值就照用 | `test_看不懂的一律自己生一個` |
| 正規式放寬成 `\d{8}T\d{6}Z-.*` | `test_看不懂的一律自己生一個` |
| 完全不看環境變數 | `test_格式對就照用` |

三條都紅。

**第二條第一次被我判成「沒紅」，那是判錯了。** 我把它掛在
`test_不准夾帶路徑跑出去` 名下，而那支測試餵的是 `../../../etc/passwd`
——沒有時戳前綴，放寬後的正規式一樣不匹配，所以它不會紅。
真正接住這個破壞的是 `test_看不懂的一律自己生一個` 的四個參數化案例。

**保證是完整的，錯的是我對「誰在守它」的判斷。** 這跟第七、十三次同一族
（破壞打歪），但這次的形狀是：**同一個檔案裡有兩支測試，我挑錯了那一支。**
問法要再多一句：**紅的是不是我點名的那一支？如果不是，是誰接住的？**
（不能只看「有沒有紅」——那會讓一條真的沒人守的保證被別人的紅掩蓋掉。）

這一批的順序也是反的（實作先寫、測試後補），所以負控是唯一的證明。

### 第十九次（2026-08-30，長提示不准走 argv：把一個記憶變成殼）

2026-08-30 派三份研究，提示直接寫在雙引號字串裡：

| | 發生什麼 |
|---|---|
| 有 `agent(prompt, opts)` 這種括號 | zsh **當場 parse error**，exit 1 |
| 有 `` `docs/設計/` `` 這種反引號 | **靜默汙染**——那五段被替換成空字串 |
| 剛好沒有 | 正常 |

**最貴的是中間那個**：沒有報錯、程序正常啟動、燒完整的 token，
然後交出一份對著殘缺題目寫的答案。當場失敗的那個反而便宜。

我的第一個修法是寫進記憶——使用者當場指出「不然就都只是記憶」。

**偵測不可能**：反引號被吃掉之後那段字完全消失，nova 收到的是一份
看起來正常的短提示，它無從分辨「本來就沒有那段」跟「那段被吃掉了」。
所以只能**改通道**：`--提示檔` 與 stdin 都不經過 shell 解析，
argv 是唯一會被展開的那條——長提示一律禁走 argv。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 換行不擋 | `test_含換行就擋` |
| 訊息不給替代方案 | `test_兩條安全的路都要講` |
| `--提示檔` 被忽略（又回去讀 argv） | `test_提示檔多行多長都可以` |
| `跑` 不接同一份規則 | `test_跑也擋得住多行` |

四條都紅。

**型別也在替我們記事情**：`讀提示(*, argv片段, 提示檔)` 兩個關鍵字**都是必填**
（`提示檔` 可以是 `None`，但要傳）。給預設值的話，忘了接 `--提示檔` 的
子命令會安靜地只剩下危險的那條路——而那正是這個模組要防的事。
少傳一個在 mypy 就紅，不必靠人記得。

**規則不精確是刻意的**：`"$(cat 檔)"` 其實安全，卻也被擋。nova 站在邊界上
分不出安全的組法與危險的組法，而被擋下來的用法有一個嚴格更好的替代。

順帶修掉一個測試設計問題：那六支紅的測試第一次跑了 **224 秒**，
因為守衛還不存在，`--用 codex` 一路走到真的模型。改成一律帶假 CLI
——**測試在防護退化時的行為也是行為**。

## 2026-08-30：遮罩型別化（決策 0002 的第一個實例）

四個落盤點各自記得呼叫 `遮罩()`，2026-08-30 發現**四個裡漏了一個**（PR #95）。
修法是把「必須記得的事」變成「不得不傳的型別」：落盤點收 `已遮罩文字`，
餵裸 `str` 在 mypy 就紅。

| 破壞什麼 | 該紅的測試 |
|---|---|
| `成果.任務` 改回裸 `str` | `test_型別真的擋得住沒遮過的字[成果.任務]` |
| `帳本.事件.文字` 改回裸 `str` | `test_型別真的擋得住沒遮過的字[帳本.事件.文字]` |
| `_接續票內容` 的 `前情` 改回裸 `str` | `test_型別真的擋得住沒遮過的字[收件._接續票內容 的前情]` |
| `_寫下遮過的` 的 `文字` 改回裸 `str` | `test_型別真的擋得住沒遮過的字[收件._寫下遮過的 的文字]` |
| 歸檔繞過落盤入口自己 `write_text` | `test_收件的落盤只准走遮過的那個入口` |
| 在 `遮罩.py` 以外的檔案硬轉 `已遮罩文字(…)` | `test_不准在遮罩以外的地方硬轉` |
| `已經遮過了` 的 `因為` 給預設值 | `test_因為是必填的關鍵字` |
| 拿掉空理由的檢查 | `test_空理由不算理由`（3 支） |
| 閘的 mypy 拿掉 `--no-incremental` | `test_閘的mypy規則不會被假快取騙過去` |

九條都紅，而且**每條只紅指名的那一支**。

### 第一次跑的時候九條全部顯示「沒紅」——兩個獨立的原因

**① 比對器寫錯（我的問題）**：pytest 只把 parametrize id escape 成 `\uXXXX`，
測試函式名不會。我把兩者都 escape，期望字串永遠對不上。

**② mypy 的快取給了假結果（真缺陷）**：③ 與 ④ 兩條負控紅的都是③那一格。
成因是 **mypy 的快取鍵是路徑＋大小＋`int(mtime)`**，只在 mtime 對不上時
才去比內容雜湊——而那兩次破壞（`已遮罩文字` → `str`，各差 2 個字元）
讓檔案長度**完全相同**（6685 bytes），又落在同一秒。

這比 ruff 那條更容易踩：**連保留 mtime 的動作都不需要，同一秒內改兩次就中**。
人手改檔碰不到，腳本或 agent 連續改檔一定碰得到。
所以閘的 mypy 也帶 `--no-incremental`（146 個檔 0.07 → 1.09 秒），
測試裡自己跑的那個 mypy 也帶。由 `tests/整合/test_閘不准吃快取.py` 背書
（重現一支、閘一支，跟 ruff 同構）。

**教訓**：負控沒紅時先問「我的量法會不會就是成因」——這次兩個原因都在量法那邊，
而其中一個是真的缺陷。

## 2026-08-30：撞號不准靜默（三個落盤點）

執行識別碼撞號時三處都不出聲：帳本 `"a"` 交錯合併、`已處理/` `write_text`
靜默覆寫、背景輸出 `"w"` 靜默截斷。而且撞號**不是只有機率問題**——
`NOVA_RUN_ID` 只驗格式不驗歸屬，背景派工又會把它交給子程序，
一個殘留的值就是必然撞號。

| 破壞什麼 | 該紅的測試 |
|---|---|
| `開帳本` 的 `"x"` 改回 `"a"` | `test_同一個識別碼開第二次要當場炸`、`test_撞號的退出碼是確定失敗` |
| `歸檔` 的 `open("x")` 改回 `write_text` | `test_同號歸檔第二次要當場炸`、`test_第一份成果不准被蓋掉` |
| 背景輸出的 `"x"` 改回 `"w"` | `test_同號的背景輸出檔不准被截斷` |

三條都紅，而且**每條只紅指名的那幾支**。
**退出碼釘在 1（確定失敗）**：開檔當下還沒叫過模型、沒有副作用，重跑安全；
3 的語意是「不知道做了沒、不准重跑」剛好相反，4 是工作流停止規則專用。

### 第一次跑的時候三條都「多紅了別支」——`__pycache__` 給了假結果

**這是今晚第三個「快取鍵不含內容」的坑**（前兩個是 ruff 與 mypy），而且最嚴重：
它讓**跑的不是你剛寫回去的那份程式碼**。

Python 判斷 `.pyc` 新不新鮮，看的是它記在檔頭的
**source 的 `size` ＋ 整數秒 `mtime`**，不含內容。而這次的破壞是
`open("x")` → `open("a")`——**一個字元換一個字元，size 完全不變**；
還原又落在同一秒，於是：

```
pyc 記的 source mtime=1788060537 size=9484
py  實際的      mtime=1788060537 size=9484   → 判定新鮮，用舊的
```

症狀最難認的地方：`inspect.getsource()` 讀的是 `.py`，**印出來是新的**；
執行的是 `.pyc`，**跑起來是舊的**。所以「程式碼明明改了、行為就是不變」，
而且怎麼看原始碼都看不出問題。追了大約 15 分鐘。

`PYTHONDONTWRITEBYTECODE=1` **只擋寫、不擋讀**，舊的 pyc 照樣會被撿起來用。
唯一可靠的是**把 `__pycache__` 刪掉**。負控腳本現在每一輪都先清一次。

實測代價：`pytest tests/單元` 有 pyc 0.86 秒、冷編 1.02 秒（差 0.16 秒）。
所以閘的 pytest 規則跑之前一律清掉 `src/` 與 `tests/` 的 `__pycache__`。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 閘的 pytest 不再清 `__pycache__` | `test_pytest的閘跑之前會把pycache清掉` |

**這條的第一版是假綠。** 測試原本直接呼叫 `_丟掉pycache(專案根目錄)`
再斷言目錄空了——負控當場證明：把閘裡那一行呼叫刪掉，它照樣過。
測到的是**函式本身**，不是**它被接上去了沒**。改成走 `建規則表` 真的跑一次
閘的 pytest 規則之後才紅得起來。

**教訓（第二次寫下同一句）**：負控沒紅、或紅到別支時，
先問「我的量法會不會就是成因」。今晚三次負控異常，三次都在量法那邊，
其中兩次是真的缺陷（mypy 快取、Python pyc）。

## 把轉移表變誠實那一輪（出口窮舉 ＋ `階段表` 參數）

| 破壞什麼 | 結果 |
|---|---|
| 從 `TDD階段表` 某一列拿掉一種出口（`結果未知`） | **匯入 `狀態機.py` 當場 `ValueError: test 的出口沒窮舉：缺 ['unknown']`**，8 個測試模組 collection error。機制本身由 `test_少宣告一種出口就建不出階段定義` 背書 |
| `跑工作流` 的索引改建自全域 `TDD階段表`（等於又忽略參數） | `test_傳進去的表真的會被走` 等 3 支紅（就是 `Test階段表真的是傳進來那一張` 那一整組，其餘 567 支全綠） |
| 把「寫一支會紅的測試」的 `結果未知` 從護欄接成中止 | `test_結果未知在每個階段都是護欄`、`test_結果未知是護欄不是壞掉`、`test_任何階段結果未知都要停`、`test_結果未知當場停` 共 4 支紅 |

**第一條的紅不是「一支測試紅」，是整個模組匯不進來。** 那正是這條保證要的形狀
（「建表當下就炸」），但它會**淹掉別的紅**——所以指名的執法點是那支自己組一個
缺格 `階段定義` 的測試，它不必把正典的表弄壞就證得出訊息說得出少了哪一格。

## 2026-08-30：`nova 重構`——第一個能單獨叫的節點

使用者的話：「worker 內的每一個節點都應該也是可以單拆出來使用……而且也都能隨時組裝。」

在它存在之前，要重構只有兩條路：跑整條 TDD 七階段（貴、慢、表達不了
「只整理這個模組」），或者自己動手。**第二條便宜一個數量級**，
所以護欄擋不擋得住不重要——沒有人會走那條貴的。給路跟補洞是同一件事的兩半。

護欄的判準是**跑之前拍一張快照、跑之後再拍一張**，不是「模型說它沒改」。
重構員的提示第一條就寫著「不准改任何測試檔」，但那是懇求：模型可以忽略，
而且忽略了沒有人會發現——測試被改掉之後跑起來還是綠的。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 護欄只比內容（不看新增與刪除） | `test_刪掉一支測試是最嚴重的那種`、`test_新增測試也算動到` |
| 護欄不濾非測試路徑（改實作也算違規） | `test_改實作不算動到測試` |
| 子命令跑完不比對快照 | `test_動到測試檔就回護欄`、`test_護欄要說得出動到哪一個檔` |
| 重構不帶角色提示（變成裸的 `nova 問`） | `test_重構要把重構員的規矩交出去` |

四條都紅，而且每條只紅指名的那幾支。

**兩層都要有，缺一不可**：只有護欄的話模型每次都會踩（事後攔截，token 已經花了）；
只有提示的話它踩了沒人知道。前者是實線的包圍環，後者是事前告知。

**刪除為什麼要單獨列**：它在「逐檔比對內容」裡長得像什麼都沒發生——
那個鍵直接消失了。少了這一條，重構員把礙事的測試刪掉就過關。

## 2026-08-30：Bash 護欄改看行為（**第一件真的用 nova 做完的實作**）

hook 有兩條 PreToolUse：`Edit|Write|NotebookEdit` 走 `nova 檢查編輯`（會擋、
要求走 nova 或記繞過理由），`Bash` 走 `nova 檢查指令`（只擋三條禁令，
完全不看寫了什麼檔）。於是 `python3 - <<'PY' … write_text(…) PY` 一路綠燈，
而用 Edit 改同一個檔會被擋——**護欄的判準是工具名，不是行為**。

這一輪是**交給 `nova 跑` 做的**：測試員寫了 214 行 15 支測試（275,600 token，
撞護欄停在實作前）；`--起點 impl` 接著跑第二輪 5 階、674,254 token，
實作、驗證綠、重構都做完，48 支測試綠、九條閘全綠。

| 破壞什麼 | 該紅的測試 |
|---|---|
| `檢查指令` 不看寫檔（回到只擋三條禁令） | `test_寫入管轄檔案要被擋下並提示繞過或走nova`、`test_沒說理由就照擋` |
| 判準放寬回「任何相對路徑」 | `test_重導到不像路徑的東西要放行` |
| 繞過對 Bash 無效（拿掉出路） | `test_說得出理由就放行` |
| 訊息又填回佔位符 | `test_擋下來的訊息要帶真的會話識別碼` |

四條都紅，而且每條只紅指名的那幾支。

### 護欄接上的那一刻就把作者鎖在外面（三個缺陷，都是實際踩到的）

**一、判準過寬。** 重導目標只問了「在不在管轄範圍」，而 `在管轄範圍嗎`
對任何相對路徑都成立（只要不以 `.` 開頭）——於是 `echo 說明 > 一句話`
裡那句中文也被當成受管轄的路徑。收緊成「要真的落在 `src/`、`tests/`、
`docs/` 底下」，跟 python heredoc 那條用同一份前綴表。

**二、訊息裡是字面佔位符。** 擋下來印的是 `nova 繞過 --會話 <會話識別碼>`。
hook 的 JSON 本來就有 `session_id`，`檢查編輯` 一直都接著用，只有這條漏了。

**三、繞過對 Bash 這條路完全無效。** `檢查指令` 沒有叫 `說得出理由了嗎`。

三個加起來的後果是**死鎖**：想記一次繞過理由，`nova 繞過` 那條指令自己
被擋下——因為理由裡引用了訊息中的佔位符，那個角括號命中重導樣式。
**它印給人照做的那句話，照做就被自己擋。**

解法是把理由裡的角括號換掉才記得成。三條都修了，各有測試背書。

## 思考深度進派工表（2026-08-30，nova 自己做的，排程撿走那張票）

| 破壞什麼 | 該紅的 |
|---|---|
| `派法.思考深度` 給預設值（變成「忘了傳也安全」） | `tests/單元/test_派工表.py::test_派法必須明確指定思考深度` |
| 推理那格的深度從 `max` 改掉 | `tests/單元/test_派工表.py::test_推理工作的思考深度是max` |
| `固定提示角色` 不把深度傳進 `呼叫選項` | `tests/單元/test_角色.py::test_思考深度會傳進呼叫選項` |

三條都紅，而且只紅指名的那一支。

### 第四條沒紅——而那正是它要說的事

破壞 `_子命令_問` 裡的 `思考深度=這次.思考深度`（改成 `None`），
`tests/整合/test_派工接線.py` 的 `test_例行工作會帶上深度high` 與
`test_推理工作會帶上深度max` **兩支都照樣綠**。

原因不是測試寫壞了，是**兩家的預設剛好等於表裡的值**：

| | 表裡寫的 | 那一家的預設 | 所以 |
|---|---|---|---|
| 例行（agy） | `high` | `gemini-3.7-flash-high` | 拿掉接線，型號一個字都不變 |
| 推理（codex） | `max` | `codex推理強度 = "max"` | 拿掉接線，`model_reasoning_effort` 一個字都不變 |

所以那兩支證明得了「旗標真的送到 CLI」，證明不了「送過去的值來自派工表」。
**`nova 問` 那半的接線現在沒有測試背書**，`nova 工作流` 那半有（第三條紅了）。

要補得起來只有一條路：讓表裡某一格的值跟那家的預設不同。
那是**策略**（使用者裁定 agy 用 high、codex 用 max），不是實作細節，
所以不准為了讓測試有鑑別度去改它。這一格留著，等哪天策略真的變了再補。

## 2026-08-30：節點契約的終局窮舉

這一輪補上節點契約缺的那半：四種終局、退出碼數值、六種護欄原因、不可變、
明傳上下文與結構型節點。六條負控各自只改一格；負控 runner 每次先用
SHA256 確認 `src/nova/契約/節點.py` 確實不同於基線，並在測試前後清掉
`src/` 與 `tests/` 底下的 `__pycache__`，也確認跑的是專案 `.venv` 的 Python。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 拿掉 `節點結果未知.結果` property | `test_節點結果未知的結果是退出碼三` 紅 |
| 把 `結果未知 = 3` 改成 `2` | `test_節點結果未知的結果是退出碼三` 紅 |
| 刪掉 `護欄原因.扇出超限` | `test_護欄原因六種且只有這六種` 紅 |
| 拿掉 `節點上下文` 的 `frozen=True` | `test_節點契約資料類別不可變` 紅（`節點上下文` 案例） |
| 把 runner 的 `上下文=上下文` 改成錯的物件 | `test_節點上下文每一格都由呼叫者明傳` 紅 |
| 讓 runner 拒絕沒有顯式基底的節點 | `test_執行節點接受不繼承基底的形狀相同物件` 紅 |

六條都紅；每次還原後節點契約檔雜湊都回到基線。

## 2026-08-30：系統架構師的架構決策契約

這一輪補上研究證據到 PlanWorker 之間的 typed 邊界：來源、節點契約、四種結果出口、
停止政策與未決實驗都放在 frozen dataclass；驗證器拒絕六種不能交給計畫員的決策問題。
節點契約清單由呼叫端明傳，沒有偷建全域 registry。每次負控都先用 SHA256 斷言
目標檔案真的變了，測試前後都清掉 `src/` 與 `tests/` 底下的 `__pycache__`，還原後再
斷言雜湊回到基線。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 驗證器拿掉缺來源檢查 | `test_沒有來源的決策不能交給計畫員` 紅 |
| 驗證器拿掉缺停止政策檢查 | `test_沒有停止政策的決策不能交給計畫員` 紅 |
| 驗證器拿掉呼叫端提供的節點契約存在性檢查 | `test_不存在的節點契約要被拒絕` 紅 |
| 驗證器不再要求四種終局都出現在出口 | `test_出口必須窮舉四種終局` 紅 |
| 驗證器接受沒有到期或驗證方式的未決實驗 | `test_未決實驗要有到期與驗證方式` 紅 |
| 驗證器拿掉重複節點識別碼檢查 | `test_節點規格識別碼重複要被拒絕` 紅 |
| 驗證器在第一個問題後提早返回 | `test_同時犯多種錯時要全部報出來` 紅 |
| 驗證器反向拒絕完整出口 | `test_完全合法的決策通過驗證` 紅 |

八條都紅；每次還原後架構決策契約檔雜湊都回到基線。

### Review 補強：型別與角色接線

`節點規格.停止` 改成 `停止政策 | None`，讓驗證器檢查的壞輸入在型別上可表達；
`節點=()` 則明訂為合法的空決策，表示這次不需要執行節點，不把沒有實際節點誤報成缺停止政策。
`出口表版本` 的註解也指回 `節點規格.允許結果`，避免把出口窮舉誤認成另一張表。
`未決實驗` 的到期與驗證方式不可留白；四種終局則要求每個節點各自完整列出。

系統架構師藍圖仍由既有 `建角色表` 組出，並固定為唯讀；`什麼時候派我` 同時寫明
「系統架構師」負責工作流形狀與契約，「計畫員」只決定這次先做哪幾步；提示也明寫
架構師不寫 code、不產 Graph runtime、不執行自己產的決策。

本輪兩條角色負控同樣先比較破壞前後雜湊，清快取後只跑指名測試，還原後再比較基線：

| 破壞什麼 | 該紅的驗證 |
|---|---|
| 系統架構師藍圖改成可編輯 | `test_系統架構師只讀且不執行自己的決策` 紅 |
| 提示拿掉「不准執行自己產的決策」 | `test_系統架構師只讀且不執行自己的決策` 紅 |

兩條都紅，負控後都還原到原始碼基線。

### 合併 #116 之後，那條接線一度變成隱形的

角色改走 `迴圈/角色工廠.py` 這條路之後，我照規矩對合併結果做一次負控：
拿掉 `建角色` 裡的 `思考深度=藍圖.思考深度`，**整套測試沒有一支紅。**

原因跟上面那一節同一個：既有的整合測試比對 CLI argv，而
agy 的預設型號本來就叫 `gemini-3.7-flash-high`、
codex 的預設推理強度本來就是 `max`——**兩家的預設剛好等於表裡的值**，
所以拿掉接線，argv 一個字都不會變。

補 `tests/單元/test_角色工廠.py::test_藍圖的模型與思考深度要傳進角色`：
在單元層直接比對，而且刻意用**跟任何一家預設都不同的值**（`xhigh`、
一個不存在的型號）。

| 破壞什麼 | 該紅的 |
|---|---|
| `建角色` 不傳 `思考深度` | `test_藍圖的模型與思考深度要傳進角色` |
| `建角色` 不傳 `模型` | 同上 |

兩條都紅在那一支。**這一節是負控自己逼出來的一支測試**——
如果只看「九條閘全綠」，這個洞會一路留著。

## 2026-08-30：單獨節點薄 runner

新增 `nova.迴圈.節點.單獨執行`，只經既有的 `契約.節點.執行節點` 呼叫一次；根上下文由
`根上下文` 固定成無工作流、`root` 分支與空父邊。runner 也會在可觀測的呼叫邊界檢查停止
政策與輸出契約，結果未知原樣停下，不另開角色／LLM 路徑。

| 破壞什麼 | 該紅的測試 |
|---|---|
| runner 不轉遞呼叫者提供的依賴 | `tests/單元/test_節點單獨執行.py::test_沒有工作流也有根上下文與停止政策` |
| runner 不轉遞停止政策 | `tests/單元/test_節點單獨執行.py::test_沒有工作流也有根上下文與停止政策` |

固定負控將 `依賴=依賴` 改成 `依賴=None`；指定測試由綠變紅，還原後再驗證。

## 2026-08-30：行號錨點改由變異操作推導

這一輪修的是登記表的脆弱錨點，不是關掉 `WRONG_TEST`。前一晚有兩種漂移：
「逾時」那刀登記 `{276}`，實際命中 `258`；「本地腦能力邊界」那刀則是在
`派工表.py` 上方加兩行註解後，別人填的 `{29}` 漂成 `{30, 31, 32}`，
而且改檔的人與看到閘紅的人不是同一個。

現在 runner 從操作的唯一文字錨點讀基線檔，推導錨點涵蓋的全部 executable 行；
跨行錨點不再只取第一行，docstring 等不可執行行也不會被誤當成 footprint。
推導不到錨點、操作不支援或最後得到空集合時，直接回 `RUN_ERROR`，不靜默放過
coverage 前置；唯一例外是登記表明寫 `必須覆蓋=frozenset()`，且操作是模組層
單一名稱常數替換，代表測試直接驗值。`WRONG_TEST` 仍在套用變異前判定，
`SURVIVED` 仍只表示走到破壞點卻沒有把變異殺掉。

`必須覆蓋=None` 代表由操作推導；非空集合只作少數非區域性操作的額外 coverage
斷言，一般登記不再填行號。顯式空集合不是預設值，且不能套在一般操作上逃掉推導。
目前登記表另加兩條固定負控，確保推導退化時閘本身會紅：

| 破壞什麼 | 該紅的測試 |
|---|---|
| 推導永遠回空集合 | `test_行號由操作推導且點錯先報WRONG_TEST_正常變異是KILLED` |
| 跨行推導只取首行 | `test_跨行錨點的每個可執行行都被推導` |

兩條固定負控都已實跑並被殺掉；原有 12 刀也以不填行號的登記重新跑過。

## 2026-08-30：單獨節點停止政策逐條負控

補上 `單獨執行` 缺的停止政策測試；每一條護欄原因都有自己的指名測試，另加一支
政策沒有超限時不得誤擋的反面。`邊識別碼` 也實際用來組輸入來源的父邊，因此移除
`pyproject.toml` 裡的 `F401` 例外後 ruff 仍然通過。

固定負控共八條。每次破壞前先清 `src/` 與 `tests/` 下的 `__pycache__`；驅動器再斷言
測試名存在、`src/nova/迴圈/節點.py` 的 SHA256 與基線不同、使用的是專案 `.venv` 的
Python，並要求 pytest 輸出含被點名的測試名。每次還原後再次清快取，SHA256 回到
`f0520adc21d625373beded7efa147ce6882256784d561053d4e7c5fab9634dcc`。

| 破壞什麼 | 該紅的測試 |
|---|---|
| `最多呼叫 < 1` 改成 `< 0` | `test_最多呼叫小於一時回步數護欄且不呼叫節點` 紅；原因變成 `輸出不合約` |
| `最多秒 <= 0` 改成 `< 0` | `test_最多秒為零時回逾時護欄且不呼叫節點` 紅；原因變成 `輸出不合約` |
| 輸出來源的 `執行 == 上下文.執行` 改成 `!=` | `test_輸出來源不合上下文契約時回輸出不合約護欄` 紅；不合約成功被放行 |
| 未知結果不重跑的條件加上 `not` | `test_結果未知不重跑時原樣回結果未知` 紅；未知被改成 `預算` 護欄 |
| token 護欄的 `>` 改成 `<` | `test_結果超過token上限時回預算護欄` 紅；超額成功被放行 |
| 呼叫後秒數護欄的 `>` 改成 `<` | `test_呼叫後超過秒數上限時回逾時護欄` 紅；超時成功被放行 |
| 無進展護欄的 `< 1` 改成 `> 1` | `test_成功結果無進展且上限為零時回無進展護欄` 紅；無進展成功被放行 |
| token 護欄的 `>` 改成 `>=` | `test_政策寬鬆時成功結果不誤判護欄` 紅；恰好等於上限被誤擋成 `預算` |

八條都紅，而且每條都紅在點名的那一支；全部還原後目標測試 9 支、ruff 與 mypy
均為綠。

## 2026-08-30：分派單先驗證再交付

這一輪的固定負控基線是 `src/nova/迴圈/計畫.py` 的 SHA256：
`ef0505d8fe5cacb14ac490d657437346c709d1992fbe86e320df2c5fe61d4986`。負控腳本在
每次跑測試前先斷言破壞後的雜湊真的不同，也先斷言指名的測試名稱存在；每次破壞前、
破壞測試後、還原後都清掉 `src/` 與 `tests/` 底下的 `__pycache__`，並確認用的是
專案 `.venv` 的 Python。三次還原後雜湊都回到基線。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 把未知節點的判斷反過來 | `test_未知節點要被拒絕` 紅；破壞後雜湊 `3ecae23738b12c9e628739f3938cf70f7e439b0a38623e142ac0abd5ac920e40` |
| 把循環前置的判斷反過來 | `test_循環前置要被拒絕` 紅；破壞後雜湊 `af6bc3bed2d74c1d13dabd4405117a0c8f2ed6a9187fb30ffb7c1bf6f9698d19` |
| 把「自報總額超過 host」整個判斷反向 | `test_超出預算要被拒絕` 紅；破壞後雜湊 `8de610f7ff65e93d727eeecfd636909391f2ac4ceaa38825a143beae13c1dcc7` |

三條都只改一格；還原後指定測試復綠。

### 預算負控第一次沒有紅：先問點名，再問破壞是否打中

第一次只把預算三項 `or` 裡的呼叫次數比較從 `>` 改成 `<`，
`test_超出預算要被拒絕` 仍然綠。先用 `rg` 確認測試名存在且確實被執行，才發現
token 與秒數的兩個比較仍是 `>`，所以同一支防護還是報了 `超出預算`；不是點名
錯，也不是測試漏了。還原後改成反轉整個 `if 分派單總額超過主機` 分支，才得到上表
的真紅。負控不能只看「我改了一個字」；要確認那一格真的讓被測的判斷失效。

## 2026-08-30：扇出屏障與分支終局

這一輪完成第一個研究扇出節點的固定負控。負控腳本先用 SHA256 確認每次破壞
真的改到 `src/nova/迴圈/扇出.py`，只跑指名測試，前後清掉 `src/` 與 `tests/`
底下的 `__pycache__`，並用專案 `.venv` 的 Python 執行。四條都紅；每輪還原後
扇出 runner 的 SHA256 都回到基線。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 屏障在必要分支全回來前就把結果交給整合 | `tests/單元/test_扇出.py::test_必要分支未全部回來前不呼叫整合` |
| 把必要分支的逾時降成確定失敗或成功 | `tests/單元/test_扇出.py::test_必要分支逾時時整階終局是結果未知` |
| 把必要分支的結果未知當成空結果或成功 | `tests/單元/test_扇出.py::test_必要分支結果未知時整階終局是結果未知` |
| 把必要分支的確定失敗誤判成結果未知 | `tests/單元/test_扇出.py::test_必要分支確定失敗時整階終局是確定失敗` |

## 2026-08-30：扇出 §3.5 六個上限

這一輪補上扇出政策六個上限的測試背書，另加一支寬鬆政策反面測試。
開跑前拒絕的兩條鎖定具名 `ValueError` 原因；派送期的三條鎖定整階
`結果代碼.護欄`（code 4）；最大並行數則鎖定同時進行數不超過政策，
且寬鬆政策仍然回成功。六輪負控各只改一條檢查，沒有改扇出契約形狀。

負控每輪先以 SHA256 確認 `src/nova/迴圈/扇出.py` 的雜湊不同於基線
`a52f9c1f2782ddfdbae05765982cd8ab588f21e2b0a7b105db3f8accff83450d`，
測試前後都清掉 `src/` 與 `tests/` 底下的 `__pycache__`，並以專案
`.venv/bin/python` 只跑指名測試。每輪都以非零退出回報紅；還原後六輪
雜湊都回到上述基線。

| 破壞什麼 | 破壞後 SHA256 | 該紅的測試 |
|---|---|---|
| 拿掉 `最大分支數` 的開跑前拒絕 | `a11c50fa8e9be4b05555effd832daf0b65db2f066c91ad20b9edc9062128ce03` | `test_最大分支數超過時開跑前拒絕` |
| 拿掉階段 token 已累計到上限的 admission 檢查 | `39fb325123cfa124c454152075f47dded3c230f8b1bd1df45dec44bf446f4fd9` | `test_階段最多token累計到上限就不再派下一顆` |
| 拿掉「每分支最多 token × 分支數」開跑前預留檢查 | `cdf21d66d58609f9239c0448ef8764208b11cbd043790dc6d3ede214d3e9607c` | `test_每分支token預留總和超過階段上限時開跑前拒絕` |
| 拿掉最多秒的 admission 檢查 | `653ae99d812f75e12066713b0814420c2edece598334cefa62f03451dd6ad768` | `test_最多秒到期後不再派下一顆` |
| 拿掉最多呼叫的 admission 檢查 | `3a735a164f841accc42664266d3e9eadbad0ec4c679226ea1fc07b51185d326d` | `test_最多呼叫用完後不再派下一顆` |
| 拿掉最大並行數的 scheduler admission 檢查 | `1cd9ab20552a0b819a14d370c1ae46651ef528166149ef043472c1aa1e847862` | `test_最大並行數限制同時進行中的分支數` |

反面測試是 `test_政策寬鬆時不誤判成護欄`；它沒有另做破壞，和六條
負控一起確認「擋得住」不會退化成「一律擋掉」。

## 2026-08-30：重構階段接上 lint／格式與 ruff 豁免登記

這一輪的負控腳本都先用 SHA256 斷言破壞確實改到目標檔，破壞前後都清掉
`src/` 與 `tests/` 底下的 `__pycache__`；還原後再驗一次雜湊。三條 ruff
負控都跑 `test_ci閘經建規則表真的會跑ruff豁免`，不是直接叫檢查函式。

| 破壞什麼 | 該紅的結果 |
|---|---|
| 往 `pyproject.toml` 偷加 `ignore:F401` | `test_ci閘經建規則表真的會跑ruff豁免` 紅，列出 `多了：ignore:F401`、`少了：無`，並印出「加豁免不是修 lint。要加，先改登記表並說明理由」 |
| 從 `豁免登記.py` 拿掉實際存在的 `ignore:N` | `test_ci閘經建規則表真的會跑ruff豁免` 紅，列出 `多了：ignore:N`、`少了：無`，並印出同一句理由 |
| 把 `規則表.py` 裡 `ruff-exemptions` 那一列整條刪掉 | `test_ci閘經建規則表真的會跑ruff豁免` 紅在「沒有從規則表跑到」；這條守的是閘本身還在不在 |
| 拿掉 `建TDD執行器` 對 `跑重構判準` 的接線 | 固定負控腳本的接線斷言紅：被叫的是普通判準，不是 lint／格式判準 |
| 把驗證重構的紅出口改回護欄 | `test_驗證重構的lint或格式紅了要退回重構員` 紅 |

ruff 三條負控的基線／破壞後 SHA256 如下：`pyproject.toml`
`6a08603df7ff31fb3a90b237f66b0cf08b583274cf7df8bf34679e1981af8914`
→ `7938584d5e8235ab359e7c7cf560cfc561dbb4c4806819e9f73ff808f8224624`；
`豁免登記.py` `bd32973d589d9535d9288f40599ae786fc61e8a1b3db66d46a850e763dd02823`
→ `9e4350902bb3e117aa92516557d1650098086215ca715dac8509e4d8fefddbc8`；
`規則表.py` `659eba62043fcffdcb67ad93595702767f18bb5d73405ddf0c3c7cc73247f5d3`
→ `e096c97f40104aefd1c331b0b6e3a00d1495f0b68eda3d13317557e3d3709973`。
三條都在還原後重新檢查，雜湊均回到各自基線；重構的兩條也維持原紀錄。

## 2026-08-30：工作流逾時跟工作形狀走

這一輪讓 `nova 跑` 與 `nova 工作流` 都能用 `--逾時`，而且明確指定的值會一路
進到 `呼叫選項.逾時秒`；未指定時，TDD 每一階使用一小時的有限預設，不再和單次
問話共用 1800 秒。模型轉接器不再用模型名稱改寫呼叫端的值。逾時回應也會留下
實際耗時、上限、結果未知、不准重跑與去看工作區的指示。

五條固定負控都由登記器先確認 SHA256 真的改變，測試前後清掉 `src/` 與 `tests/`
底下的 `__pycache__`，各只跑點名的紅測試；還原後再確認回到基線。

| 破壞什麼 | 該紅的測試 |
|---|---|
| TDD 階段預設退回單次問話的 1800 秒 | `test_TDD階段的預設逾時比單次問話長` |
| 逾時決定又被高階模型改寫 | `Test逾時由呼叫端決定::test_呼叫端指定的高低逾時都有效` |
| 工作流旗標沒有傳進 `呼叫選項.逾時秒` | `test_跑與工作流的逾時真的傳到呼叫選項` |
| 逾時訊息不帶實際上限 | `test_逾時訊息要帶實際秒數上限與不准重跑指示` |
| 逾時診斷退到半成品後面 | `Test逾時的回應::test_逾時診斷要在第一行` |
| `WRONG_TEST`：逾時訊息負控原登記 `src/nova/載體/模型/轉接.py:276` | 實測被點名測試命中的是可執行行 `258`；最後判定為第 2 種（`必須覆蓋` 的行號錯誤），改登記為 `258`，重跑後該刀 `KILLED` |

## 2026-08-31：`nova 線` 看板

本票另外以兩條固定負控確認唯讀看板的缺資料分支與子命令接線沒有消失；兩條都跑
`tests/整合/test_線.py::test_沒有成果帳本時上一次怎麼收要誠實說查不到`，並在破壞後
恢復原狀。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 把成果帳本不存在的分支反向 | `test_沒有成果帳本時上一次怎麼收要誠實說查不到` |
| 拿掉 `線` 子命令的唯一處理函式登記 | `test_沒有成果帳本時上一次怎麼收要誠實說查不到` |
## 2026-08-30：禁令只判斷真正執行的命令

這一輪修正 `src/nova/載體/禁令.py` 的命令判斷：先去掉 heredoc 內文，再保留引號
剖析命令段；環境變數、`sudo`／`env`、括號與 git 全域旗標只改變命令外形，不能
讓真正的 `git`／`gh` 命令繞過硬禁令。每輪負控都由 runner 先確認目標檔案 SHA256
確實改變，測試前後清掉 `src/` 與 `tests/` 底下的 `__pycache__`；舊的 `_危險詞`
原文掃描機制已由剖析取代，歷史紀錄仍保留作為不可重播的變更背景。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 把硬禁令剖析改回包含 heredoc 內文 | `test_heredoc內文提到缺少刪除分支的合併指令仍然放行` |
| 把保留引號的 `posix=False` 改成 `posix=True` | `test_git_commit訊息提到旗標仍然放行` |
| 拿掉環境變數與 `sudo`／`env` 前綴剖析 | `test_命令前綴包住真的git仍然要擋` |
| git 全域旗標不吃掉 `-C` 的參數 | `test_命令前綴包住真的git仍然要擋` |
| tokenizer 不把括號拆成命令分隔詞 | `test_命令前綴包住真的git仍然要擋` |

五條都紅；還原後目標檔案雜湊回到基線。六列情境另由上述測試檔逐列釘住：合併缺
`--delete-branch`、heredoc 內文、`echo` 文字、實際 `git commit --no-verify`、文件
內文，以及 `git commit -m` 的訊息文字。

## 2026-08-30：無進展護欄不准空轉

無進展的機械判準是：「同一批測試連續紅三輪，且工作區雜湊未變，才算無進展。」
門檻取 3：小於已發生的 12 輪，也保留兩輪回饋確認的餘裕；審查退件的正常回頭邊
不會因為階段重複就被截斷。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 同一批紅測試的停止條件從 `>=` 改成 `>` | `test_無進展護欄.py::test_同一批失敗測試且工作區未變要提早停` |
| 無進展舊路徑拿掉紅色過濾並混用階段證據 | `test_工作流.py::Test審查的回頭邊::test_審查一直要求修改會撞到步數上限` |
## 2026-08-30：ruff 格式排除進入豁免登記

前置測試先在自包含的最小 TOML 放入 `[tool.ruff.format].exclude`；實跑
`tests/單元/test_豁免登記.py::test_格式排除要被當成未登記豁免而指名` 得到紅燈，
原解析器錯把它當成「ruff 豁免符合登記表」。修正後，訊息會列出
`多了：format:exclude:tests/單元/test_架構決策.py`，並印出「加豁免不是修 lint」。
同一輪也先在最小 TOML 放入 `[tool.ruff].extend`；實跑
`tests/單元/test_豁免登記.py::test_外接設定要被當成未登記豁免而指名` 得到
`1 failed`，原解析器同樣錯把它當成「ruff 豁免符合登記表」。

這次盤點並納入登記的鍵是：頂層 `exclude`／`extend-exclude`／`include`／
`extend-include`；
`[tool.ruff.lint]` 的 `exclude`／`ignore`／`extend-ignore`／
`per-file-ignores`／`extend-per-file-ignores`；鎖定的 Ruff 0.16.5 實測仍接受、只發
deprecated 警告的頂層 `ignore`／`extend-ignore`／`per-file-ignores`／
`extend-per-file-ignores` 也用同樣標籤納入；頂層 `extend` 外接設定則登記為
`extend:路徑`；以及
`[tool.ruff.format]` 的 `exclude`／`extend-exclude`。本 repo 原有的
`extend-exclude = ["docs"]` 因此登記為 `extend-exclude:docs`。

既有頂層與 `[tool.ruff.lint]` 的 `ignore`／`per-file-ignores` 共用
`ignore:X`／`per-file-ignores:路徑:規則` 標籤，刻意不加 `lint:` 前綴；這輪不改既有
登記表語意，避免把同一項豁免當成新增或刪除。

頂層 `include`／`extend-include` 控制 Ruff 收集的輸入範圍；`include` 能收窄
預設範圍，`extend-include` 也會改變這份範圍設定，兩者攤平為
`include:路徑`／`extend-include:路徑`。即使目前 `pyproject.toml` 沒有設定，
之後新增時仍會當場變成 `多了：…`。也查過 `force-exclude`／`respect-gitignore`；兩者只決定排除規則在
`ruff check --no-cache .`／`ruff format --check --no-cache .` 中是否生效，目前未設定，
故不拆成路徑豁免項，但後續若啟用仍須在這裡說明。

`select`／`extend-select` 是整體規則基線，不是額外豁免；`lint.pydocstyle.convention`
目前設為 `google`，同樣是 D 規則基線；`lint.mccabe.max-complexity` 目前是 8，
是複雜度門檻，不是檔案豁免。這幾類都已查過，刻意不放進這份集合。`fixable`／
`unfixable`／`lint.extend-fixable` 只控制自動修正；`analyze.exclude` 只影響分析命令，
也刻意不納入這份 lint／format 豁免表。

單元測試 `test_各層排除鍵都會攤平` 與 `test_輸入範圍鍵也會攤平` 也逐一守住
頂層、lint 層與 format 層的 `exclude`／`extend-exclude`，以及頂層
`include`／`extend-include` 標籤；Ruff 的 lint 表沒有 `extend-exclude`，所以沒有
把不存在的鍵列入解析。負控保留 `格式排除` 變數，只把它替換成空集合，確保紅的是
「format 排除沒有登記」而不是模組載入錯誤。

| 破壞什麼 | 該紅的結果 |
|---|---|
| 把 `豁免登記.py` 的 format 排除解析替換成空集合 | `tests/單元/test_豁免登記.py::test_格式排除要被當成未登記豁免而指名` 紅，列出 `多了：format:exclude:tests/單元/test_架構決策.py`，並印出「加豁免不是修 lint」 |
| 把 `豁免登記.py` 的 `extend` 解析替換成空操作 | `tests/單元/test_豁免登記.py::test_外接設定要被當成未登記豁免而指名` 紅，列出 `多了：extend:ruff-base.toml`，並印出「加豁免不是修 lint」 |
| 把 `豁免登記.py` 的頂層排除解析替換成空集合 | `tests/整合/test_豁免登記接線.py::test_ci閘經建規則表真的會跑ruff豁免` 紅，指出少了現行的 `extend-exclude:docs` |
| 把 `豁免登記.py` 的舊式頂層豁免解析替換成只讀 lint 表 | `tests/單元/test_豁免登記.py::test_舊式頂層豁免鍵也會攤平` 紅，指出 `ignore`／`extend-ignore`／兩組 per-file 豁免沒有被抓到 |

## 2026-08-31：agy 代跑別家型號時不准加深度後綴

`agy models` 列得出 `claude-sonnet-4-6`、`claude-opus-4-6-thinking`、
`gpt-oss-120b-medium`——**那條通道的額度是獨立的一池**（實測週限額 100%、
Gemini 那池同時只剩 58%）。原本 `_agy型號` 無條件補深度後綴，
送出 `claude-sonnet-4-6-high` 這種不存在的型號，等於整池走不到。

負控先記 SHA-256 再斷言檔案真的變了，破壞前後都清 `src/`、`tests/` 的 `__pycache__`。

| 破壞什麼 | 原始雜湊 → 破壞後 | 該紅的測試 | 結果 |
|---|---|---|---|
| 拿掉族別判斷（退回無條件加後綴） | `8775cf2f…` → `7020424f…` | `test_agy跑別家型號時不准加深度後綴` | 紅，退出碼 1；還原後回到 `8775cf2f…` |

## 2026-08-31：agy 的 gemini 族只准一顆型號（白名單）

使用者裁定：Gemini 那池只用 `gemini-3.7-flash-high`。
**白名單不是黑名單**——agy 每次上架新型號，黑名單就漏一格，
而漏掉的那格會安靜地照跑（帳單看得到、當下看不到）。

副作用是誠實的：深度旋鈕對 gemini 只剩一格，給 `low`／`medium` **當場炸**，
不准默默用 high（默默降級＝使用者以為叫到了 low，帳照付、深度沒動、沒有訊息）。
claude／gpt 是另一個池，不受這條限制。

| 破壞什麼 | 原始雜湊 → 破壞後 | 該紅的測試 | 結果 |
|---|---|---|---|
| 把白名單判斷改成永不成立 | `0327f4c7…` → `780fb6d7…` | `test_gemini族的其他型號一律當場擋[gemini-3.1-pro]` | 紅，退出碼 1；還原後回 `0327f4c7…` |

**兩支既有測試守的行為被這條裁定取代了**，一併改成守新行為（不是刪掉）：
原本守「深度後綴會被換掉」與「gemini-3.1-pro 也能換型號」的那兩支，
改成守新行為（不是刪掉）：一支改成守「給 low 要當場炸」，
一支改成守「別族的型號原樣送出」。

**順帶記一個閘的限制**：`test_文件即事實::test_文件提到的測試都真的存在`
會掃文件裡所有 `test_…` 字樣，要求它們都存在。
但**歷史紀錄必然會提到已經改名的測試**——它不區分「現在的宣稱」與「當時的事實」。
這一輪是把舊名改寫成描述才過的閘，那是繞過不是修好。

## 2026-08-31：`--模型` 旗標，跟白名單同一輪進

白名單與旗標**必須同一輪落地**——先進旗標就會有一段時間是
「能指定型號但沒人管」，那正是白名單要擋的東西。

| 破壞什麼 | 原始雜湊 → 破壞後 | 該紅的測試 | 結果 |
|---|---|---|---|
| 拿掉命令列把旗標套進藍圖那兩行 | `b13ab4d6…` → `8c52bb68…` | `test_模型旗標真的傳到呼叫選項` | 紅；還原後回 `b13ab4d6…` |

另外一支 `test_不給模型旗標就照派工表` 守的是**沒給旗標時不准動策略**——
只測「給了會生效」那一半，等於沒守住「不給也偷偷改掉」這個風險。

## 2026-08-31：「換一顆腦」的判準是對話不是家族名

使用者裁定：**同一家的模型，只要是 new session 就不是同一個人審查。**
獨立性來自 context 隔離，不是模型權重不同；換一家能多減一層
（不同權重、不同偏誤），但那是加分不是門檻。

查證過的兩件事，都跟預期一致：

- `派工()` 在建腦前**拿不到對話識別碼**（那是呼叫之後才有的），
  所以事前檢查能做的只有家族名——而那正是要放寬的東西。
- `docs` 與 `契約/帳本.py` **都沒有記 `對話識別碼`**，
  所以「事後驗證兩邊是不同對話」現在沒有背書。

因此真正的保證落在**結構**：`固定提示角色.做()` 組呼叫選項時
沒有續接這個欄位，所以它不可能續接到同一個對話。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 讓角色偷偷帶一個續接值 | `test_角色結構上不可能續接到同一個對話` |
| 把本地腦的審查資格檢查一起放寬 | `test_本地腦仍然不准當審查員` |

第二刀守的是**放寬的範圍**：這次鬆的只有家族名那一條，
9B 的實測邊界（長提示 2/3、誠實拒答 1/3）沒有因此改變。

## 2026-08-31：命令列那條路也要放寬——兩個呼叫端只改一個等於沒改

`#140` 改了門面（`nova.派工`）的家族名檢查，但 `src/nova/載體/命令列.py`
**還留著自己那一份**，所以 `nova 跑 --用 agy --審查用 agy` 仍然被擋在門口。
序列跑第一條任務的時候當場撞上。

這正是 `#138` 那輪的同一個教訓（`審查資格理由` 有門面與命令列兩個呼叫端），
而我在 `#140` 驗證了門面卻漏了命令列。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 把家族名檢查加回命令列 | `test_命令列也不再用家族名擋同一家` |
| 拔掉命令列的本地腦資格檢查 | `test_命令列的本地腦資格檢查沒有被一起放寬` |

### 過程中踩到的兩個坑，都記下來

**一、測試假綠。** 第一版測試用 `subprocess` 跑
`nova 跑 … --審查執行檔 …`，但 `--審查執行檔` 是**門面的參數、`nova 跑` 沒有**，
argparse 先失敗（退出碼 2），而我的斷言是「輸出裡沒有『換一顆腦』」——
**剛好成立**。兩支測試當場綠，但一行檢查都沒走到。
實跑一次才發現。

**二、`subprocess` 測試的 coverage 追不到子程序的行。**
改用子程序之後變異閘報 `WRONG_TEST：沒覆蓋 [807]`。
既有的整合測試全都是直接呼叫 `命令列.主程式()` 而不是開子程序——
原本以為那是風格，**其實是因為變異閘追不到**。已寫進測試檔的 docstring。

## 2026-08-31：放寬檢查之後，原本擋在門口的驗收測試會直通真模型

拆掉家族名檢查，`tests/驗收/test_委派給其他llm.py::Test工作流的換腦保證`
那兩支跟著紅。看起來像「守舊行為所以該改」，但真正的問題比那嚴重一級：

那個檔案的 docstring 第 4 行就是全檔前提——**「用假 CLI 跑，所以不燒 token」**。
那兩支是**唯二沒帶 `--執行檔` 的**，之前不燒只因為**門口就把它們擋掉了**。
檢查一放寬，它們就一路跑到真的 `codex exec`，紅出來的訊息是
`[quota-exhausted] Reading additional input from stdin...`。

**綠著燒 token 的測試不會在閘上現形**——這次是撞光額度才露餡。

新版用 `--執行檔 假CLI` 加 `--最多步數 0` 兩層保險：
`range(0)` 是空迴圈，所以一步都不跑就收在護欄（退出碼 4），
斷言「不是退出碼 2、stderr 沒有『換一顆腦』」就證明檢查放行了，
而且旗標哪天接錯也打不到真模型。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 把家族名檢查加回 `_子命令_工作流` | `test_同一家不再擋在門口`（退出碼 2 不是 4） |

同一刀讓 `tests/整合/test_換腦判準走命令列.py::test_命令列也不再用家族名擋同一家`
一起紅，另外兩支（旋鈕還在、本地腦資格）保持綠——證明破壞打在對的位置。

### 第三個坑：子字串斷言撞上**別條規則**的錯誤訊息

那一格的舊名是 ~~`test_審查用是必要參數`~~（刪除線＝**當時**的名字，
不算「這支現在還在」的宣稱，見 `test_標記過的舊測試名不算宣稱`）。
它斷言 `"審查用" in stderr`，宣稱守的是
「省略 `--審查用` 會被 argparse 當場擋」。

**那條保證從來不存在**：`--審查用` 是 `default=None`，不給就走派工表。
它會綠，是因為**另一條規則**（家族名重疊）的錯誤訊息
「…同時出現在 --用 與 **--審查用**」剛好含這三個字。
拆掉那條規則，這支才現形。

子字串斷言的通病：**斷言的字出現在輸出裡，不代表你以為的那條路走過了。**
改成守「`--審查用` 這個旋鈕沒有消失」（比照 `test_工作流有token預算這個旋鈕`），
判準本身的保證留在 `tests/單元/test_換腦判準.py`。

### 順手清掉四處還在宣稱舊規則的文字

`剖析器.py` 的 `--審查用` help、`命令列.py::_子命令_工作流` 的 docstring、
`docs/設計/03-角色與工作流.md` 的已知缺口 1、`docs/路線圖.html` 的節點註。
**互相衝突的兩條比多餘的一條更貴**——尤其那份設計文件同時宣稱
「是必要參數」（從來不是）與「不能跟 --用 同家」（已拆），兩句都錯。

## 2026-08-31：併 main 收線，兩種「看起來很正常」的錯

收 `nova 線` 這條停了一晚的線時，`tests/負控/登記.py` 是共同附加點，
每次併 main 都撞衝突。連踩兩個坑，兩個都不會當場報錯。

### 一、`git merge main` 併到的是**本地** main ref

前一個 PR 剛用 `gh pr merge` 合併掉，那份東西只在遠端。
主 worktree 沒 `git pull`，本地 `main` 就是舊的。

結構化合併按 union 算出「46 刀」，數字完全正常——
比對 `識別="..."` 清單才發現前一個 PR 的 2 刀不見了。
**衝突解得再對，來源錯了就是錯的。**

一律 `git fetch origin main && git merge origin/main`，不要用裸的 `main`。

### 二、驗「相對 origin/main 只有加號」不夠

第二次併對了 main，`git diff origin/main -- tests/負控/登記.py` **只有加號**，
看起來乾淨。但閘紅了六刀 `WRONG_TEST`，其中**四刀是 main 自己的**。

原因：合併把本線分支從**舊 main** 帶來的 `必須覆蓋=frozenset({28})`
這種舊行號登記**加了回去**。`#134` 之後 `必須覆蓋` 由操作推導，
main 上那幾刀早就沒有這個欄位了——多加回來就等於用舊行號覆蓋掉推導。

**錯誤是「多加了不該有的」，不是「少了東西」，所以只看減號驗不出來。**

正解是不要合併那個檔：**基底直接取 `origin/main`，只 append 本線新增的刀**。

```bash
git checkout origin/main -- tests/負控/登記.py
# 然後把本線的刀 append 到元組尾端，不帶 必須覆蓋
```

這樣 `class 變異` 的定義永遠不會被動到——第一次併的時候它就被換成了本線的
舊版（`必須覆蓋: frozenset[int]` 必填），mypy 當場紅。

### 三、`test_線.py` 走 subprocess，coverage 追不到

剩下本線那 2 刀 `WRONG_TEST：沒覆蓋`。原因跟 `#141` 同一個：
`subprocess.run([nova執行檔, ...])` 的行 coverage 記不到。

改成直接呼叫 `命令列.主程式()`（`monkeypatch.setenv`／`chdir` 換掉 env 與 cwd，
`capsys` 收輸出），兩刀當場 KILLED，而且測試從子程序的秒級變成 **0.17 秒**。
建測試資料的 `git` 子程序留著——那不是被測對象。
## 2026-08-30：`nova 收` 自動合併紅線

`_子命令_收` 是會依序呼叫 `git` 與 `gh` 的自動合併節點；新增
`tests/整合/test_收尾紅線.py`，用暫存目錄裡的假執行檔記錄完整 argv，不開 PR、不碰真的遠端。
七條保證各有一支指名測試，基準實作七支全綠。

固定負控登記在 `tests/負控/登記.py`。每一筆都由 runner 先驗證變異前後 SHA256 不同，
清理 `src/` 與 `tests/` 下的 `__pycache__` 後，只跑指名測試；七筆變異都被指定測試打紅，
暫存副本移除後原始碼雜湊仍回到基線 `66d55a8b9cb1b1b6f7ff3d4e1962a655cb8189fc34d15f232fb1fffeb75285a8`。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 往 `git commit` 插入 `--no-verify` | `test_提交指令不准帶繞過驗證旗標` |
| 往 `gh pr merge` 插入 `--admin` | `test_合併指令不准帶管理員旗標` |
| 拿掉 `gh pr merge` 的 `--delete-branch` | `test_合併指令一定帶刪除分支旗標` |
| 把 CI 紅的判斷反過來 | `test_CI紅時不會組出合併指令` |
| 把閘紅的提前返回判斷反過來 | `test_閘紅時後續提交推送開PR都不發生` |
| 把 CI 逾時的結果未知（3）改成閘紅（1） | `test_等不到CI回結果未知三` |
| 在閘紅提前返回前插入寫檔 | `test_閘紅時只跑閘不修改任何檔案` |

七筆負控都在變異副本中退出失敗；原始工作樹沒有 `src/` 實作變更。

## 2026-08-31：燒掉三輪才看見的單點故障，與一個假的備援

排程每 15 分鐘醒來跑 `nova 工作流 --從收件匣`，**兩個腦旗標都不帶**。
連續三輪都是 `aborted`，前三階跑完、燒掉幾十萬 token，死在 review。
一直以為是「當時剛好額度用盡」的偶然。

### 一、不帶旗標時審查走的是推理列，而那一列是單鏈

```
排程 argv：nova 工作流 --從收件匣
    ↓ 不給旗標就問派工表（命令列.py:799 _哪幾家）
_階段的工作[審查] = 工作種類.推理
    ↓
_表[推理] = 派法(腦們=("codex",))      ← 單鏈、無備援
    ↓
codex 額度死到 09-06 → 每一輪照劇本走到 review 才撞牆
```

**單點故障不會顯示成「設定錯了」，會顯示成「工作做到一半沒收尾」。**
帳本上看到的是 aborted 與燒掉的 token，看不到「這條鏈只有一顆」。

### 二、補上備援之後才發現那個備援是假的

推理列指名 `模型=codex高階模型`，而 `模型` 是**角色層級**的，
透過 `呼叫選項` 傳給鏈上每一顆。`轉接.py:352` 自己寫著
「`選項.模型` 原樣傳下去不翻譯——各家的模型命名空間不交集」。

所以 codex 額度耗盡換到 claude 時，claude 會拿到 `--model <sol 的型號>`，
當場 `model-not-found` 再失敗一次。帳面看起來像「兩家都掛了」，
真正的原因（型號傳錯家）一個字都不會出現。

修法：`接力腦.詢問` 換家時清掉型號，其餘旋鈕原樣傳。
既有的 ~~`test_選項原樣傳給每一顆`~~ 斷言整個物件相等，順帶把型號也綁了進去——
那不是它要守的東西，改成 `_型號除外` 並把例外理由寫進 docstring。

### 三、agy 便宜的前提被量掉了

例行列原本 agy 打頭，理由寫在註解裡：「agy 便宜且快」。三格實驗推翻它：

| 條件 | input tokens |
|---|---|
| 空目錄 ＋「回一個字：好」 | 13,720 |
| **nova 目錄** ＋ 同一句 | **13,726**（只差 6 個） |
| nova 目錄 ＋ 真任務 | 375,061 / 410,294 |

第二格是關鍵：工作區從空的換成 2,007,790 字元的 repo，固定成本沒動。
**所以不是 CLI 把工作區打包進 context，是模型自己在工具迴圈裡選擇讀滿。**
一度以為是機械打包，那會推出「只能縮小工作區」的結論，
害「在角色提示教它怎麼找」這條路被判死——實驗證明相反。

claude 同一份工作約 8,600 token，46 倍。例行翻成 claude 打頭，agy 留備援。

量法：`cd <目錄> && agy --model gemini-3.7-flash-high -p "回一個字：好" --output-format json`，
讀 `usage.input_tokens`。旗標是 `--model` 不是 `-m`。

### 四、兩處殘留的過時斷言

`#140` 已裁定家族名重疊不是自寫自評（判準是**對話**不是家族名），
`_工作流前置檢查` 也早就不查重疊了，但兩支測試還在守那條規則：
~~`test_例行與推理不准重疊`~~、~~`test_不給旗標時檢查要拿派工表挑出來的家去比`~~。
它們會把正確的修法判成錯的。換成守現在真正的保證（兩列都要有備援、
兩邊都問得出非空的鏈）。

另外 `test_不給用哪家時例行階段走派工表的例行` 抄了一份「第一順位是 agy」，
跟單元層重複——翻面時要改兩處，漏改的那處會用過時的斷言擋下新策略。
整合層那份刪掉，只留階段對應。

## 2026-08-31：角色提示只講「做什麼」，不講「怎麼找」

`迴圈/角色提示.py` 的四個角色提示（測試員／實作員／重構員／審查員）一個字都沒講
讀取策略，各家 CLI 就各用各的預設。量到的差距：

| 家 | 單階 input token |
|---|---|
| claude（按需 Grep／Read） | ~8,600 |
| agy（讀滿工作目錄） | 375,061 / 410,294 |
| codex（多輪重送） | 826 萬 |

旗標關不小：`codex exec --help` 沒有任何 token／budget／max-turn 旗標，
`agy --help` 只有 `--add-dir`（**加**目錄）。剩下的桿只有提示。

### 一度走錯的岔路

先假設是「CLI 在呼叫前把工作區機械打包進 context」。**若成立，提示寫什麼都沒用**
——模型看到提示時 repo 已經在 context 裡了，只能縮小工作區或不派它做 repo 工作。

三格實驗量掉這個假說：

| 條件 | input tokens |
|---|---|
| 空目錄 ＋「回一個字：好」 | 13,720 |
| **nova 目錄** ＋ 同一句 | **13,726**（只差 6 個） |
| nova 目錄 ＋ 真任務 | 375,061 / 410,294 |

第二格是判別格：工作區從空的換成 2,007,790 字元的 repo，固定成本沒動。
**所以是模型自己在工具迴圈裡選擇讀滿**，提示是有效的桿。

只做第一格會得到相反的結論——空目錄跑短提示掉到千級，可能只是因為提示短、
模型沒去讀，跟工作區大小無關。**分不開的實驗不算實驗。**

### 這條保證的極限

單元測試守的是「那幾段話不會被人順手刪掉」，**不是 token 真的降下來**。
提示是懇求不是保證——模型可以讀了那句話還是把 repo 吃滿。真驗收要量：
重派後看帳本裡 agy 單階 input 對照基線 375k／410k。
兜底的殼是 `契約/工作流.py` 的 `單次最多token`（預設 2,000,000）。

### 變異閘擋住了整類保證

登記那一刀時撞到 `RUN_ERROR：操作沒有可推導的 executable 行`。
**多行字串的內容不是 executable 行，coverage 追不到**——角色提示、說明文字
這類住在模組層字串常數裡的保證，錨點必然落在內容中間，於是整類都不准登記。

`_推導破壞行` 改成：推導不到行且是模組層單一名稱的賦值時回 `frozenset()`
（跟顯式 `必須覆蓋=frozenset()` 同一個語意），否則照舊炸。
**空集合是標記不是後備**——函式內部推導不到行代表錨點打歪了，
靜默放行的話刀會看起來 KILLED 而其實沒破壞到被測的東西。
兩格都由 `tests/負控/test_行號錨點.py` 背書。

## 2026-08-31：兩支永遠紅的測試，跟永遠綠的一樣沒有訊號

`tests/整合/test_本地能力邊界.py` 的四支探針全走 `assert 通過率 == 3/3`。
但 `docs/研究/本地腦能力邊界.md` **早就寫著**「這兩支是能力報告，不是回歸閘」、
「預期各有失敗」——文件說的話沒有程式碼背書，它就只是一句願望。

後果是那兩支每次跑都紅。**一支永遠紅的測試跟一支永遠綠的一樣沒有訊號**：
端點掛了、契約改了、回應解析不出來，你分不出來，因為它本來就是紅的。

修法：`_量測` 拆成兩個。`_量測` 是回歸閘（已量到穩定，退步就紅）；
`_報告` 是能力報告——判定改成「通道活著、三次都拿得到成功回應」，
通過率只寫進 stderr。**能力不足不紅，通訊壞掉才紅。**

負控：把 `_問` 的端點指到死 port，那兩支當場紅（通道判定生效，不是能力判定）。

### 順帶量到的：n=3 對這種方差量不出東西

同一支探針、同一個端點、同一顆模型：

| 能力 | 08-30 | 08-31 第一次 | 08-31 第二次 |
|---|---:|---:|---:|
| 長提示不漏中段指令 | 2/3 | **0/3** | **3/3** |
| 拒絕答不出的事實 | 1/3 | 2/3 | 1/3 |

長提示那列跨越整個值域。用它當回歸閘會隨機紅，用它當「已知邊界」也站不住——
要判定這條能力，樣本數得先拉到能分辨「模型不行」與「這三次剛好不行」。
拒答那列三輪都在 1/3～2/3，一致地不可靠，結論比長提示硬。

## 2026-08-31：一張前提不成立的票，跟它揭露的真缺口

票 05 說「`nova 帳本` 摘要漏算 `unknown` 收場，實際 862,500 卻印 414,986，
低估 52%」。**查下去發現沒有這個 bug。**

```
agy  2 次（成功 1 / 失敗 0 / 未知 1） · 785355→77145 token
總計 862500 token          ← 現在算得完全正確
```

`_記一次呼叫` 從頭到尾沒有 outcome 過濾。而 `帳本讀取.py` 最後一次改動是
08-30 09:40（#103），那輪帳本檔案 08-31 04:37 才寫完 impl 那筆——
**我當時是在那一筆還沒落盤的時候量的**，看到的自然只有第一筆。

「回報缺陷前先問『我的量法會不會就是成因』」——這次又是。

### 但票不是白開的：那條保證真的沒人守

grep 過整個 `tests/`：**沒有任何測試守「每一種收場的 token 都要算進總計」**。
現在是對的，但有人加一個 `if outcome == "success"` 就靜悄悄壞掉，
而壞掉的樣子是「帳單比實際便宜」——沒有人會來抱怨。

補了四支，其中一支是**現況記錄**：CLI 被 kill 前沒回報 token 時，收斂端當 0，
所以分不出「花了 0」與「不知道花多少」。會低估不會高估。要分開得在契約加
「未知」狀態，那還沒有呼叫端要求——先把現況釘住，改動時那支會紅。

負控：在 `_記一次呼叫` 加 `and 事.get("outcome") == "success"`，
兩支當場紅（已登記進變異閘，77 刀全 KILLED）。

## 2026-08-31：對等實驗——讀取策略讓 agy 降 36%，但沒把它變成 claude

#150 把讀取策略寫進四個角色提示之後，量了一組**對等**實驗：
同一個 11 行題目、同一個工作目錄（nova repo）、同一顆模型
（`gemini-3.7-flash-high`）、同一階（測試員），**兩邊只差角色提示**。

| 角色提示 | input tokens |
|---|---:|
| 舊（沒講讀取策略，`2d705af`） | 264,537 |
| 新（#150） | **168,561** |
| | **降 36.3%** |

### 三件要一起講的事

1. **提示是有效的桿，但不是量級的桿。** 168,561 仍是 claude（~8,600）的
   **19.6 倍**。例行維持 claude 打頭是對的——#149 那個決定沒有被這個結果推翻。

2. **n=1，幅度不可信。** 同一天量的本地 9B 探針，同一條能力三輪是 2/3、0/3、3/3，
   方差跨越整個值域。這個 36% 是**方向明確、幅度不可信**。要拿它做決策得先加樣本。

3. **第一次量的數字不能用。** 先跑的是「#150 之後 168,561 vs 基線 375,061／410,294」，
   看起來降 55%——但基線那兩筆跑的是票 01/02/03 的題目，這次是 11 行小題目。
   **題目不同，讀的量本來就不同**，那是蘋果比橘子。對照組（同題目跑舊提示）
   才把提示的效果分離出來。

### 順帶：護欄擋下了我自己踩的坑

第一次跑量測時把題目用 `"$(cat 題目.md)"` 塞回 argv，被 nova 擋下：

```
這段提示有換行，看起來是組出來的而不是打出來的，不准走 argv。
```

exit 0、沒有燒任何 token。`--提示檔` 才是那條路。
**我的記憶裡就有這條，還是踩了**——護欄比記憶可靠。
## 2026-08-31：「claude 比較便宜」這個結論，從來沒有被真的量過

整晚拿 `input_tokens` 比較三家，claude 一直遠低於 agy。追下去發現：
**claude 把大部分 input 算在快取欄位，而 nova 從來沒把那一欄落盤。**

```
claude 空目錄跑一句話：
  input_tokens = 2        ← nova 記的是這個
  cache_read   = 14,206   ← 真正讀進去的量在這裡
  cache_create = 13,851

固定成本 ~14,000，跟 agy 的 13,720 幾乎一樣，只是算在別的欄位。
```

同一個題目跑一階：agy `input=168,561`，claude `input=12` 而 **成本 US$0.2152**。
花了 21 美分，帳本記 12 個 token。

### 斷在哪

```
CLI 回報 cache_read_input_tokens
    ↓ 解析.py:180 讀到了
回應.用量.快取讀取token（契約有這個欄位）
    ↓ 接力.py:144 還特地處理了它
記帳.py:116-118 只填 輸入token／輸出token／成本美金
    ↓  ← 斷在這裡
契約/帳本.py 的 `事件` 沒有這個欄位、`欄位對應` 表也沒有
    ↓
帳本事件沒有它 → 總計不含它 → claude 看起來幾乎免費
```

**後果不是統計失真，是派工決策建立在一個對 claude 系統性歸零的量上。**
#149 把例行翻成 claude 打頭時，用的就是這個數字。方向可能仍然對
（claude 的 21 美分 vs agy 燒的額度不是同一種資源），但**當時的理由不成立**。

### 修完之後

```
修之前：claude  2→3 token                    總計 5
修之後：claude  2→3 token（＋快取讀取 2702）  總計 2707     ← 541 倍
```

順帶量到：這次快取只有 2,702，手動跑 `claude -p` 是 14,206——
**差的 1.1 萬是 nova 的 `隔離設定` 擋掉的 `~/.claude/CLAUDE.md`**。隔離真的在省錢。

負控：把總計改回 `輸入token + 輸出token`（不加快取），
`test_快取讀取的token算進總計` 當場紅（已登記進變異閘）。

## 2026-08-31：本地腦的風險不在 usage，在準確度與資源控管

工具迴圈接上之後，我照雲端那套做了 usage 加總——使用者當場指出方向錯了：
**本地跑不燒 API 額度**，token 記得再準也沒有帳單要對。真正的風險是
「它做得對不對」與「它會不會把機器吃掉」，而那兩件事都不會有帳單來提醒你。

盤點下來漏了三格：

| 缺口 | 後果 |
|---|---|
| 單次 `write_file` 沒有大小上限 | 模型可以寫一個 100MB 的檔案 |
| 一輪能寫幾個檔沒有上限 | 回合上限內可以把整個工作目錄覆蓋掉 |
| 「實作員不准改測試檔」只寫在提示裡 | 那是懇求——測試是驗收機制，讓實作階段改得到它等於自己發及格證 |

第三格最關鍵，而且它正好是這個 repo 的核心判準：規則寫在 prompt 裡是虛線的
懇求，寫在包住執行者的程式碼裡才是實線的包圍環。工具箱直接不給那把刀，
模型**不用知道、不用記得、也違反不了**。

**擋的是寫不是讀**——實作員得看測試在斷言什麼才寫得出實作。

三格負控（都已登記，變異閘 84 刀）：
- 拿掉單檔寫入上限 → `test_單次寫入的內容有大小上限` 紅
- 拿掉單輪寫檔上限 → `test_一輪最多寫幾個檔案` 紅
- 「不准寫」改成字串前綴比對 → `test_同前綴的兄弟目錄不受影響` 紅
  （`tests` 擋住時 `tests-資料` 不該跟著被擋——比路徑節點，不比字元）

## 快取建立的 token 從來沒被記到（#160）

`cache_creation_input_tokens` 是**真的付費的 input，按 1.25× 計價**，而且不是
快取讀取的子集——實錄 `claude_ok2.json` 兩欄同時有值（建立 8,094、讀取 8,570）。

`claude_ok.json` 更極端：**輸入 10、快取讀取 0、快取建立 16,668**。
少了這一欄，那次呼叫在帳本裡只剩 10——**99.94% 的量憑空消失**，
而 2026-08-31 之前所有「哪一家比較貴」的比較都建立在那個數字上。

兩把新刀（變異閘 86 → 88 刀）：
- 總計拿掉 `+ 家.快取建立token` → `test_進總token` 紅
- claude 解析改成 `快取建立token=None` → `test_claude的快取建立要記下來` 紅

順手修好一把被自己改壞的刀：`總計不算快取讀取token` 的錨點原本寫死整行
`總token=sum(...)`，這次把總計抽成 `_一家花掉多少()` 之後那行就不存在了。
**錨點寫死整行，重構時會靜默失效**——改成只錨住加總的那個運算式。
## 角色併接有兩份實作，而且沒有東西把它們釘在一起（#161）

`命令列.py` 的 `nova 重構` 那條路自己抄了一份分隔符字面量，
跟 `載體/角色.py` 的 `組提示()` 是**同一個知識兩份實作**。
兩份剛好長得一樣，所以沒有任何測試會紅——改了 `_分隔`，
`nova 問` 那條路會跟著變、`nova 重構` 這條不會，兩條路送給模型的東西
從此不一樣，而且沒人會發現。

**雙向負控**（兩邊都跑過）：把 `角色.py` 的 `_分隔` 改成 `===負控===`——

| 狀態 | 結果 | 證明了什麼 |
|---|---|---|
| 併接還是兩份 | **紅** | 這支測試守得住分歧 |
| 改成共用同一份 | **綠** | 命令列真的走了 `組提示` |

登記的刀（變異閘 88 → 89 刀，實測 1.21 秒）：
- `提示=組提示(角色, 這次.提示)` → `提示=f"{角色}" + 這次.提示`
  → `test_重構送出去的提示等於組提示產出的` 紅

**測試不准開子程序**：coverage 追不到子程序的行，變異閘會判 `WRONG_TEST`。
`#141` 踩過一次，這票又踩一次——改成直接呼叫 `主程式` 才過。
## 「規則被上下文淹掉」這個診斷是錯的——帳本當場否證（2026-08-31）

本地 27B 跑滿 8 回合不收尾，我原本的診斷是「回合 3 之後全在 grep 已經讀進來的
東西」，推論是角色提示被淹掉、該把規則搬到 system 層。**帳本裡早就有數字，
去算就好，零改動**（`工具名稱`／`工具參數摘要`／`工具回合`／`工具成功` 從 #157 起就在）。

算出來的重複讀取比例：

| run | 工具呼叫 | 相異 | 完全重複 |
|---|---|---|---|
| `20260831T031448Z` | 14 | 13 | 1（7.1%） |
| `20260831T035701Z` | 14 | 14 | **0（0.0%）** |

**幾乎沒有重複。** 診斷垮掉。但真相更糟，看序列就知道：

```
回合 1-4   讀完了該讀的四個檔案（該收尾了）
回合 5-8   開始 grep 自己幻想出來的名字
           run1: "_階段的派法(階段: 階段代碼)"   帶完整簽章去 grep，形狀猜錯
           run2: "工作種類\." → "工作種類."      跳脫符號試兩次
           run2: "def _階段的工作種類"            這函式不存在（它叫 _階段的派法）
```

兩個 run 加起來 28 次工具呼叫，**`write_file` 零次**。

**病是「不知道該收尾了」，不是「上下文被規則淹掉」。** 兩者的修法完全不同：
前者要的是收尾壓力（回合倒數、後段只留寫入類工具），後者才是搬 system 層。
搬 system 層對這個病一點用都沒有——而 codex 的 A/B 另外證明搬過去還會更差
（見 `docs/設計/02` 的專節）。

**教訓**：帳本裡已經有的欄位，先去算，再去改東西。這次的順序差點反過來。

## 回合快用完了要催收尾（#163）

帳本挖出來的病（見上一節）：兩個 run 共 28 次工具呼叫、`write_file` **零次**，
全部撞上 8 回合上限。回合 1-4 就讀完該讀的了，5-8 在 grep 自己幻想出來的函式名。

**催收尾是迴圈的責任，不是提示的責任**——提示裡的是懇求，包住執行者的
程式碼裡才是保證。剩 3 回合起，每輪在工具結果後面塞一則倒數提醒；
**已經寫過檔就不催**（它在做正事，催是噪音）。

兩把刀（變異閘 89 → 91 刀，各約 2.6 秒）：
- `if 寫過檔 or 剩 > 剩幾回合開始催 or 剩 <= 0:` → `if True:`（永遠不催）
  → `test_回合快用完時要把剩幾回合講出來` 紅
- 拿掉 `寫過檔 or`（寫著檔也催）→ `test_已經寫過檔就不催` 紅

## 催了還不寫，就把找東西的工具收掉（#164）

`#163` 的倒數提醒是**懇求**——模型可以不理。這一票補上**保證**：
最後 2 回合把 `grep` 與 `read_file` 從 tools 陣列拿掉，模型連發出那個呼叫的
形狀都沒有。

**收掉的是找東西，不是做事情。** `write_file` 留著，收光了它連收尾都做不到。
唯讀角色這時會拿到空陣列，那是對的：唯讀的產出本來就是文字報告不是檔案。

分兩票做（`#163` 倒數、`#164` 收工具）而不是一票，是為了**退步時分得出來是哪個造成的**。

兩把刀（變異閘 91 → 93 刀，各約 2.6 秒）：
- `只留寫入 = 回合 > 最多工具回合 - 收尾幾回合只准寫` → `只留寫入 = False`
  → `test_最後兩回合不給找東西的工具` 紅
- 寫入工具也跟著收掉（`and not 只留寫入`）→ `test_最後兩回合還留著寫入` 紅

## #163／#164 的端到端驗收：真的收尾了，也真的寫檔案了（2026-08-31）

兩票合併後跑真的 27B-8bit，兩個題目：

**題目一（原本會 abort 的那題）**

| | 工具呼叫 | 回合 | 收場 |
|---|---|---|---|
| 之前 | 14 次 | 撞滿 8 | **aborted**（回合 5-8 grep 幻想出來的函式名） |
| 之後 | 10 次 | 回合 5 收尾 | **success** |

模型的結論是「這支測試已經存在，重寫會是綠的，不滿足紅測試的驗收」——
**那個判斷是對的**（題目過時了，那支測試 `#152` 就進去了）。

**題目二（真的缺檔案的題目，tmp 工作區）**

```
回合 1-5   探索（找 pytest 設定，全是 ✗ 因為檔案不存在）
回合 5     剩 3 回合 → 倒數提醒觸發
回合 6     write_file  ← 催完立刻收斂
收場       success
```

產出的測試檔（那個 tmp 工作區的 `單元/test_計算` — **不在這個 repo 裡**）
跑起來**真的是紅的**（`assert 4 == 5`），
正是題目要求的。模型還自己處理了 `sys.path`（那個工作區沒有 pytest 設定）。

**倒數提醒直接命中**：兩題都是催完的下一回合就收斂。
## 路線圖有五格說「沒做」，其實早就做完了（#167）

使用者照路線圖圈了三件要推進的，**其中兩件早就做完了**——
節點契約（8 支測試）、角色工廠（3 支）。再查下去發現總共五格過期：

| 節點 | 圖上說 | 實況 |
|---|---|---|
| 節點契約 | 無 | `契約/節點.py` ＋ 8 支測試 |
| 扇出 | 無 | `迴圈/扇出.py` ＋ **11 支測試** |
| 系統架構師 | 無 | `契約/架構決策.py` ＋ 8 支測試 |
| PlanWorker | 無 | `迴圈/計畫.py` ＋ 13 支（`test_分派單.py`） |
| 角色工廠 | 無 | `迴圈/角色工廠.py` ＋ 3 支測試 |

**爛掉的路線圖不是沒有訊號，是錯的訊號。**

## 為什麼現有的兩支守不住

```
test_標成已完成的檔案必須真的存在   態=好 → 檔要存在      守「好」那邊
test_未做的格子不准宣稱檔案         態=無 → 檔要空        守「無」不准列檔
                                    ↑
                          「無」的格子檔是空的，而空是合法的
                          「這格其實早就做完了」從兩支中間掉出去
```

補的那一支讓「無」也宣告證據：`預期` 欄位說「如果做了會長在哪」，
那些路徑一個都不准存在。做了就得改態，改態就得把路徑搬到 `檔`。

負控（變異閘 93 → 94 刀）：把 sdk 那格的 `預期` 從一個**還不存在的**
模型轉接器路徑，換成 `src/nova/載體/模型/本地.py`（存在）
→ `test_未做的格子不准是其實已經做完的` 紅。

（那個不存在的路徑這裡不寫全——寫了會被 `test_文件提到的檔案都真的存在` 抓，
**而且我已經被抓過兩次**：第一次是 E2E 產出的 `單元/test_計算`，第二次是這個。）

## 順手修好變異 runner 的兩個限制

那把刀第一次跑不起來，暴露出 runner 只服務 Python 目標檔：

1. `_複製專案` 只複製 `src`／`tests`——文件類的刀在基線階段就 `FileNotFoundError`，
   而那看起來像 runner 壞了。`docs` 只有 792K（比 `src` 的 1.5M 還小），直接加進去。
2. `_覆蓋率前置` 用 `ast.parse` 推導可執行行——HTML 當場 `SyntaxError`。
   非 `.py` 目標檔跳過那一關，**代價寫在 docstring 裡**：資料檔的刀分不出
   `KILLED` 與 `WRONG_TEST`，所以要挑「唯一會讓那支測試紅」的破壞。

## 同一個錯抓了兩次，所以把文件即事實搬進提交閘（#167）

`tests/驗收/test_文件即事實.py` 守「文件宣稱存在的檔案要真的存在」，
但它住**驗收層**——`nova 閘 提交` 的 `pytest-unit` 只跑 `tests/單元`，跑不到它。

結果是：改完文件、提交閘全綠、推上去 CI 才紅，而且 CI 的輸出把失敗詳情
截成一排進度點，每次都得本地重跑一遍才知道紅在哪。

2026-08-31 一天內中了兩次：
1. E2E 產出的測試檔路徑（那個檔在 tmp 工作區，不在 repo 裡）
2. 路線圖用來指「這個還不存在」的那個路徑

**兩次就是機械判準抓得到的證據**——那就該進閘，不是靠人記得。
實測 8 支 0.06 秒，提交閘從 7 條變 8 條、總時間 5.96 秒，還在 10 秒預算內。

負控（變異閘 94 → 95 刀）：把 `docs-facts` 的閘點從 `提交` 改成 `ci`
→ `test_文件即事實要在提交閘` 紅。

## 重構閘的變更範圍：agy 落了一半，缺的那半是最貴的假綠（#167）

派 agy 跑「重構實線閘補兩格」，它在 **guardrail（碼 4）** 停下——
6 階、43 萬 token、US$0.98。碼 4 是停止規則按設計生效，不是失敗。

**它做好的**：`跑出範圍了嗎`、`_在範圍內嗎`（比路徑節點不比字元前綴）、
一支寫得很清楚的單元測試。品質可以。

**它沒做的，而且那一格最貴**：

```
拍快照 = 拍測試快照     # 只掃 tests/ 底下
                ↑
      範圍護欄拿到的快照裡根本沒有 src/ 的檔
      差集永遠是空的 → 護欄永遠放行
```

測試綠、閘綠、護欄函式在——**但它守不到任何東西**。這是 CLAUDE.md
說的「用規避換來的綠是假綠」的又一個現場，而且比 `__all__` 那次更難發現：
那次是名字沒進去，這次是**護欄看的地方不對**。

補上 `拍全樹快照`（跳過 `.git`／`.venv`／`__pycache__` 等 12 個目錄，
不是為了乾淨是為了跑得完），並接到命令列：**給了 `--範圍` 才拍全樹**，
沒給就維持原本的輕量快照，既有呼叫端一個都不壞。

三把刀（變異閘 95 → 98 刀）：
- `_在範圍內嗎` 改成字元前綴比對 → `test_範圍內放行而同前綴的兄弟目錄要被抓出來` 紅
- `拍 = 拍全樹快照 if 參數.範圍 else 拍快照` → `拍 = 拍快照` → `test_動到範圍外要回護欄碼` 紅
- `拍全樹快照` 不排除垃圾目錄 → `test_不拍垃圾目錄` 紅

**第二格（重構完再驗一次判準綠）還缺著。**

## 成果帳補兩欄：這次爛掉是不是因為規則變了、該退回哪（2026-08-31）

外部校準（`docs/設計/08-外面怎麼講載體工程.md`）指出的缺口：`成果` 落盤十欄，
答得出「做完了沒、花了多少」，答不出**「當時規則表是哪一版」**與**「該退回哪個 commit」**。
沒有這兩欄，一次壞掉的執行只剩「它壞了」這個結論——沒有可歸因的變數，也沒有可回去的點。
而規則表天天在改。

兩個新來源都刻意不走 git 的 commit：

- `policy_version` 走**規則表檔的內容雜湊**。閘是照工作區那份跑的，不是照 HEAD 那份跑的；
  改了還沒提交就跑一次的話，走 git 會給出上一版的答案——**那比沒有答案更糟，
  因為它看起來像個答案。**
- `rollback_point` 是**起跑前**拍的 HEAD，不是跑完之後。工作流跑完 HEAD 可能已經被它自己推走，
  那時候拍到的是終點，而這一欄的用途正是「要退回去的話退到哪」。工作目錄不是 repo 時
  這一欄**不落盤**（不是空字串）——「這個問題不成立」跟「有答案但沒記到」是兩件事。

登記兩把刀（變異閘 88 → 90 刀，兩把都 KILLED）：

| 破壞什麼 | 該紅的測試 |
|---|---|
| `成果轉字典` 過濾掉 `policy_version` | `tests/單元/test_成果.py::test_帳上答得出當時的規則表與該退回哪個commit` |
| `版本()` 回固定字串不回內容雜湊 | `tests/單元/test_規則表.py::test_版本跟著規則表的內容變` |

**第一次的刀砍歪了，記下來免得下次再砍一次**：原本砍在 `_欄位對照` 那一列，
runner 判 `WRONG_TEST` 而不是 `KILLED`——那是**模組層級的資料**，import 時就跑完了，
per-test 的 coverage context 抓不到那一行。刀要砍在測試執行時真的會跑到的行。

接線那兩支手做（**不進自動登記**，理由同上一段：它們跑子程序，coverage 追不到
`_歸檔成果` 那幾行）：

| 破壞什麼 | 該紅的測試 |
|---|---|
| `起點commit=起點` 改成 `起點commit=None` | `tests/驗收/test_成果帳本.py::test_帳上答得出當時規則表哪一版與該退回哪個commit` |
| `規則表版本=規則表版本()` 改成 `規則表版本=None` | 同上，外加 `test_工作目錄不是repo時那一欄不落盤` |

兩次都先斷言破壞真的生效（錨點恰好出現一次）再跑，還原後回綠。

## 扇出擋假獨立：沒有資料依賴不等於獨立（2026-08-31）

[`docs/設計/09-接下來往哪走.md`](設計/09-接下來往哪走.md) 的第 1 階。

兩個分支的輸入互不相干、屏障也收得齊，但它們都寫同一個地方的話，
先寫的會被後寫的蓋掉——**而蓋掉的樣子跟兩個都成功一模一樣**：
分支結果全綠、缺口是空的、整階終局是成功。這種邊在提示裡看不見
（兩邊的任務描述互不提及），只能靠呼叫端宣告的寫入範圍機械地比。

**在派送前拒絕，不是跑到一半才護欄**：跟「分支數超過上限」同一類，
計畫本身就不成立，派出去只是白花兩個分支的錢。

登記兩把刀（變異閘 90 → 92 刀，兩把都 KILLED）：

| 破壞什麼 | 該紅的測試 |
|---|---|
| `_撞在一起嗎` 永遠回 `False` | `tests/單元/test_扇出.py::test_兩個分支寫同一個地方就不是獨立的` |
| `_撞在一起嗎` 改成比字元前綴 | `tests/單元/test_扇出.py::test_同前綴的兄弟目錄不算相交` |

第二把守的是那個坑本身：比字元前綴的話 `src/nova/契約` 與 `src/nova/契約-舊`
會被誤判成同一塊地，這一格就從護欄變成擋路的，而第一個反應會是把它關掉。
同一個坑在 [`src/nova/載體/重構護欄.py`](../src/nova/載體/重構護欄.py) 的範圍比對上出現過一次。

**沒宣告寫入範圍的分支不檢查**（跟重構的 `--範圍` 同一條：不給就不檢查）。
代價講清楚：沒宣告的分支撞在一起時這裡攔不到。擋下沒宣告的會讓這一格
變成沒人能用的閘，而那種閘的下場是被關掉。

## 自主派工必須帶機械驗收：沒有驗收就是沒有停止條件（2026-08-31）

`契約/分派.py` 早就有 `缺驗收條件` 這個拒絕代碼、`迴圈/計畫.py` 也有驗證器、
13 支測試全綠——**但整套零呼叫端**。按 CLAUDE.md 的判準，
沒有接上任何一條路的驗收機制等於不存在。這一格是把它接上的第一條路。

判準是**誰在現場**，不是任務難不難：

| 來源 | 帶不帶驗收 | 為什麼 |
|---|---|---|
| `typed`（你敲）／`file`（cp 進去） | 不必 | 人在現場，驗收是他自己看 |
| `schedule`（排程）／`mcp`（協定） | **必須** | 做完之後沒有人會看，下一輪直接接著做 |

**放行那半是刻意的。** 逼人在現場的票也寫驗收指令，他只會改走別條路進來，
於是這道閘等於不存在。

閘紅票的驗收不必發明：**紅的是哪個閘，驗收就是那個閘**。

| 破壞什麼 | 該紅的測試 |
|---|---|
| `丟一件` 的驗收檢查改成 `if False:` | `tests/單元/test_驗收才准派.py::Test丟一件擋下沒驗收的自主票::test_自主票沒帶驗收不准派` |
| 擋下來之前先把檔寫進收件匣 | 同檔 `::test_被擋下的票不准留在收件匣` |
| `_自主來源` 擴成全部四種來源 | 同檔 `::test_人給的票不帶驗收照樣派` |
| `讀出驗收` 不濾掉 strip 後是空的那些 | 同檔 `Test讀出驗收::test_空白的驗收不算驗收` |
| 閘紅票的驗收寫死 `提交` | `tests/單元/test_閘紅成票.py::test_閘紅票的驗收指向紅掉的那個閘` |

**第五把第一次是 SURVIVED 的**，因為那支測試只測 `閘點="提交"` 一格——
寫死成「提交」的實作照樣綠。改成兩個閘點都測才 KILLED。
教訓跟 [`負控腳本要斷言我真的改到了`] 同一條：**刀砍在只有一個值的地方等於沒砍**，
而「資料放在不會出錯那行」正是五種假綠之一。

## 跨程序閘鎖：三條 nova 同時跑閘會互相拖出假紅（2026-08-31）

`規則表.py` 的「一次只跑一條」管的是**一個閘內部**的規則順序。
三個 nova 各自開一個閘，那三份 pytest 同時吃滿 CPU，誰也不知道誰在跑
（平行測試設定吃 3/4 核心，兩條就超過機器容量）。

代價不是慢，是**假紅**：`tests/負控/登記.py` 每把刀的 `最多秒` 是 2.0，
執行器拿它當 `subprocess.timeout`。機器超載時刀跑不完被殺，
判成「這把刀沒被殺掉」——一支好好的測試被報成壞的，
而下一步通常是有人去把它「修好」。

**鎖是機器的，不是專案的。** 每個 git worktree 被 nova 當成不同專案，
鎖檔跟著專案走的話三個 worktree 各拿各的鎖——三把鎖，零保護。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 鎖檔路徑改成掛在 `專案/` 底下 | `tests/整合/test_閘鎖.py::test_鎖檔路徑不含專案識別` |

**另外兩把刀登記不成，而失敗本身是這次最值錢的產出：**

1. 「閘佔不到鎖時當成閘紅」砍在 `命令列.py` 的 `except 佔不到`，判 **WRONG_TEST**
   ——那支測試走 subprocess 跑 `python -m nova`，而 **coverage 追不到子程序的行**
   （CLAUDE.md 記過的坑，這是第二次踩）。端到端測試留著，但它進不了變異閘。
2. 「閘鎖用完不放掉」判 **SURVIVED**：把 `LOCK_UN` 與 `handle.close()`
   **兩行都拿掉**，`test_放掉之後別人拿得到` 照樣綠——CPython 的 refcount
   在 `佔住` 收尾時就關了檔案，而關 fd 就是放鎖。
   **那兩行原本看起來像保證，其實是裝飾。** `LOCK_UN` 已刪，
   `handle.close()` 留著是為了不依賴 CPython 的實作細節，理由寫進測試 docstring。

第 2 條是「沒有測試背書的程式碼是裝飾」最乾淨的一個實例：
它不只沒用，還會讓人以為保證住在那裡。

## 收尾時真的去跑驗收：把「模型說完成了」換成「驗收說了算」（2026-08-31）

`#172` 讓自主票必須**宣告**驗收，`#175` 讓驗收指令**跑得動**，
但沒有人在收尾時去跑它——所以到那一格為止，收件匣迴圈的停止條件仍然是
「工作流回 0」，而那個 0 的意思是**模型沒有摔**，不是**事情做對了**。

**驗收紅回 4（護欄）不回 1**：4 的定義本來就含「重構改壞行為」那一類，
而且回 4 之後不必新增任何流程——`_也許接著排` 已經在看 `碼 == 護欄碼`，
`最多輪次 = 3` 已經是那個重試迴圈的停止規則。

| 破壞什麼 | 該紅的測試 |
|---|---|
| `_跑一件` 收尾時不呼叫 `_驗收說了算` | `tests/整合/test_收尾跑驗收.py::test_端到端_驗收紅的票會退回收件匣而不是進已處理` |
| 驗收紅回 `1` 不回 `護欄碼` | 同檔 `::test_驗收紅的票不准當成做完` |
| 碼非 0 時照樣跑驗收 | 同檔 `::test_工作流本來就沒成功時不跑驗收` |
| 沒宣告驗收就判它沒做完 | 同檔 `::test_沒宣告驗收的票不受影響` |

**第一把是接線本身。** 沒有它，`_驗收說了算` 的五支測試全綠，
但把 `_跑一件` 裡呼叫它那一行拿掉也照樣全綠——保證只活在「我看過 code」上。
那支端到端測試繞了一圈才成立：假 CLI 只吐實錄不寫檔，TDD 前六階都會被
「工作區沒被動過」判成結果未知（**護欄在正確工作**，只是擋在要測的東西前面），
改從 `--起點 review` 進去才通，因為收尾那一段是所有起點共用的。

**第三把第一次是 SURVIVED，而失敗揭露了測試設計錯誤與一個更嚴重的風險：**
原本那支測試用**綠**驗收，但綠的驗收會回原碼，變異前後一模一樣——測不出
「有沒有真的去跑」。換成永遠紅的驗收才 KILLED。
而真正的危險比原本寫的更重：不是「多燒一次時間」，是
**`3`（結果未知）被驗收紅改寫成 `4`（護欄）**，於是 `_也許接著排` 自動排票，
把可能已經做過的事再做一次——3 的整條語意就是為了擋這件事。

## 一天二十二個 PR 之後：紀錄該記什麼、不該記什麼（2026-08-31）

這一天合併了二十二個 PR，其中十五個是 nova 自己跑完整 TDD 七階做的，
每一個都有登記的變異刀。**但這份紀錄沒有逐條抄它們，那是刻意的。**

`#183` 之後刀按主題分檔住在 `tests/負控/登記們/`，每個檔的 docstring
就寫著「這幾把守什麼、為什麼」。**可執行的登記比散文更硬**，
抄一份到這裡只會多一個會跟程式碼對不上的地方（CLAUDE.md：規則只寫一份）。

所以這份紀錄從今天起只記**進不了登記的那些**——失敗的刀，以及它們揭露的東西。

### 三次 WRONG_TEST，同一個模式

變異閘判 WRONG_TEST 的意思是「該紅的測試沒覆蓋被改的那幾行」。
今天踩了三次，每次原因不同但形狀一樣：**我以為那行改了測試會紅，
而那行根本沒被那支測試執行**。

| 砍在哪 | 為什麼測試碰不到 |
|---|---|
| 模組層級的 `_欄位對照` | import 時就跑完了，per-test coverage 追不到 |
| `subprocess` 跑的 CLI | coverage 追不到子程序 |
| 圈禁後面的 `if not 目標.is_dir()` | 圈禁擋住時先 raise，那三支測試走不到 |

**變異閘驗證的不只是「測試會紅」，還有「測試真的在看那段程式碼」。**
第二項才是它比一般測試多出來的東西，而三次都是它當場抓到的。

### 兩把刀登記不成，而失敗本身是產出

1. **「閘鎖用完不放掉」判 SURVIVED**：把 `LOCK_UN` 與 `handle.close()`
   **兩行都拿掉**，測試照樣綠——CPython 的 refcount 在收尾時就關了檔案，
   而關 fd 就是放鎖。那兩行原本看起來像保證，其實是裝飾。`LOCK_UN` 已刪。
2. **「閘佔不到鎖時當成閘紅」判 WRONG_TEST**：那支測試走 subprocess 跑 CLI。
   端到端測試留著，但它進不了變異閘——**這種測試的保證等級比登記過的低一階**，
   要知道自己拿的是哪一種。

### 新機制上線的第一次實戰都會暴露自己的邊界

三個例子，共同點是**機制沒錯，錯的是分類**：

| 機制 | 第一次實戰暴露的邊界 |
|---|---|
| `#182` 證據截斷「留開頭」 | pytest 的 FAILURES 在進度點**之後**，留開頭留到的是進度點 |
| `#183` 登記們收集 | `__init__.py` 沒有 `登記` 屬性，fail-closed 當場炸——它是 package 標記不是登記模組 |
| `_印結果` 的 `[:20]` | 前 20 行全是進度點，**所有閘紅都查不出原因**，被咬四次才發現 |

第三個最貴：它讓「CI 為什麼紅」變成只能重跑碰運氣，而那正是這個 repo
最想擋掉的那種看不見。

## 2026-08-31：§7 診斷表對齊規格

這一格的保證只有一條：程式碼裡的九列跟 `docs/AGENT_ARCHITECTURE.md` §7
**同一份**。所以負控要破壞的是程式碼那一份（規格是硬規則一，一個字都不准改），
問的是「抄錯一個字，測試會不會當場紅」。

| 破壞什麼 | 該紅的測試 |
|---|---|
| 把「中斷後工作消失」那列的修復方向從 `狀態持久化與檢查點邊界` 砍成 `狀態持久化` | `tests/驗收/test_診斷表對齊規格.py::test_診斷表逐列對得上規格第七節` |

親眼看到紅，而且訊息直接指名是哪一列：
`At index 6 diff: ('中斷後工作消失', ('Harness', 'Graph'), '狀態持久化') != (..., '狀態持久化與檢查點邊界')`。
還原後三支全綠。

**為什麼只破壞一格就夠**：測試比的是整串 tuple 相等，不是逐欄各寫一條斷言，
所以任一列任一欄漂掉都走同一條斷言。這裡要驗的不是「九列各有一道防護」，
而是「這份抄寫有沒有跟來源綁著」——綁著的證據就是改一個字會紅。
真正會腐爛的情況是規格自己被上游改了，那時測試讀到的新原文對不上舊抄寫，
一樣是這條斷言接住。
## 「親眼看到它紅」在七階裡是假的：兩個機制、同一個後果（2026-09-01）

兩把刀都登記進 `tests/負控/登記們/動測試護欄.py`，這裡只記**為什麼是兩把不是一把**
——同一句「親眼看到它紅」在自主七階裡從兩個獨立的洞漏光，而它是最高原則第一條。

| 拔掉哪條接線 | 該紅的那支 | 親眼看到 |
|---|---|---|
| 階段邊界的快照比對（`_動了測試就收護欄`） | `tests/整合/test_階段邊界擋動測試.py` | 5 failed（測試員放行那支照樣綠） |
| 驗證紅改回整套 suite 非零退出（`_只驗這輪新寫的測試`） | `tests/整合/test_驗證紅只驗新測試.py` | 4 failed |

**兩把刀砍的都是「接線」，不是護欄本身。** `載體/重構護欄.py` 寫好、有測試、有負控刀，
卻只有 `nova 重構` 這個獨立子命令在用；沒有人在場的那條（收件匣 → 七階）一格都沒接。
所以這裡要守的不是「判準寫對了嗎」，是「產線真的呼叫它嗎」——
第一把拔掉之後那個模組的每一支測試仍然全綠，正是這種洞的長相。

第二把的不對稱值得記：測試員順手寫實作反而會被 `驗證紅` 間接擋住（測試變綠→退回），
唯獨「動測試讓它綠」這個最經典的作弊，過去是被 nova 自己的 verify 階段蓋章放行的。
