# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## nova 是什麼

**宿主反轉架構**：`nova = harness engineering[loop engineering[llm]]`

一般的看法是「LLM 是主角，外面包一層工具」。nova 反過來：**模型是被載體包住的元件**，
系統的行為由外層決定。中括號是包含關係——最外層是載體，載體裡面是迴圈，
迴圈裡面才是模型呼叫。

規格原文是 [`docs/AGENT_ARCHITECTURE.md`](docs/AGENT_ARCHITECTURE.md)。動手前先讀它；
本檔只寫**看檔案看不出來、踩到會很貴**的那些。

## 起手式

```bash
uv sync                          # 第一次，或改了相依之後
uv run nova 閘 提交               # commit 前的 7 條，第一個紅就停
uv run nova 閘 ci --全部跑完      # CI 那 8 條，一次看到所有紅的
uv run pytest -k <關鍵字> -x      # 內圈：只跑手上那幾支
uv run pytest -m 真cli            # 真的打三家 CLI，燒 token，兩個閘都排除
```

**一律走 `uv run`，不要先 activate venv**——忘了 activate 會靜默跑到系統 Python 3.9。

其餘子命令問 `uv run nova --help`；對外 API 的形狀看 `src/nova/__init__.py` 的 `__all__`。

## 這個 repo 的坑

### 分層是時間預算，不是分類學

`tests/單元/` 只放純函式，**會 fork 子程序的一律進 `tests/整合/`**。
`pytest tests/單元` 是提交閘唯一的測試規則，混一支 fork 進去就是每次 commit 都付那個錢
（實測混著 7.1 秒、分乾淨 1.2 秒）。由
`tests/驗收/test_專案骨架.py::test_單元層不准fork子程序` 擋著。

### 規則只寫一份

全部登記在 `src/nova/載體/規則表.py`，pre-commit／CI／agent hook 三處各只有一行呼叫 nova。
**不要往 YAML／JSON 裡塞邏輯**——設定檔裡的程式碼測不到，等於沒有保證。
平行度與階段排程的實測數字在 [`docs/設計/05-測試怎麼跑最快.md`](docs/設計/05-測試怎麼跑最快.md)。

### 三個會讓你以為有保證、其實沒有的地方

1. **squash 合併的 commit 訊息繞過 commit-msg hook**（訊息由 GitHub 從 PR 標題組出來）。
   只有本地加速器，沒有伺服器端保證。
2. **`gates` 這個 job 名稱是 main 保護規則的 required check context**，改了保護就失效。
3. **三種快取的鍵都不含內容**（ruff、mypy、Python 的 `__pycache__`），
   所以閘裡的 ruff 帶 `--no-cache`、mypy 帶 `--no-incremental`、
   跑測試前清掉 `__pycache__`。拿掉任何一個都會出現「閘綠、CI 紅」。
   **同一秒內改兩次、長度剛好一樣就中**——而長度剛好一樣在
   「一個字元換一個字元」這種改法上是常態。
   由 `tests/整合/test_閘不准吃快取.py` 背書。

### `pyproject.toml` 停用的那幾組 ruff 規則不要「修好」

`N`、`PLC2401`、`RUF001-003`、`D400/D415/D403` 內建「識別字只能是英文、標點只能是半形」
的假設，跟本專案的繁體中文命名衝突——開著會產生 266 條噪音把 8 條真問題淹掉。
理由寫在設定檔的註解裡。

### repo 是 public

洩漏一次就是永久的，GitHub 的快取與別人的 clone 收不回來。
`tests/驗收/test_機密不進版控.py` 同時掛在 pre-commit，所以 commit 當下就會擋。

## 三層在這個 repo 的落點

```
Harness  載體 → src/nova/載體/    判別法：把模型從圖上刪掉，還站著的都住這裡
  └── Loop  迴圈 → src/nova/迴圈/  七欄位缺 stop rule 的不是迴圈，是成本漏洞（§3.2）
        └── 模型呼叫與工具結果
邊上流動的結構化證據 → src/nova/契約/   跨層一律 schema 化，自由段落會讓歧義累積（§4.3）
```

**Graph 層現在故意不存在。** 它是 §10 建置順序的第 4 階——看過真實 trace、
隱式控制流程真的難測了才畫。在那之前新增 `src/nova/圖/`、節點註冊表、編排 DSL
都算違規（§8 反模式一：還不理解工作就先畫圖）。要開這一層，先拿出 trace。

## 判準：保證住在哪

判準**不是「誰呼叫誰」**，是**保證住在哪**。規則寫在 prompt 裡是虛線的懇求，
可以被忽略、漏了沒人發現；寫在包住執行者的程式碼裡才是實線的包圍環，
執行者「不用知道、不用記得、也違反不了」。所以**驗收權不在執行者手上**。

由此推出四條，違反時會產生**假的安全感**（比沒有規範更糟）：

1. **只以文件或 skill 形式存在的規範等於不存在。**
2. **文件若宣稱某件事「會被自動擋下」，把關器就必須存在且有測試背書。**
3. **墊片證明的是轉遞形狀，不是可達性。** 假 CLI 測得出參數有沒有傳對，
   測不出那個執行檔存不存在。要補一格用真 PATH 跑的端到端。
4. **用規避換來的綠是假綠。** 例：讓 `__all__` 對 `in` 說謊——兩支測試都綠，
   名字根本沒進去。

哪些已經是殼、哪些還不是，見 [`docs/設計/06-IHarness要補什麼.md`](docs/設計/06-IHarness要補什麼.md)。

## 載體的狀態住哪：歸屬與完整性是兩條軸

**「屬於某個專案」跟「存在那個專案裡面」是兩件事。** 存在裡面就等於交到執行者手上。

1. **會被餵回模型的東西，執行者不准碰。** 進度檔下一輪會變成 `前情`，
   放在工作目錄裡等於讓它替未來的自己種話——**實測過，模型真的會寫進去**
   （`檢查進度檔位置` fail-closed 擋著）。
2. **只給人看的東西，要按專案分。** 屬於哪個專案是**索引**問題不是**存放位置**問題：
   存在專案外面、用專案當鍵，兩件事同時成立。

nova 現在的工作目錄就是自己，所以它在完善自己——**這是同一條規則的兩個實例，
不是兩套規則。**

## 硬規則

1. **`docs/AGENT_ARCHITECTURE.md` 是外部規格原文，一個字都不准改。**
   要修正理解寫在別的檔案裡。
2. **不得以「模型說完成了」當停止條件**，不得建議「換更強的模型」當第一修復，
   **不得讓同一個對話自寫自評**（§8 反模式二、§12）。
   判準是**對話不是家族名**——同一家開新對話已經是不同的審查者，
   換一家是加分不是門檻。精確定義在 `tests/單元/test_換腦判準.py`。
3. **診斷順序：環境 → 回饋 → 流程。** 拿不到檔案／跨 session 掉狀態 → 載體；
   有動作但沒證據就停 → 迴圈；該跑的節點沒跑 → 圖。
   「agent 表現不好」不是可行動的診斷。
4. **新增保證要做一次固定負控**（故意破壞、看它紅、改回來），寫進 commit 訊息，
   並在 [`docs/負控紀錄.md`](docs/負控紀錄.md) 補一列。**那些不必重推**——
   懷疑某條防護時照著「破壞什麼」那欄做一次就好。

## 退出碼有四種語意

**0 成功、1 確定失敗、3 結果未知、4 護欄生效**（工作流才有）。

- **4 不是壞了**，是停止規則按設計生效（預算、步數、卡住、重構改壞行為）。
  外圈看到 4 **不准去「修」**——護欄最省事的修法是把上限調高，那是自己拆執法點。
  要放寬是人的決定。
- **3 是「不知道工作做了沒」**（逾時被殺、輸出解析不出來），**腳本不准重跑**——
  重跑會把可能已經做過的事再做一次。

## 設計文件在哪

| | |
|---|---|
| 機械化閘 | [`docs/設計/01-機械化閘.md`](docs/設計/01-機械化閘.md) |
| 三家 CLI 的旗標、型號、各家的坑 | [`docs/設計/02-統一LLM介面.md`](docs/設計/02-統一LLM介面.md) |
| 角色與 TDD 七階段 | [`docs/設計/03-角色與工作流.md`](docs/設計/03-角色與工作流.md) |
| 載體六個面 | [`docs/設計/04-載體要長什麼樣.md`](docs/設計/04-載體要長什麼樣.md) |
| 測試怎麼跑最快 | [`docs/設計/05-測試怎麼跑最快.md`](docs/設計/05-測試怎麼跑最快.md) |
| IHarness 還缺什麼、哪些已經是殼 | [`docs/設計/06-IHarness要補什麼.md`](docs/設計/06-IHarness要補什麼.md) |

**各家自帶的載體一律關到最小**（工具、session、家目錄設定），由
`tests/整合/test_模型轉接.py::Test把各家載體關到最小` 背書。
**旗標與型號全在 02，不要抄一份到這裡。**
