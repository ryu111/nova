11339       01:17 二  9月/ 1 02:23:13 2026   uv run nova 工作流 --提示檔 /Users/sbu/.local/state/nova/專案/nova-10449efd/收件/20260831T163151Z-mcp-nova-線-漏報一條確實在跑的線-而那是我的-4f9796.md --工作目錄 /Users/sbu/nova-wt-線漏報 --審查用 agy --預算token 25000000
11203 01:23 二 9月/ 1 02:23:07 2026 /Users/sbu/.local/bin/claude --print --output-format json --setting-sources  --strict-mcp-config --restricted --add-dir /Users/sbu/nova --tools Read,Write,Edit,Bash,Grep,Glob --allowedTools Read,Write,Edit,Bash,Grep,Glob --permission-mode acceptEdits --system-prompt  --effort high 你是 nova 工作流裡的實作員。上一步已經有一支紅的測試了。\012\012規矩：\0121. 用**最少**的程式碼讓那支測試變綠。這一步允許寫得醜。\0122. **不准改測試。** 測試紅了先假設是實作的問題。\0123. 不做沒被要求的抽象：一個實作不寫 interface，不會變的值不寫 config。\0124. 回覆時說明你改了哪些檔案。\012\012讀取策略：先定位、按需只讀真正需要的檔案，不要把整個工作目錄讀進來。\012**不要自己跑全套測試**，只跑跟這次改動直接相關的那幾支。\012驗證是 verify 階段的事，nova 下一階會替你跑一次完整的；\012你在這裡跑等於同一套跑兩次，而且每輪詢一次「跑完了沒」就把整份提示重送一次。\012\012專案規矩：繁體中文命名與註解，不准簡體字。\012\012---\012\012任務：# 排隊沒有順序：等 20 分鐘的可能輸給剛到的\012\012## 現況（讀過實作，2026-09-01）\012\012`src/nova/載體/閘鎖.py` 的 `_等到拿得到`（:109）是**非阻塞加固定間隔輪詢**：\012\012```python\012while True:\012    try:\012        fcntl.flock(fd, fcntl.LOCK_EX | fcntl.LOCK_NB)\012    except OSError:\012        if time.monotonic() - 起 >= 最多等幾秒:\012            raise 佔不到(...)\012        time.sleep(_每隔幾秒再試)\012    else:\012        return\012```\012\012走 `LOCK_NB` 的理由寫在 docstring 裡而且是對的：阻塞版 `flock` **沒有上限**，\012前面那個程序卡住就等到天荒地老，而且看不出在等什麼。\012\012**但輪詢完全沒有順序保證**，於是有三個問題：\012\012### 一、飢餓：等最久的不會先拿到\012\012鎖一放開，所有正在 `sleep` 的程序誰先醒誰先拿。醒的時機由各自的\012`sleep` 相位決定，跟等了多久**完全無關**。\012\012實測環境：2026-09-01 00:47，四條線在等同一把鎖，\012其中 `扇出接樹` 已經跑了 1:32:01、`兩份派法` 38:29。\012沒有任何機制保證前者先拿到——它可能繼續輸給後者，一輪又一輪。\012\012### 二、短工作排在長工作後面\012\012被鎖圈住的是 `subprocess.run`，而那條指令可能是：\012\012| 指令 | 大概多久 |\012|---|---|\012| `uv run pytest -q`（含 121 把變異刀） | 數分鐘 |\012| `ruff check --no-cache .` | 0.016 秒 |\012| `ruff format --check` | 0.014 秒 |\012\012`建重構判準` 會跑 ruff（`判準.py:111-120`）。**一個 0.016 秒的檢查\012可能排在一個數分鐘的 pytest 後面**，而且它拿到鎖之後只佔用 16 毫秒。\012\012### 三、在等什麼、等多久，外面看不到\012\012我今天要靠 `lsof /Users/sbu/.local/state/nova/鎖/閘.lock` 才知道有四條在排隊，\012再靠 `ps` 的 `%cpu` 與子程序才推斷出誰是持有者。\012`nova 線` 對排隊中的線只印「在跑嗎：是」，看不出它其實在睡。\012\012## 這一格不做的事（重要）\012\012**不准把鎖拿掉、也不准調鬆上限。** 鎖存在的理由寫在模組 docstring 裡：\012機器超載時變異刀的 `最多秒=2.0` 逾時會被判成「這把刀沒被殺掉」——\012**一支好好的測試被報成壞的，而下一步通常是有人去把那支測試「修好」**。\012那個假紅比排隊貴得多。\012\012**不准改成計數式鎖**（同時允許 N 條）——那是另一張票（任務 39 的乙半），\012而且它跟「順序」是兩個獨立的問題：計數鎖照樣可以不公平。\012兩件混在一起的話，改善的效果分不出是誰的功勞。\012\012## 輸入\012\012- `src/nova/載體/閘鎖.py` 全部（`佔住`、`_等到拿得到`、`鎖檔路徑`、\012  `預設最多等幾秒`、`_每隔幾秒再試`）\012- `src/nova/載體/判準.py:44-62`（`_佔住機器跑`，唯一的判準側呼叫端）\012- `src/nova/載體/命令列.py` 的 `_子命令_閘`（佔不到回 3）\012- `tests/整合/` 底下現有的閘鎖測試\012- `docs/負控紀錄.md:2157-2247`（**已經記過**：閘鎖只有 1 把刀，\012  另外兩把登記不成的原因是 subprocess 的 coverage 追不到、refcount 先放鎖。\012  **不要重推那兩個結論**）\012\012## 輸出\012\012排隊要有順序。**先到先得（FIFO）是基準**，因為它是唯一能證明「不會餓死」的形狀。\012\012至少要處理的兩件事：\012\0121. **等最久的先拿到。** 現在的相位輪詢做不到，所以需要一個共享的排隊狀態\012   （例如鎖目錄底下的號碼牌檔，或另一把「發號」鎖）。\012   **不准用「隨機退避」當答案**——它降低碰撞率但不保證順序，\012   而不保證順序就是這張票要修的東西。\0122. **等待狀態要看得見。** 至少讓 `nova 線` 分得出「在跑」與「在等鎖」，\012   並印出等了多久。**這一格的實際價值一半在這裡**——\012   今天我是靠 `lsof` 加 `ps` 手動推斷出四條在排隊的，\012   下一次沒有人會想到去 `lsof` 一個鎖檔。\012\012**保住三件既有的保證**（它們都有理由，改壞了會回到更糟的狀態）：\012- 等待有上限，超過就 `佔不到` → 呼叫端回 **3（結果未知）**，不是紅\012- 程序被 `kill -9` 之後鎖自動放掉（這是選 `flock` 而不是「檔案存不存在」的理由）\012- 鎖檔在 `狀態根目錄()/鎖/` **不含專案識別**（每個 worktree 被當成不同專案，\012  跟著專案走就是「三把鎖，零保護」）\012\012**短工作優先**是加分不是門檻：它需要呼叫端申報「我大概要跑多久」，\012而那個數字會騙人。**如果做，要有測試證明它不會讓長工作餓死**；\012沒把握就別做，FIFO 已經解掉最貴的那個問題。\012\012## 驗收\012\012<!--nova:驗收 uv run nova 閘 提交-->\012<!--nova:驗收 uv run pytest tests/單元 tests/整合 -q-->\012\012## 停止\012\012- **要有一支測試證明 FIFO**：三個等待者按順序進來，持有者放鎖之後，\012  **第一個進來的先拿到**。\012  這支要能真的抓到亂序——所以三個等待者的 `sleep` 相位要刻意錯開\012  （現在的實作正是相位決定勝負，測試不錯開相位就可能碰巧綠）。\012  先寫、先跑、**親眼看到它紅**。\012- **要有一支測試證明不餓死**：一個等待者在場的同時，讓別人反覆搶鎖 N 次，\012  斷言那個等待者在 N 次之內拿到。\012- **要有一支測試證明上限還在**：拿不到就 `佔不到`，而且呼叫端收到的是\012  「跑不起來」不是「紅」。\012- **固定負控**：把 FIFO 的排序條件改掉（例如改成後到先得），\012  跑第一支測試，**親眼看到它紅**，再改回來。\012  過程寫進 commit 訊息、補一列到 `docs/負控紀錄.md`。\012- **測試不准依賴真實的時間長度**（`sleep 3` 之類）——那種測試在忙的機器上\012  會變成 flaky，而 flaky 的護欄測試遲早被人加 `skip`。\012  用可注入的時鐘或事件，不要用牆鐘。\012- **會 fork 子程序的測試一律放 `tests/整合/`**（`tests/單元` 是提交閘的規則，\012  混一支 fork 進去每次 commit 都付那個錢）。\012- **不准動 `_每隔幾秒再試` 的值來「改善」**——那不是這張票的問題，\012  而且調小會讓等待者空轉吃 CPU，正好害了它們原本在等的那件事。\012\012你現在的階段：最少的程式碼讓它綠\012\012上一步（verify-green）的結果：\012<<<UNTRUSTED ==================================== ERRORS ====================================\012__________________ ERROR collecting tests/整合/test_判準不跑負控刀.py ___________________\012ImportError while importing test module '/Users/sbu/nova/tests/整合/test_判準不跑負控刀.py'.\012Hint: make sure your test modules/packages have valid Python names.\012Traceback:\012tests/整合/test_判準不跑負控刀.py:30: in <module>\012    from nova.載體.規則表 import 負控檔們, 負控檔排除參數\012E   ImportError: cannot import name '負控檔們' from 'nova.載體.規則表' (/Users/sbu/nova/src/nova/載體/規則表.py)\012=========================== short test summary info ============================\012ERROR tests/整合/test_判準不跑負控刀.py\012!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!\01237 deselected, 1 error in 0.24s\012E   NameError: name '工作目錄 is not defined UNTRUSTED>>>
11344       01:17 二  9月/ 1 02:23:13 2026   /Users/sbu/nova/.venv/bin/python3 /Users/sbu/nova/.venv/bin/nova 工作流 --提示檔 /Users/sbu/.local/state/nova/專案/nova-10449efd/收件/20260831T163151Z-mcp-nova-線-漏報一條確實在跑的線-而那是我的-4f9796.md --工作目錄 /Users/sbu/nova-wt-線漏報 --審查用 agy --預算token 25000000
