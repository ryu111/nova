"""守「成果帳答得出這一輪的驗收」的那幾把刀。

`#178` 讓驗收紅回護欄碼 4，但帳上看不出那個 4 是怎麼來的——
護欄有好幾種來源（預算、步數、卡住、重構改壞行為、驗收紅），
而「模型每次都撞上限」與「模型每次都說做完了但驗收不過」的修法完全相反。
"""

from pathlib import Path

from tests.負控.登記 import 替換一次, 變異

登記 = (
    #: 沒跑驗收（人給的票）跟驗收全綠是兩件事。塌成一種的話，
    #: 「這件沒有驗收機制」會長得像「這件驗收過了」。
    變異(
        識別="沒跑驗收與驗收全綠塌成一種",
        目標檔=Path("src/nova/契約/成果.py"),
        操作=替換一次(
            "    驗收: tuple[驗收紀錄, ...] | None = None",
            "    驗收: tuple[驗收紀錄, ...] = ()",
        ),
        該紅=("tests/單元/test_成果.py::test_沒跑驗收與驗收全綠不准長得一樣",),
        最多秒=15.0,
    ),
    #: 只記「紅了」不記「哪一條紅了」的話，一張宣告三條驗收的票，
    #: 事後查不出是哪一條擋住的。
    變異(
        識別="成果帳不記驗收那一欄",
        目標檔=Path("src/nova/契約/成果.py"),
        操作=替換一次('    ("驗收", _驗收鍵, None),', "    "),
        該紅=("tests/單元/test_成果.py::test_帳上答得出這一輪跑了哪幾條驗收與哪一條擋住了",),
        最多秒=15.0,
    ),
)
