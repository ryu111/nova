"""守「派工直接搶收件匣裡的原票，不另外落新票」的負控刀。

派工給收件匣內的票時，若退回「落新票再搶」，收件匣會產生新票副本，
且待處理票張數不減，違反張數守恆。
"""

from pathlib import Path

from tests.負控.登記 import 替換一次, 變異

_該紅 = "tests/整合/test_派工走收件匣.py::test_派收件匣裡的票直接搶原票且張數守恆"

登記 = (
    #: 把匣內票直接搶原票改回落新票再搶，張數守恆測試必須紅。
    變異(
        識別="派工匣內票退回落新票再搶",
        目標檔=Path("src/nova/載體/命令列.py"),
        操作=替換一次(
            "    if 票路.parent.resolve() == 收件匣.resolve():",
            "    if False:",
        ),
        該紅=(_該紅,),
        最多秒=120.0,
    ),
)
