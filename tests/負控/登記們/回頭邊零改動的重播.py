"""守「回頭邊退回的模型階段零改動時，下一次判準是確定性重播、不准跑」那把刀。

判定（`狀態機.上一次同一個判準紅在第幾步` 與 `工作流._回頭邊後的重播`）留著、
常數留著、理由字串留著，只把主迴圈裡「照這個判定收線」那一句拿掉——
**算得出是重播卻照樣跑**。這正是最容易假綠的形狀：判定看起來一應俱全，
沒有人照它的結果停。

錨點是主迴圈那條「沒進展」串接裡的重播那一項：刪掉它之後 `連續審查要求修改`
與 `卡住了` 照舊串在一起，語法仍然成立，所以紅的一定是行為、不是 SyntaxError。

刀砍下去之後那次判準會真的再跑一遍同一棵樹，兩支行為測試各自紅在自己的計數器上。
"""

from pathlib import Path

from tests.負控.登記 import 刪除一次, 變異

登記 = (
    變異(
        識別="重播照樣跑判準",
        目標檔=Path("src/nova/迴圈/工作流.py"),
        操作=刪除一次(
            "            or _回頭邊後的重播("
            "定義, 軌跡快照, tuple(工作區雜湊們), 任務.工作目錄, 階段表)\n"
        ),
        該紅=(
            "tests/單元/test_無進展護欄.py::test_退回實作後工作區雜湊未變就不跑下一次判準",
            "tests/單元/test_無進展護欄.py::test_退回測試員後工作區雜湊未變也不跑下一次判準",
        ),
        最多秒=60.0,
    ),
)
