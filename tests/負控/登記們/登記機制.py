"""守「登記們／收集機制」本身的那幾把刀。

**這個檔放在 `登記們/` 底下是刻意的**：新機制要用新機制自己背書。
放回 `登記.py` 的話，證明的是舊路徑還能動，證不到收集這條路。
"""

from pathlib import Path

from tests.負控.登記 import 替換一次, 變異

登記 = (
    #: 漏收不報錯就是假綠：`登記們/` 底下多一個檔卻沒被收進來，
    #: 那幾把刀等於不存在，而且沒有人會發現。
    變異(
        識別="登記們漏收了也不報錯",
        目標檔=Path("tests/負控/登記.py"),
        操作=替換一次(
            '        訊息 = f"登記們/{檔.name} 沒有 登記；漏收不報錯就是假綠"',
            "        return ()",
        ),
        該紅=("tests/負控/test_登記們收集.py::test_模組沒有登記屬性要當場報錯",),
        最多秒=20.0,
    ),
)
