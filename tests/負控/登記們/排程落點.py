"""守「排程不准住在要被巡的那棵樹裡」那一把固定負控刀。

這一格壞掉的樣子是**外表毫無異狀**：plist 印得出來、`launchctl load` 收得下去、
排程也真的會醒——只是它站在主工作區裡。平常一切正常，等到那棵樹壞掉
（rebase 到一半、conflict 沒收、被 `git clean` 掃過）的那一天，排程跟著一起壞，
而那正是最需要它還活著的時刻。

守門的那一格拿掉不會讓別的測試變色（`WorkingDirectory` 仍然是一個合法字串），
所以釘一支具名測試。錨點吊在 `排程設定` 的 `def` 那一行，**不吊在函式本體裡
會被重寫的某一行**。
"""

from pathlib import Path

from tests.負控.登記 import 替換一次, 變異

_落點那支 = "tests/單元/test_排程不准住主工作區.py::test_工作目錄在專案底下就raise"

登記 = (
    #: **票指定的那一把**：在真的 `排程設定` 前面插一個同名前置樁，把 `專案`
    #: 換成一個絕不會包含任何工作目錄的路徑（原本那支被改名，沒有人再叫得到它）
    #: ＝「工作目錄在專案底下」的比較永遠不成立，守門的那一格等於不在。
    變異(
        識別="排程設定看不見真的專案等於工作目錄不設防",
        目標檔=Path("src/nova/載體/排程.py"),
        操作=替換一次(
            "def 排程設定(",
            "def 排程設定(**參: object) -> str:\n"
            "    # 前置樁：換一個不可能包住工作目錄的專案進去，\n"
            "    # 「工作目錄不准在專案底下」那一格就永遠過得了。\n"
            '    參["專案"] = Path("/nova負控/不存在的專案")\n'
            "    return _原本的排程設定(**參)  # type: ignore[arg-type]\n"
            "\n"
            "\n"
            "def _原本的排程設定(",
        ),
        該紅=(_落點那支,),
        最多秒=120.0,
    ),
)
