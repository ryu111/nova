"""守「收件匣自己會看時鐘」的兩把固定負控刀。

這條路壞掉的樣子都是**外表毫無異狀**：

1. 時間門形同不在——`不早於` 照樣寫在票上、欄位讀得出來、`nova 狀態` 一切正常，
   只是下一次醒來就把票搶走、再撞一次同一個額度、再燒一次錢。
2. 等額度的界限漂一格——功能全對，只是一天最後那個 5 小時視窗沒人等，
   票沒排、狀態看起來就像做完了。

兩件事都不會讓別的測試變色，所以各釘一支具名測試。錨點吊在函式的 `def` 那一行
與那一整條運算式上，**不吊在會被重寫的函式本體裡的某一行**。
"""

from pathlib import Path

from tests.負控.登記 import 替換一次, 變異

_時間門那支 = "tests/整合/test_收件匣等重置時間門.py::test_不早於還沒到收下一件就不放行到了才放行"
_界限那支 = "tests/整合/test_收件匣等重置時間門.py::test_第六個等待窗還在額度上限內就要排得出去"

登記 = (
    #: **票指定的第一把**：在真的 `時候到了嗎` 前面插一個同名前置樁，一律回 `True`
    #: （原本那支被改名，沒有人再叫得到它）＝不看時間，票到手就搶。
    #: 2026-09-02 那天四條線就是這個狀態：`不早於` 是純裝飾。
    變異(
        識別="時候到了嗎一律放行等於沒有時間門",
        目標檔=Path("src/nova/載體/收件.py"),
        操作=替換一次(
            "def 時候到了嗎(",
            "def 時候到了嗎(不早於: str, *, 現在: datetime) -> bool:\n"
            "    # 前置樁：不看時間，票到手就搶。\n"
            "    return True\n"
            "\n"
            "\n"
            "def _原本的時候到了嗎(",
        ),
        該紅=(_時間門那支,),
        最多秒=120.0,
    ),
    #: **票指定的第二把**：等額度的界限從 `>` 漂成 `>=`＝第六個視窗不准等，
    #: 直接落到人身上——而那是一天的最後一個窗，通常人正在睡覺。
    變異(
        識別="額度等待界限漂一格砍掉第六個視窗",
        目標檔=Path("src/nova/載體/收件.py"),
        操作=替換一次(
            "return 額度等待 > 額度等待最多",
            "return 額度等待 >= 額度等待最多",
        ),
        該紅=(_界限那支,),
        最多秒=120.0,
    ),
)
