"""守住正式判準的等待資料不能在載體層被丟掉。"""

from pathlib import Path

from tests.負控.登記 import 替換一次, 變異

_測試 = "tests/整合/test_閘鎖等待承載.py::test_正式判準的等待毫秒要流進工作流步驟結果"

登記 = (
    #: `src/nova/載體/判準.py:210` 若把正式判準回報的等待改寫成 0，
    #: 工作流就再也收不到兩條判準實際排過的 2,000 毫秒。
    變異(
        識別="正式判準把回報的等待改寫成零",
        目標檔=Path("src/nova/載體/判準.py"),
        操作=替換一次(
            "            等過幾毫秒 = 毫秒",
            "            等過幾毫秒 = 0",
        ),
        該紅=(_測試,),
        最多秒=60.0,
    ),
)
