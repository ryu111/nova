"""守「同一類失敗第二次才算模式」的那幾把刀。

第一次失敗是雜訊。門檻設 1 等於每個失敗都算，清單當天就淹掉；
接力鏈算成三次的話第一次執行就直接過門檻，門檻等於沒有。
"""

from pathlib import Path

from tests.負控.登記 import 替換一次, 變異

登記 = (
    變異(
        識別="重複失敗門檻降到一",
        目標檔=Path("src/nova/載體/重複失敗.py"),
        操作=替換一次("重複門檻 = 2", "重複門檻 = 1"),
        該紅=("tests/整合/test_重複失敗.py::test_只出現一次的不算",),
        最多秒=20.0,
    ),
)
