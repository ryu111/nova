"""守「錨點失效的訊息帶得出登記檔名」那把刀。

錨點被先併入的票改掉時，`registered-mutation` 只報 `RUN_ERROR：錨點應恰好一次`
——人要在 192 筆登記裡自己找那把刀住哪個檔。`登記來源` 就是為了補這一格：
`_擋掉重複識別` 收 `_收集` 那次傳進來的 `檔.name`，導出成模組層對照。

**這把刀砍的是「查對照」那一行，不是對照本身。** 對照是 import 時就建好的，
coverage 在 `-p tests.負控.執行器` 底下只在測試執行期開著，錨在建表那一行會被
收成 `WRONG_TEST`；查表這一行則在體檢跑到那筆壞錨點時才執行，追得到。

砍法是**把查表換成寫死目錄名**：查不查表都回 `"登記們/"` 的話，
`登記們/` 底下那些刀會拿到一模一樣的目錄名，等於沒說。

這把刀對錨法有兩個要求（用標記錨就是為了不必寫死整行原文）：

1. `tests/負控/登記.py` 裡「查 `登記來源` 拿這把刀的登記檔名」那一行，
   行尾掛 `  # 負控錨:訊息帶登記檔名`。
2. 那一行是函式體第一層（4 個空格）、把查到的來源綁到區域名 `來源`
   ——刀把整行換成「不查表、一律回目錄名」的寫法。
"""

from pathlib import Path

from tests.負控.登記 import 替換標記行, 變異

登記 = (
    #: 訊息只到 `登記們/` 的話跟沒說一樣：那把刀還是得靠人 grep 才找得到家。
    變異(
        識別="登記來源照抄外層拿不到檔名",
        目標檔=Path("tests/負控/登記.py"),
        操作=替換標記行(
            標記="負控錨:訊息帶登記檔名",
            變成='    來源 = "登記們/"  # 負控錨:訊息帶登記檔名',
        ),
        該紅=("tests/負控/test_跑前體檢.py::test_錨點失效的訊息帶得出登記檔名與命中次數",),
        最多秒=120.0,
    ),
)
