"""守「迴圈在進狀態機之前把上游額度岔開」那一格。

這一格壞掉的樣子跟今天的 main 一模一樣，而且**外表毫無異狀**：工作流照樣跑完、
照樣收場、照樣寫成果帳，只是收在 `結束代碼.中止`（退出碼 1＝東西壞了、人來修）。
沒有東西可修，於是那條線就躺著——2026-09-02 那天四條線各躺了 72 分鐘。

**錨點為什麼吊在 `def <名字>(` 上，不吊在函式本體**：本體是這一格的實作，
它會被重寫（早收晚收、判準怎麼寫都還在動）；被重寫過的錨點會讓刀找不到目標
變成 `RUN_ERROR`，護欄反而沒人守。函式簽章那一行是這格的介面，動它就是換介面。

樁怎麼做到「一律回 `None`」：把同名的前置樁插在原函式之前，並讓原本那支換一個
名字——Python 後定義的名字會蓋掉前面的，不改名的話樁形同不存在（刀會 SURVIVED，
但原因是刀沒咬到，不是保證還在）。
"""

from pathlib import Path

from tests.負控.登記 import 替換一次, 變異

_額度那支 = (
    "tests/整合/test_額度收成等重置.py::test_假claude回429時工作流收成額度而不是中止且重置時戳正確"
)

登記 = (
    #: **票指定的那一把**：短路那一格一律回 `None` ＝ 額度掉回狀態機，
    #: 所有確定失敗被壓成同一種，收場變 `中止`。
    變異(
        識別="額度短路那一格一律回None讓額度掉回狀態機",
        目標檔=Path("src/nova/迴圈/工作流.py"),
        操作=替換一次(
            "def _額度就等重置(",
            "def _額度就等重置(*_參數: object, **_具名: object) -> None:\n"
            '    """負控樁：這一格不在的時候，額度就是一個普通的確定失敗。"""\n'
            "    return None\n"
            "\n"
            "\n"
            "def _額度就等重置_被樁蓋掉的(",
        ),
        該紅=(_額度那支,),
        最多秒=120.0,
    ),
)
