"""守「顧問只搬證據，不替診斷先猜修法」的那把刀。

票指定的固定負控：在 `顧問.py` 加一張按原因分支的對照表
（`{"touched-tests": "修分類器"}`）並讓素材用它。那正是這一格最容易走歪的方向
——今晚 `touched-tests` 撞了 17 次，直覺會生出「修測試檔分類器」，
而實證根因是**路由**（紅送錯角色），修分類器一行都不會治到。

刀砍在素材模板那句固定文字上：它是模板裡唯一「對所有原因逐字一致」的承諾點，
被塞進一個以原因為鍵的查表之後，兩份素材挖掉資料欄就不再相同，
`test_brief不含任何按原因分支的措辭` 必須當場紅。

錨點是那句固定文字的中段（`顧問.py` 裡只出現一次），替換成同一句話加上
一個以 `原因` 為鍵的內嵌查表——**兩種失敗都是響的**：錨點對不上會在跑前體檢
當場炸，套用了卻沒讓那支測試變紅會報 SURVIVED。沒有一種是靜靜變綠。
"""

from pathlib import Path

from tests.負控.登記 import 替換一次, 變異

_那支模板 = "tests/單元/test_顧問.py::test_brief不含任何按原因分支的措辭"

登記 = (
    變異(
        識別="顧問按護欄原因分支猜修法",
        目標檔=Path("src/nova/載體/顧問.py"),
        操作=替換一次(
            "都收在同一個護欄原因。請診斷根因",
            "都收在同一個護欄原因（疑似 "
            "{ {'touched-tests': '修分類器'}.get(原因, '不明') }）。請診斷根因",
        ),
        該紅=(_那支模板,),
        最多秒=60.0,
    ),
)
