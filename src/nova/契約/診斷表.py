"""規格 §7「按症狀診斷表」的程式碼版：症狀 → 負責層 → 修復方向。

存在的理由是「這次失敗屬於哪一層」現在只活在提示裡：是虛線的懇求，
查不到、也沒有東西擋著它漂走。搬進契約層之後它是純知識——不碰模型、
不碰 I/O，所以 `契約` 不准 import `載體` 或 `迴圈` 的架構閘照樣過得去。

**這份資料是抄的，來源是 `docs/AGENT_ARCHITECTURE.md` §7。**
抄寫會腐爛，所以 `tests/驗收/test_診斷表對齊規格.py` 當場去讀規格原文、
parse 那張 markdown 表、跟這裡逐列比對。規格哪天多一列或改了修復方向，
那支測試會紅——**改這裡之前先確認規格真的改了**，不要反過來遷就程式碼。

列的次序照規格排，不是集合：表是拿來「查症狀」的，次序即優先順序。
"""

from dataclasses import dataclass


@dataclass(frozen=True)
class 診斷列:
    """一個症狀對應的負責層與修復方向。"""

    #: 觀察得到的現象，查表的鍵。用規格的原文，不要改寫得比較順口。
    症狀: str
    #: 擁有這個失敗的層。多層並列代表兩層都得動，不是二選一；
    #: `簡化` 不是層，是規格對「還不該畫圖」那一列給的處方。
    負責層: tuple[str, ...]
    #: 往哪裡修。**不是解法**，是該去翻哪一類機制。
    修復方向: str


#: 規格 §7 的九列，次序與字句與規格一致。
診斷表: tuple[診斷列, ...] = (
    診斷列(
        症狀="無法安全取得正確的資料或工具",
        負責層=("Harness",),
        修復方向="工具契約、權限、沙箱、context 注入",
    ),
    診斷列(
        症狀="跨 session 忘記進度",
        負責層=("Harness",),
        修復方向="持久狀態、檢查點、進度工件、壓縮策略",
    ),
    診斷列(
        症狀="第一次嘗試接近但不穩定",
        負責層=("Loop",),
        修復方向="外部評分器、確定性測試、回饋、有界重試",
    ),
    診斷列(
        症狀="過了成功點還在做/沒證據就停",
        負責層=("Loop",),
        修復方向="證據型終止狀態、預算感知停止規則",
    ),
    診斷列(
        症狀="多個專家需按受控順序執行",
        負責層=("Graph",),
        修復方向="明確節點、邊、路由條件、join",
    ),
    診斷列(
        症狀="多步驟流程中失敗難定位",
        負責層=("Graph", "Harness"),
        修復方向="對齊圖節點的有狀態追蹤",
    ),
    診斷列(
        症狀="中斷後工作消失",
        負責層=("Harness", "Graph"),
        修復方向="狀態持久化與檢查點邊界",
    ),
    診斷列(
        症狀="分數上升但實際品質下降",
        負責層=("Loop", "Harness"),
        修復方向="評估器或改進邊界過擬合",
    ),
    診斷列(
        症狀="流程變動太頻繁,固定圖畫不住",
        負責層=("簡化",),
        修復方向="簡化 harness、控制權留給模型、延後圖形化",
    ),
)
