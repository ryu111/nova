"""派工的契約：一種工作，一條腦鏈。

**這是策略，所以它是資料不是提示。** 使用者的原話是「多把工作交給 agy，
需要動腦推理的給 sol max」——那句話寫在 CLAUDE.md 裡是懇求（我會忘、
會在趕時間時直接用手邊那顆），寫成表加上會紅的測試才是保證。

分類法刻意只有兩格。三格以上就會開始出現「這件事算哪一格」的爭議，
而爭議會讓人乾脆不用它。等真的有第三種工作**而且分不進現有兩格**再加
（`docs/決策/0001-規則要住哪一層.md` 講的同一條：先有重複才有抽象）。
"""

from dataclasses import dataclass
from enum import StrEnum


class 工作種類(StrEnum):
    """這件事需要動腦嗎。值 ASCII——會出現在 CLI 旗標與帳本裡。"""

    #: 讀檔、摘要、格式轉換、照現成樣子寫。答案對不對看得出來。
    例行 = "routine"
    #: 設計取捨、審查、找漏洞。**答案對不對要靠推理才看得出來。**
    推理 = "reasoning"


@dataclass(frozen=True, slots=True)
class 派法:
    """派給誰、用哪顆模型。

    `腦們` 是**一條鏈**不是一家：前一顆失敗換下一顆，這才叫分擔額度。
    只有一顆的鏈是單點故障。
    """

    腦們: tuple[str, ...]
    #: `None` ＝ 用那一家的預設。**推理類不准是 None**——
    #: 不指名就會拿到便宜的預設模型，而且看起來完全正常。
    模型: str | None = None
